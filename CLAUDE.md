# AI-Assistant（前端 Demo 协同约定）

## 项目简介

AI-Assistant —— 面向企业专业岗位的「AI 同事」应用的**管理后台前端交互 Demo**。本仓库已于 2026-09-01 由发布版本包（管理后台 + 后端服务 + 配置分发契约层）降级为纯前端 demo：后端与部署编排已整体移除，仅保留 Vue 3 管理后台前端，用于演示岗位 / 技能 / 连接器（MCP·API）/ 知识库 / 记忆等能力的产品交互。项目概览与启动步骤见 `README.md`。

## 重要约束

- **技术栈只用下列已定栈，不擅自新增技术栈 / 语言 / 运行时**；确需新增，先停下说明原因并征得项目负责人同意（普通 npm 库/依赖属正常项目依赖，可正常使用）。
- **Git 协作（2026-09-02 起恢复）**：工程托管于 GitHub `https://github.com/kira-chen/iWorker-Console.git`（多人共享）。原「不做 Git 管理」拍板（2026-09-01）自本日废止。常规 git 操作（add/commit/pull/push/分支）可正常执行；**不改 Git 历史（rebase/force push 需负责人同意）**。`node_modules/`、`frontend/dist/`、`版本包-*.zip` 不入库（见 .gitignore）；改动记录仍以 `docs/PRD-review/` 为准，git 提交说明与之并行不替代。
- **不得重新引入后端**：不新增服务端代码 / 数据库 / 部署编排。页面数据一律走前端侧 mock 层（参考 `frontend/src/api/knowledgeBaseMock.js` 模式），保持 demo 纯前端可运行。
- 历史文档（原对外契约 / 架构设计 / 旧 PRD / 前端设计稿 / 旧更新日志等）已统一打包为 `docs/历史文档归档-20260901.zip`（2026-09-01 归档）：**仅作参考、不再构成约束，不要解包回工作区**；原「接口契约变更铁律」随发布单元一并废止。docs/ 只保留 `prd/PRD-20260828/` 与 `PRD-review/` 两个工作目录。
- **改动记录统一写 `docs/PRD-review/YYYY-MM-DD.md`**（2026-09-01 起，北京时间自然日）：三段式「需求/问题 → 方案 → 影响面」，PRD 对齐类工作附待拍板差异清单。`docs/update/` 与 `docs/API迭代需求/` 已停更，仅作历史存档，不再写入。

### PRD 对齐铁律（2026-09-01 负责人拍板）

- 对齐基准 = 各模块 prd md 文字稿 + `docs/prd/PRD-20260828/` **根目录**的《数字员工管理端-模型MCP用户-交互原型-v2.html》交互原型，**两者内容应一致**；模块目录里的截图**只是参考，不作依据**。
- md 与交互原型如发现不一致，视为 PRD 内部缺陷：不要自行取舍，记入 `docs/PRD-review/` 待负责人裁决。
- 根目录另一共用文件《各模块必填选填字段一览表.md》是全模块必填/选填字段的权威一览，做表单校验对齐时须一并核对。
- 交付标准：不需要真实实现，达到「文字表述 + 可交互原型示意」即可（mock 数据层支撑交互）。

## 技术栈

- **前端：** Vue 3 + Vite + Pinia + Element Plus + Axios

## 前端编码规范

- 使用 Vue 3 Composition API（`<script setup>` 语法）
- 组件化开发，单一职责原则
- 状态管理统一使用 Pinia
- UI 组件库采用 Element Plus
- 统一封装 HTTP 请求（axios 拦截器，见 `frontend/src/api/request.js`）
- CSS 采用 Scoped CSS 方案
- 关键交互需有 loading 态和错误态处理
- 文件命名：组件 PascalCase，工具函数 camelCase
- 视觉与交互延续既有设计语言（Notion 风格令牌与双主题）：以 `frontend/src/assets/` 下的样式令牌与现有页面范式为准（原设计规范文档在历史归档 zip 内，需要时查阅）
- Mock 数据放在前端 api 层旁路文件中（`xxxMock.js`），不散落进组件

## 安全红线

- 任何真实密钥 / 令牌绝不写入代码 / 文档 / 日志（demo 无后端，正常情况下不应出现密钥需求）
- 数据库备份、运行落盘数据等历史产物不放入本项目目录

## 可选：角色分工参考

`.claude/agents/` 内附一套按角色划分的 Claude Code 子 agent 定义（2026-09-01 已按 demo 定位重组为 6 人班底：产品经理[PRD 对齐审查员] / 架构师[前端架构与 mock 层] / 前端开发[demo 工程主力] / UI 设计师 / 测试工程师[原型验收员] / 最终用户），`.claude/commands/pm.md` 为配套的任务分解命令（含 PRD 对齐轮标准流程）。原后端 / 大模型专家 / 提示词工程师三角色已裁撤（定义存于历史归档 zip）。这是可选的工作方式参考，不构成对协作流程的强制约定。
