# AI-Assistant（前端 Demo 协同约定）

## 项目简介

AI-Assistant —— 面向企业专业岗位的「AI 同事」应用的**管理后台前端交互 Demo**。本仓库已于 2026-09-01 由发布版本包（管理后台 + 后端服务 + 配置分发契约层）降级为纯前端 demo：后端与部署编排已整体移除，仅保留 Vue 3 管理后台前端，用于演示岗位 / 技能 / 连接器（MCP·API）/ 知识库 / 记忆等能力的产品交互。项目概览与启动步骤见 `README.md`。

## 工程大原则（2026-09-10 负责人拍板，一切工作与方案的评判基准）

本工程 = **前端代码开发 + 浏览器数据轻量化存储实现产品 DEMO**，服务两个目标：

1. **完整表达产品经理对功能设计的想法**——交互示意的完整与准确优先；
2. **通过代码示例的方式让研发更容易读懂**——代码本身是给研发看的参考实现，可读性是交付物的一部分。

后续所有工作、技术方案、取舍裁决均以此为基准：凡与这两个目标都不沾边的投入（如生产级性能/安全/部署优化）默认不做；技术债治理以「可读性收益」为取舍标准。

## 重要约束

- **技术栈只用下列已定栈，不擅自新增技术栈 / 语言 / 运行时**；确需新增，先停下说明原因并征得项目负责人同意（普通 npm 库/依赖属正常项目依赖，可正常使用）。
- **Git 协作（2026-09-02 起恢复）**：工程托管于 GitHub `https://github.com/kira-chen/iWorker-Console.git`（多人共享）。原「不做 Git 管理」拍板（2026-09-01）自本日废止。常规 git 操作（add/commit/pull/push/分支）可正常执行；**不改 Git 历史（rebase/force push 需负责人同意）**。**提交与推送节奏（2026-09-10 负责人拍板）**：本地按「一个验证过的闭环批次 = 一次 commit」小步提交（每次 commit 前全量测试 + 构建须绿），保留细粒度可回溯历史；**推送不自动、不逐条**——仅在负责人指令下将阶段版本一次性推送（推送前过质量门禁：全量测试绿、build 通过、无密钥外泄、无冲突）。**提交粒度：一件事一条，同一件事的补丁在推送前并入（2026-09-10 负责人拍板）**——判据是「事」不是「天」：一天做完一件事就一条，一天处理了七八件不同的事就该有七八条，**不按天打包合并**。理由：①出问题回退时一条 = 一件事，撤这条即可，大包会连带撤掉无关改动；②裁决可追溯（如「九页签选 C」「T1/T2/T3 不做」各自成条，日后查决策来由一条 git log 到位）；③跨人交接的改动（如替他人补测试基础设施）必须单独且指名，混进大包对方找不到。**该合的是同一件事被拆碎的补丁**（「改按钮样式」→「补漏一处」→「再修一处」应并成一条），这类碎片在推送前自行合并，不留给他人阅读。**拉取与冲突处理规范（2026-09-10 负责人拍板）：「开工先拉、推前再核」**——①每个开发任务/批次**启动前**先 `git pull` 拉齐远端，基于最新代码开发，不做「先开发、提交时再解决冲突」；②开发期间远端若有他人新提交（长任务在下一批次开工前会再拉到），以及**每次推送前**必须先 fetch 比对——有分叉就先合并（merge，不 rebase）、合并后全量测试跑绿，再推；③合并遇到实质冲突（同文件同处双方都改）优先与对方沟通归属，不盲目取舍；已知他人正在改的文件不并行硬改，确需并行走隔离 worktree。`node_modules/`、`frontend/dist/`、`版本包-*.zip` 不入库（见 .gitignore）；改动记录以 git 提交说明为准（`docs/PRD-review/` 已于 2026-09-17 停用）。
- **PR 流程与 CI（2026-09-15 负责人拍板启用）**：改动经 Pull Request 进 main，不再直推。
  - **日常四步**：①`git switch -c <类型>/<简述>` 开临时分支（如 `fix/task-name-counter`）；②推分支 `git push -u origin <分支名>`；③在 GitHub 开 PR，点 **Enable auto-merge**；④走开——CI 绿则自动合并、临时分支自动删除，CI 红则停住等修。本地「commit 前全量测试 + 构建须绿」的规矩不变，PR 只是多一道兜底。
  - **仍然攒着推**：CI 按 **push 次数**触发而非 commit 条数，所以「一个闭环批次攒够再推」这条照旧——本地 5 条 commit 一次推 = 跑 1 轮，逐条推 = 跑 5 轮。
  - **CI 已做的两处省额度优化**（`.github/workflows/ci.yml`）：push 只认 main（其余分支走 PR 事件，避免同一次推送触发两轮）；同分支新推自动取消旧运行（连推三次只跑完最后一次）。
  - **合并方式用 merge commit 或 rebase，不用 squash**——squash 会把「一件事一条」的细粒度历史压成一条，与上一条的提交粒度约定冲突。
  - **一次性配置**（负责人在 GitHub 网页操作，只需配一次）：Settings → Branches → Add branch protection rule，分支填 `main`，勾选「Require a pull request before merging」与「Require status checks to pass before merging」并选中 `单测 + 构建`；Settings → General 勾选「Automatically delete head branches」。**auto-merge 按钮依赖前者**，不配则不出现。
  - **例外**：紧急修复或纯文档改动经负责人同意可直推 main（push 到 main 仍会触发 CI 体检）。
- **不得重新引入后端**：不新增服务端代码 / 数据库 / 部署编排。页面数据一律走前端侧 mock 层（参考 `frontend/src/api/knowledgeBaseMock.js` 模式），保持 demo 纯前端可运行。
- 历史文档（原对外契约 / 架构设计 / 旧 PRD / 前端设计稿 / 旧更新日志等）**仅作参考、不再构成约束**：2026-09-10 起已归档材料统一存负责人本机仓库根 `Archive/`（.gitignore，不入库、不要搬回工作区），仓库侧一律经 git 历史找回；原「接口契约变更铁律」随发布单元一并废止。docs/ 工作目录：`PRD/`（PRD 长期正本，git 直改，2026-09-10 起不再按版本建日期文件夹）、`产品经理待办任务/`（2026-09-17 增设：跨人待办，按人一份，见下条）、`调研讨论/`（团队调研与讨论记录，2026-09-03 增设，一主题一文件、命名 `YYYY-MM-DD-讨论核心.md`；**2026-09-17 负责人定三条铁规**：①**发起必须有负责人的明确指令**——「就 X 建一个讨论点」这类话才能新建文档，不得因话题相关就自行开一份；②讨论中的重要内容（关键论据、被推翻的结论、拍板）**过程性记录进文档**，写完一轮就提交入 git；③**必须以明确结论收口**——文末「结论」段写清定了什么、去向（进 PRD / 代码 / 规则 / 待办，或明确放弃），没有结论段的讨论不算闭环、不得归档）、`01-PRD改动记录/`（2026-09-07 增设：按 PRD 版本各一份《改动记录-YYYYMMDD.md》，三段式变动主题 + 末尾待确认清单，永久留档）、`02-PRD与代码一致性审查/`（2026-09-06 增设，**2026-09-17 改名**去「原型」字样；审查对象 = md↔代码现状，产出《PRD与代码现状一致性审查结果-YYYYMMDD.md》，只列差异点，Q 编号条目按类聚合，旧版遗留条目标【上版日期已存在】）、`03-待决策清单/`（原 03-PRD和原型待决策差异点，09-17 改名；**所有等负责人拍板的清单统一放这里**，含测试审计的待决策）、`04-待补需求定义/`（原 04-原型退役补定义需求，09-17 改名：需产品补定义的事项清单）、`05-测试用例审计/`（2026-09-12 增设：`/test-audit` 技能产出的《测试用例审计-YYYYMMDD.md》，含 T 处置清单 / K 代码缺陷 / J 待裁决；终点 commit 记 README 基线表）、`规范/`（09-17 增设：长期有效的参考——`UI/` 设计图、`接口/` 接口规范）。轮次复盘写在 `调研讨论/YYYY-MM-DD-复盘-主题.md`（原 `复盘/` 目录 09-17 并入）。目录规则与文档生命周期见 `docs/README.md`。
- **PRD 修订走固定流程**：`docs/PRD/` 为 PRD 长期正本（2026-09-10 起 **git 直改模式**：修订直接改 md 提交，git diff 即变更记录，不再投放日期文件夹整包替换）。PRD md 出现 git 变更（自己改完、pull 到他人改动、或负责人知会）时执行 `.claude/skills/prd-import/` 技能（变更审查→md↔代码现状一致性审查→负责人拍板→分组改造→回归验证与阶段推送）；发现变更只报告不自动启动，负责人确认前不动工改造。
- **改动记录（2026-09-17 负责人改版）**：`docs/PRD-review/` 日记录台账**停用**（历史已归档负责人本机），不再写日记录；改动说明写进 commit message（一件事一条，说清需求/方案/影响面），需要留痕的裁决写进当轮工作项文档（02 审查结果 / 03 待决策清单）。**跨人待办统一记 `docs/产品经理待办任务/<人>.md`**：执行任务时发现需要他人处理的事项，追加到对方文件的表里（序号 / 状态 未处理→已处理 / 发起人 / 问题详述：现状·依据·期望），对方处理完自己把状态改「已处理」并随 commit 提交，不删行。四人对应（文件名 → GitHub 账号）：陈森亮 `slchen` → kira-chen（负责人）、曹春林 `clcao` → godness4ccl-sudo、孙丹阳 `dysun` → Diana37-bot、浦月 `yuepu` → yue0403。发起人一栏写中文名即可。**待办按模块分派**：01 总览 / 02 岗位 / 03 能力 / 05 治理 / 06 组织 → 浦月；04 运行 → 曹春林。`docs/update/`、`docs/API迭代需求/` 早已停更。

### PRD 对齐铁律（2026-09-10 负责人改版；原 2026-09-01 版随原型退场废止）

- **对齐基准 = `docs/PRD/数字员工管理端PRD/` 内各模块 md 文字稿（长期正本，2026-09-10 起 git 直改、不再按版本换目录），md 为唯一口径**。交互原型已退场（2026-09-09/10 确立）：不再修改、新版 PRD 不再附带；旧原型与模块截图仅历史参考，不作依据。**PRD 交付不含页面截图（2026-09-17 负责人重申）**：一致性核对直接对照 md 文字描述与 frontend 源码运行效果，不以 PNG 为交付物或对齐依据（09-10 运行规格批「必须含 PNG」的例外随本条废止）。
- md 与代码现状不一致：默认**按 md 落地并记录**；md 与近期负责人拍板冲突的例外——停下提请裁决，不自行取舍（差异记入当轮 02-审查结果，需他人处理的记入 `docs/产品经理待办任务/`）。
- 同包《各模块必填选填字段一览表.md》是全模块必填/选填字段的权威一览，做表单校验对齐时须一并核对。
- 视觉与交互基准：站内既有设计语言（`frontend/src/assets/` 令牌 + 现有页面范式），同类页面怎么长就怎么长，不自创样式。**设计图**：`docs/规范/UI/` 下的设计图（现有《列表页UI.png》为列表页布局与配色基准，负责人后续会陆续上传更多）**仅作视觉参考**——列序、时间列、查询按钮等以代码现状为准，不按图上的数据内容对齐（2026-09-11 拍板，09-17 入册）。旧《UI设计规范.md》已作废（git `08b9c14` 可取回）。
- 交付标准：不需要真实实现，达到「文字表述 + 可交互示意」即可（mock 数据层支撑交互）。

## 技术栈

- **前端：** Vue 3 + Vite + Pinia + Element Plus + Axios

## 前端编码规范

- 使用 Vue 3 Composition API（`<script setup>` 语法）
- 组件化开发，单一职责原则
- 状态管理统一使用 Pinia
- UI 组件库采用 Element Plus
- 统一封装 HTTP 请求（axios 拦截器，见 `frontend/src/api/request.js`）
- CSS 采用 Scoped CSS 方案
- 关键交互需有 loading 态和错误态处理
- 文件命名：组件 PascalCase，工具函数 camelCase
- 视觉与交互延续既有设计语言（Notion 风格令牌与双主题）：以 `frontend/src/assets/` 下的样式令牌与现有页面范式为准（原设计规范文档在历史归档 zip 内，需要时查阅）
- Mock 数据放在前端 api 层旁路文件中（`xxxMock.js`），不散落进组件

## 安全红线

- 任何真实密钥 / 令牌绝不写入代码 / 文档 / 日志（demo 无后端，正常情况下不应出现密钥需求）
- 数据库备份、运行落盘数据等历史产物不放入本项目目录

## 可选：角色分工参考

`.claude/agents/` 内附一套按角色划分的 Claude Code 子 agent 定义（2026-09-01 已按 demo 定位重组为 6 人班底：产品经理[PRD 对齐审查员] / 架构师[前端架构与 mock 层] / 前端开发[demo 工程主力] / UI 设计师 / 测试工程师[原型验收员] / 最终用户），`.claude/commands/pm.md` 为配套的任务分解命令（含 PRD 对齐轮标准流程）。原后端 / 大模型专家 / 提示词工程师三角色已裁撤（定义可查 git 历史）。这是可选的工作方式参考，不构成对协作流程的强制约定。
