# 依赖与构建产物（协作者本地 npm install / npm run build 生成；
# 需要即开即用静态包的场景用根目录版本包 zip，不走 git）
node_modules/
frontend/dist/

# 版本快照 zip（2026-09-02 起工程改由 git 管理，快照包不入库；历史归档 zip 在 docs/ 内保留入库）
版本包-*.zip

# 系统与本地文件
.DS_Store
*.local
.claude/settings.local.json
# Office 编辑锁文件（本地打开 docx/md 等文件时生成，与内容无关；曾被 post-commit 钩子的
# 目录级 `git add` 误纳入过一次提交，见 2026-09-23 commit 说明）
~$*

# 日志与临时
*.log
frontend/*.tmp.mjs

# 历史文档归档（2026-09-02 负责人指示不共享，仅本地保留）
docs/历史文档归档-*.zip

# 归档区（2026-09-10 负责人指示）：已归档不再使用的文件统一放这里，仅本地保留、不提交 GitHub。
# 迁入前已入 git 的文件可经 git 历史恢复，见 docs/归档/README.md
Archive/

# 本地审查产物（/prd-import、/test-audit 的差异清单、报告、截图）：只在本地，不入库；结论以 commit 签名行 + 待办入库
.review/

# Obsidian 编辑器个人配置（窗口布局/外观），与 PRD 内容无关，不入库（负责人 2026-09-18 定）
.obsidian/
