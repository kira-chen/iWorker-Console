# PRD-20260903 → PRD-20260904 对比分析（待负责人确认）

**生成日期**：2026-09-06　**流程依据**：prd-import 第一步（增量核对）。负责人确认解决完所有冲突点后，删除旧版 PRD-20260903 与本文件，随后启动第二步改造。

## 〇、总体结论与结构级差异

**结论：非纯增量，但本次是小版本定点修订**——冲突点 **25 条** + 增量 **12 条**，集中在四个主题：①「岗位分配→岗位管理」全局改名；②**知识库数据源（API/MCP）结构性重写**（本次唯一的大改造点，md 与原型两侧口径一致）；③多处 **PRD 向已实现代码回齐**（岗位版本管理回归、API Bearer+多参数鉴权入 md、专家背景色入 md、数据源状态筛选入 md——对应挂账 Q1/Q2 等，属利好）；④岗位工作档案规则名改自由输入。未触及：技能/模型/MCP 连接器/治理五模块/用户/角色 md 及原型对应区、岗位人格页签卡片结构、专家背景色实现区。

**结构级差异**：

| # | 项 | 说明 | 定性 |
|---|---|---|---|
| ST-1 | `04运行/` 目录再次被移除 | 上一版（0903）负责人已拍板保留并占位，本版又不含该目录——**复发项**，请再次确认（建议沿用上版裁决：保留并移入新版） |
| ST-2 | `prd.知识库.md.bak` 再次出现 | 与正文是否相同待删前核对；上版裁决为删除冗余（建议沿用） |
| ST-3 | `01总览/` 未带占位文件 | 新版有该目录但无 .gitkeep（上版裁决"做好占位"，入库时补即可，非冲突） |
| ST-4 | 原型文件名未变 | 同名替换（`数字员工管理端交互原型.html`，+4K 定点修改），无更名问题 |

---

## 一、冲突点（25 条）

### A 组：岗位系 md（5 条）

| 编号 | 位置 | 旧版内容 | 新版内容 | 变更性质 |
|---|---|---|---|---|
| MD-C1 | 岗位md 操作列定义（L45） | 六按钮（无版本管理） | 七按钮，**加回【版本管理】** | 改写。⚠与已实现代码方向一致（冻结区保留的版本管理获得 PRD 依据，联动挂账 Q2）；但 §3.1 分状态表未同步（仍写"已发布共 3 个按钮"），新版内部矛盾 |
| MD-C2 | 岗位md §3.3 vs 新增 §3.7 | §3.3"点击后打开发布前检查弹窗"（保留未删） | 新增 §3.7 断言"进入同一个版本管理侧栏" | 口径冲突并存于新版内部。⚠已实现代码=版本侧栏路径（与 §3.7 一致，与残留 §3.3 冲突，联动 Q2/Q3） |
| MD-C3 | 岗位分配 md 文件名 | prd.岗位分配.md | **prd.岗位管理.md** | 更名（命名口径收敛） |
| MD-C4 | 同文件 H1 | 「# 岗位分配产品需求」 | 整行删除（无替代 H1；正文首段仍用"岗位分配"措辞，术语混用） | 删除（PRD 内部缺陷候选） |
| MD-C5 | 同文件 页面入口 | 侧边栏"02 岗位 / 岗位分配" | 侧边栏"02 岗位 / **岗位管理**" | 改写。⚠影响已实现代码：AdminRail 菜单 label 现为「岗位分配」（0903 明确入口名不变，本版反转），需随动改名+测试 |

### B 组：专家 / 知识库 / API / 一览表（13 条）

| 编号 | 位置 | 旧版内容 | 新版内容 | 变更性质 |
|---|---|---|---|---|
| LB-C1 | API md 鉴权类型 | 不鉴权 / API_KEY | 不鉴权 / API KEY / **Bearer Token** | 功能扩展（联动挂账 Q1：与 2026-09-01 已实现口径一致，PRD 回齐） |
| LB-C2 | API md 不鉴权提示 | 提示"当前服务不需要访问凭证" | 不展示凭证字段 | 交互口径修改 |
| LB-C3 | API md API KEY 模式 | 单参数（参数名/位置/参数值必填） | **多参数表格**（参数名/描述"客户端可见"/客户端填写/位置/参数值条件必填，至少一行） | 结构性改动（与已实现一致，PRD 回齐，联动 Q1） |
| LB-C4 | API md 保存校验 | API_KEY 参数名非空 | 多参数行校验 + Bearer Token 非空 | 校验加强（随 C1/C3） |
| LB-C5 | 知识库 md 数据源筛选 | 无翻页重置句 | 「筛选变化后从第 1 页展示」 | 规则补充（实现已有，md 回齐） |
| LB-C6 | 知识库 md API 源请求方法 | GET 或 POST | POST/GET/PUT/DELETE/PATCH，默认 POST | 扩展+默认值变化。⚠影响已实现代码 |
| LB-C7 | 知识库 md API 源鉴权 | 无鉴权/API Key 单参数（Header/Query） | 无鉴权/API KEY 多参数表格（四位置）/Bearer Token，与 prd-API.md 统一 | 结构性改动。⚠影响已实现代码 |
| LB-C8 | 知识库 md API 源映射 | query/topK 自由映射 + 结果数组 JSONPath + content 必/source·score 选 | 请求/响应映射**表格化预设行**（query/topK 固定行；content/source/score 预设不可删）；**JSONPath 移除** | 结构性重构。⚠影响已实现代码 |
| LB-C9 | 知识库 md 连接测试 | "配置…后重置" | "修改…后重置" | 措辞修正 |
| LB-C10 | 知识库 md MCP 接入方式 | 引用现有 MCP / 内联配置 两种 | **仅"直接填写"**（引用现有删除）；但 §七.3 残留"选择已有 MCP 时…"句，新版内部矛盾 | 功能删减。⚠影响已实现代码（现含 EXISTING 引用模式） |
| LB-C11 | 知识库 md MCP 配置字段 | 单 Endpoint + 无鉴权/Bearer/自定义 Header | **传输方式两分支**：streamable-http（鉴权 无鉴权/Bearer/API Key+Header 名≤128）/ **stdio**（Command 下拉/Arguments/环境变量表含客户端填写）；超时独立节 | 结构性重构。⚠影响已实现代码 |
| LB-C12 | 知识库 md MCP 工具与映射 | 单选工具 + Schema 折叠 + 自由映射 | **工具多选复选框**（至少一个）+ 映射表格化（与 API 源一致）；Schema 折叠条款删除 | 结构性重构。⚠影响已实现代码 |
| LB-C13 | 一览表 API 配置节 | 请求头参数名/值恒必填两行 | 鉴权类型行 + API KEY 多参数子表 + Bearer 子表 | 与 prd-API.md 同步（联动 Q1） |

### C 组：原型（7 条）

| 编号 | 模块/功能点 | 旧版 | 新版 | 变更性质 |
|---|---|---|---|---|
| PT-C1 | 全局改名 | 导航/navMap/页签/权限 mock 共 5 处「岗位分配」 | 全部改「岗位管理」（内部 key 不变） | 术语全局改名。⚠影响已实现代码（=MD-C5） |
| PT-C2 | 知识库 MCP 源接入方式 | existing/direct radio | radio 删除固定直连；连接卡复用连接器 MCP 配置（streamable-http/stdio）+ 新增必填超时（默认 10000，1000-120000） | 流程删减+字段新增。⚠影响已实现代码 |
| PT-C3 | 知识库 MCP 检索工具 | 单选 | 多选折叠复选框，config.tool 改数组 | ⚠影响已实现代码 |
| PT-C4 | 知识库 MCP 请求映射 | 固定 query/topK/filters 三行、方向"客户端→工具" | 方向反转"工具入参←客户端字段"，表头含类型/必填/默认值；filters 删除；可添加自定义参数 | ⚠影响已实现代码 |
| PT-C5 | 知识库 MCP 响应映射 | JSONPath+固定 5 标准字段 | 自由字段白名单（默认 content/source/score，可增删）；注：kmcpMissing 仍校验旧字段，原型内部残留 | ⚠影响已实现代码 |
| PT-C6 | 知识库 API 源表单 | 独立 kapi 模块（复用连接器/直连+简版字段） | kapi 整体删除，重写为与 MCP 新口径一致的 请求配置/鉴权配置/请求参数映射/响应字段映射 四卡 | 整体重构。⚠影响已实现代码 |
| PT-C7 | 岗位工作档案·档案详情规则名 | 受控下拉（限编目字段） | 自由输入 input（syncRuleNameOptions 死代码残留） | ⚠影响已实现代码（岗位详情页工作档案区） |

---

## 二、增量清单（12 条，摘要）

- 岗位 md 新增 **§3.7 版本管理**整节（侧栏形态/发布与版本管理双入口/共用校验——与冻结区现实现一致）。
- API md 新增 **Bearer Token 模式**字段表（Authorization: Bearer 前缀、密码输入、遮罩）。
- 专家 md 新增**背景色**条款（7 色值；未标必填与默认值，且一览表专家节未同步——内部缺口）。
- 知识库 md 新增：数据源状态筛选行、§六.1.1 鉴权方式说明整节、§七.2.2 stdio 整套配置、§七.3 检索工具多选节。
- 一览表新增整节「九、知识库—数据源管理」（知识库首次进入一览表，明确 MCP 仅支持直接填写）。
- 原型新增：连接器 schema 基础函数全局导出、API 源六个交互函数、api-src 样式集、多选 select 基础支持。

---

## 三、与挂账问题及已实现代码的关系

**利好（PRD 回齐实现，建议销项联动）**：
- **Q2（岗位版本管理）**：md 正式加回版本管理（操作列+§3.7），与冻结保留的实现一致——Q2 可望销项；但 §3.1 未同步、§3.3 残留旧句（MD-C1/C2）需 PRD 侧修文。
- **Q1 的鉴权半边**：API md/一览表补齐 Bearer+多参数+客户端填写，与 9/1 实现一致；**图标与示例问题条款本轮未动**（md 仍要求、0904 原型 API 编辑器仍无），Q1 的图标/示例问题矛盾依旧待裁决。
- 专家背景色、数据源状态筛选：md 补齐已实现口径。

**需要返工的（本次真正的改造量）**：
- **知识库数据源编辑器**（KnowledgeSourceEditor 668 行，9/4 按 0903 实现）：API 源整体重构（方法 5 种/鉴权统一口径/映射表格化/JSONPath 移除）+ MCP 源重构（删引用现有/加 stdio/工具多选/映射反转）——md 与原型两侧一致，属明确改造项。
- **「岗位管理」改名落地**：AdminRail 菜单 label + 相关测试（0903 明确"入口名不变"被本版反转）。
- 岗位详情·工作档案规则名改自由输入（小改）。

**新版内部缺陷候选（记问题记录）**：岗位 §3.1 未同步、§3.3 残留；岗位管理 md H1 删除+正文术语混用；知识库 §七.3 残留引用句；原型 kmcpMissing 旧校验残留、syncRuleNameOptions 死代码；专家背景色一览表未同步且必填/默认值未标注。

---

## 四、待确认清单（建议裁决方式）

1. **批量确认「以新版为准」**：A 组 MD-C3/C4、B 组 LB-C1～C5/C9/C13、C 组全部利好回齐项——多数与已实现一致或为文档收敛，确认后仅少量代码随动。
2. **确认启动返工**：知识库数据源重构（LB-C6～C8/C10～C12 + PT-C2～C6，一揽子）；「岗位管理」改名（MD-C5/PT-C1）；工作档案规则名（PT-C7）。
3. **复发项再确认**：ST-1（04运行 目录，建议沿用上版"保留"裁决）。
4. **挂账联动**：确认 Q2 是否随本版销项（版本管理有 PRD 依据了）；Q1 图标/示例问题矛盾维持挂账。
5. 内部缺陷候选转 PRD 团队修文（见上节列表）。
