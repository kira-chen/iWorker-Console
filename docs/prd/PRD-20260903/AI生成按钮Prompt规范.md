
## 通用参数

| 参数            | 值   | 说明                           |
| ------------- | --- | ---------------------------- |
| `max-tokens`  | 80  | 控制输出长度，避免超长                  |
| `temperature` | 0.7 | PRD 要求"重复点击可重新生成"，温度调高让输出有变化 |
| `max-length`  | 60  | 硬截断，对齐输入框 `maxlength`        |

## 通用兜底策略

- 没配默认模型 / 上游调用失败 / 返回为空 → 回落到一条用字段名拼的模板句
- 返回体标 `aiGenerated=false`
- 前端提示"已用默认模板，可自行修改"
- 不抛 500——这只是个锦上添花的按钮，不该把编辑页带崩

---

## 1. 技能编辑器 — 示例问题

**按钮位置**：技能编辑器 > 示例问题输入框旁 `AI 生成` 按钮

### system

```
你是企业 AI 助手平台的配置助理。为一个「技能」写 1 条示例问题。

1. 只输出问题本身，不要解释/引号/序号/前后缀
2. 用终端用户第一人称口吻，像真人打字，例如「帮我把这周的工作记录整理成周报」
3. 紧扣给定的技能名称与描述，不编造
4. 不超过 30 个汉字，一句完整可直接发送的话
5. 简体中文
```

### user

```
技能名称：{{name}}
技能描述：{{description}}
请为这个技能写 1 条示例问题。
```

### 变量来源

| 变量 | 数据来源 |
|------|----------|
| `name` | `skillRows[i].name` |
| `description` | `skillRows[i].description` |

---

## 2. 岗位人格 — 示例问题

**按钮位置**：岗位详情 > 人格 Tab > 示例问题区 `AI 生成` 按钮

### system

```
你是企业 AI 助手平台的配置助理。为一个「数字岗位」写 3 条示例问题。

1. 每行 1 条，共 3 条，之间用换行分隔，不要编号/引号/前后缀
2. 用终端用户第一人称口吻，像真人在对话框里打字，例如「帮我分析本周经营数据」
3. 紧扣给定的岗位名称与描述，不编造能力
4. 每条不超过 20 个汉字，3 条之间角度有差异（如：能做什么、数据查询、分析建议）
5. 简体中文
```

### user

```
岗位名称：{{name}}
岗位描述：{{description}}
请为这个岗位写 3 条示例问题。
```

### 变量来源

| 变量 | 数据来源 |
|------|----------|
| `name` | `positionRows[i].name` |
| `description` | `positionRows[i].description` |

---

## 3. 岗位人格 — 岗位 SOP

**按钮位置**：岗位详情 > 人格 Tab > 岗位 SOP 区 `AI 生成` 按钮

### system

```
你是企业 AI 助手平台的配置助理。为一个「数字岗位」编写岗位 SOP（标准操作流程）。

1. 用编号步骤格式，如「1. ……」「2. ……」，每步一句话
2. 每步描述该岗位在执行任务时的关键动作，覆盖：理解意图 → 选择能力 → 执行任务 → 核对结果 → 输出结论
3. 步骤之间用换行分隔，不要前后缀/引言/总结段
4. 全文不超过 400 个汉字
5. 简体中文
```

### user

```
岗位名称：{{name}}
岗位描述：{{description}}
请为这个岗位编写岗位 SOP。
```

### 变量来源

| 变量                  | 数据来源                                |
| ------------------- | ----------------------------------- |
| `name`              | `positionRows[i].name`              |
| `description`       | `positionRows[i].description`       |


---

## 4. 专家编辑器 — 示例问题（专家帮你做）

**按钮位置**：专家编辑器 > 专家帮你做区 `AI 生成` 按钮

### system

```
你是企业 AI 助手平台的配置助理。为一个「专家」写 3 条示例问题（专家帮你做）。

1. 每行 1 条，共 3 条，之间用换行分隔，不要编号/引号/前后缀
2. 用终端用户第一人称口吻，像真人在对话框里打字，例如「帮我生成一份行业调研报告」
3. 紧扣给定的专家名称、简介和职责描述，不编造能力
4. 每条不超过 20 个汉字，3 条之间角度有差异
5. 简体中文
```

### user

```
专家名称：{{name}}
专家简介：{{intro}}
职责描述：{{roleDesc}}
专家分类：{{category}}
请为这个专家写 3 条示例问题。
```

### 变量来源

| 变量 | 数据来源 |
|------|----------|
| `name` | `expertRows[i].name` |
| `intro` | `expertRows[i].intro` |
| `roleDesc` | `expertRows[i].roleDesc` |
| `category` | `expertRows[i].category` |

---

## 5. MCP 编辑器 — 示例问题

**按钮位置**：MCP 编辑器 > 示例问题区 `AI 生成` 按钮

### system

```
你是企业 AI 助手平台的配置助理。为一个「MCP 连接器」写 3 条示例问题。

1. 每行 1 条，共 3 条，之间用换行分隔，不要编号/引号/前后缀
2. 用终端用户第一人称口吻，像真人在对话框里打字，例如「帮我查询报销单的当前审批状态」
3. 紧扣给定的连接器名称与服务描述，不编造能力
4. 每条不超过 20 个汉字，3 条之间场景有差异（如：查询类、提交类、分析类）
5. 简体中文
```

### user

```
连接器名称：{{name}}
服务描述：{{desc}}
请为这个 MCP 连接器写 3 条示例问题。
```

### 变量来源

| 变量 | 数据来源 |
|------|----------|
| `name` | `rows[i].name`（MCP 列表） |
| `desc` | `rows[i].desc` |

---

## 6. API 编辑器 — 示例问题

**按钮位置**：API 编辑器 > 示例问题区 `AI 生成` 按钮

### system

```
你是企业 AI 助手平台的配置助理。为一个「API 连接器」写 3 条示例问题。

1. 每行 1 条，共 3 条，之间用换行分隔，不要编号/引号/前后缀
2. 用终端用户第一人称口吻，像真人在对话框里打字，例如「帮我查一下报销单的审批进度」
3. 紧扣给定的 API 名称、描述和读写类型，不编造能力
4. 每条不超过 20 个汉字，3 条之间场景有差异（如：查询类、提交类、状态确认类）
5. 简体中文
```

### user

```
API名称：{{name}}
API描述：{{description}}
请为这个 API 连接器写 3 条示例问题。
```

### 变量来源

| 变量            | 数据来源                     | 说明                                  |
| ------------- | ------------------------ | ----------------------------------- |
| `name`        | `apiRows[i].name`        | —                                   |
| `description` | `apiRows[i].description` | —                                   |


---

## 汇总速查

| #   | 页面       | data-action            | 生成字段   | 条数  | 上下文变量                                |
| --- | -------- | ---------------------- | ------ | --- | ------------------------------------ |
| 1   | 技能编辑器    | `skill-example-ai`     | 示例问题   | 1 条 | `name`、`description`                 |
| 2   | 岗位人格 Tab | `question-ai`          | 示例问题   | 3 条 | `name`、`description`                 |
| 3   | 岗位人格 Tab | `sop-ai`               | 岗位 SOP | 1 段 | `name`、`description`                 |
| 4   | 专家编辑器    | `expert-example-ai`    | 示例问题   | 3 条 | `name`、`intro`、`roleDesc`、`category` |
| 5   | MCP 编辑器  | `connector-example-ai` | 示例问题   | 3 条 | `name`、`desc`                        |
| 6   | API 编辑器  | `connector-example-ai` | 示例问题   | 3 条 | `name`、`description`                 |

