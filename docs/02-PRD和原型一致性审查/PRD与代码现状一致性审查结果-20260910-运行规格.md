# 运行规格 PRD 与代码现状一致性审查结果（2026-09-10）

> 审查对象：`docs/PRD/数字员工管理端PRD/04运行/运行规格/`（含10张原型PNG）↔ 运行规格前端页面、API/mock 与浏览器实际渲染。PRD定义业务逻辑，PNG承载可见交互评审基准。

## 结论

- 审查发现的 6 组明显不一致均已在本批闭环。
- 当前无未解决的 P0/P1 一致性问题。
- 后端规格对象、岗位/个人关系与审批能力不在当前前端 Demo 的已实现范围，已在 PRD 中明确，不能将本地 mock 验证表述为后端完成。

## 闭环清单

| 编号 | 基线代码问题 | PRD 口径 | 本批处理 | 状态 |
|---|---|---|---|---|
| R1 | 旧页面使用任务超时、实例并发、临时磁盘等字段 | 使用研发支持的 9 个底层字段 | 编辑器、列表、mock 与初始化 YAML 统一到 9 字段 | 已解决 |
| R2 | 仅有面向用户的简单规格选择，缺少默认/岗位/个人模型 | 个人配置 ＞ 岗位规格 ＞ 平台默认 | 增加唯一默认规格、岗位范围、个人例外、生效来源与回退计算 | 已解决 |
| R3 | 管理员配置与用户申请边界不清 | 管理员直接授权立即生效；用户申请进入待审批 | 配置范围窗口和 mock 分开实现两种动作及状态 | 已解决 |
| R4 | 规格数据刷新后无法稳定留存 | Chrome 本地数据应可持续演示 | 接入统一持久化层，使用 `runtimeSpec` version 2 快照 | 已解决 |
| R5 | 列表排序与 PRD 的“最近更新时间倒序”不一致 | 默认倒序，允许切换升序/降序 | mock 排序与表格排序状态同步调整 | 已解决 |
| R6 | PRD 容易把页面能力理解成后端已交付 | 当前仓库是前端 Demo；YAML 才是初始化直接输入 | 在 PRD 首页补充本仓交付边界；保留并引用PNG作为前端交互评审材料 | 已解决 |

## 浏览器实走结果

- 页面可见字段与 PRD 一致：CPU/内存、临时存储、就绪超时、空闲回收、最大存活、适用岗位、用户申请、生效用户。
- 查看、编辑、配置范围、新建、删除流程可用。
- 新建测试规格后刷新仍保留；删除后再次刷新不再出现，验证持久化写入与清理均有效。
- 浅色/暗色主题、1280×720 页面宽度及控制台均无阻断问题。

## 后续研发边界

研发接后端时，应保持 PRD 的业务优先级与 9 字段契约不变；若真实接口、审批状态机或现场默认值发生变化，应先修改本目录 PRD/YAML，再同步调整代码。
