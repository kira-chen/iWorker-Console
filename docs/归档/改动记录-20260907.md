> ⚠ **本文件已归档（2026-09-09）**：待确认清单 12 项已全部销项，仅作历史留痕，**不得作为当前口径依据**。当前基准见 `docs/prd/PRD-20260908/` 与《PRD与原型一致性审查结果-20260908.md》。

# 改动记录-20260907（PRD-20260904 → PRD-20260907）

> 新版 PRD 导入流程阶段一产出（2026-09-08 增量核对，3 路并行只读审查：岗位系 / 知识库 / 用户技能审核+治理组织）。本文件只记版本间变动与待确认项；md↔原型全量一致性体检另见 `docs/归档/PRD与原型一致性审查结果-20260907.md`（阶段二产出）。

**总体结论**：定点修订 + 一个新模块——**不是纯增量**。改动集中在四个主题：①**岗位 md 大幅向原型回齐**（删认领说明/图标阻断/采集字段阻断，页签改 6+1 动态注入，业务系统页签改口径，补 10/20/20 上限与版本号语义化规则）；②**知识库 MCP 数据源删除请求/响应映射两节**——**推翻 2026-09-07 刚落地的实现**（git 174baa4）；③**新增「用户技能审核」模块 md + 原型页面**（代码侧已有旧形态实现，与新 md 属不同产品形态，需整页重做）；④岗位管理页签改名「用户岗位管理」、绑定范围扩到审核中岗位。其余：知识库抽屉删「提交停用」/检索测试标题/可见范围三处 PRD 回齐原型、API 请求映射递归规则细化、用户 md 删"每页 10 条"、AI 规范补业务系统行。未触及：技能 / 模型 / 专家 / MCP·API·业务系统连接器 / 审核中心 / 我的申请 / 用户反馈 / 访问审计 / 角色 的 md 与原型对应区；《各模块必填选填字段一览表》逐字节相同（因此**未跟进**本版多处改动，见待确认 8）。

**结构级差异**：

| # | 项 | 说明 | 定性 |
|---|---|---|---|
| ST-1 | `04运行/` 目录第三次被移除 | 0903、0904 两版均拍板"保留占位"（2026-09-04、2026-09-06 裁决），本版再次不含 | **复发项**（推翻 09-06 拍板） |
| ST-2 | `03能力/知识库/prd.知识库.md.bak` 第三次出现 | md5 与 git 中 PRD-20260903 正稿逐字节相同，不含任何新内容（上版 ST-2 同判） | 卫生项，冗余 |
| ST-3 | `01总览/` 空目录，无 .gitkeep | 上版 ST-3 已补占位，本版又丢 | 卫生项（复发） |
| ST-4 | 原型 html 同名替换 | +31KB，逐行 diff 仅 7 个 hunk：知识库数据源区 5 处 + 末尾新增 `user-skill-audit` 补丁层（L4412–4560）+ 文末空行；其余模块原型零改动 | 增量 |
| ST-5 | `.DS_Store` ×5（根/02岗位/03能力/连接器/05治理/06组织） | — | 卫生项 |
| ST-6 | `05治理/用户技能审核/` 新目录，仅 md 无截图 | 其他治理模块均附截图 | 增量（缺陷：无截图，见待确认 9） |
| ST-7 | 岗位管理 md 引用 `./岗位分配-申请审批列表页.png` 不存在 | 两版目录均无此图（上版同样悬空） | 缺陷（旧版遗留） |
| ST-8 | 岗位 md、用户 md 两文件行尾变 CRLF | 逐字节 diff 全文变动，语义 diff 需 `--strip-trailing-cr`；岗位/知识库 md 末尾丢换行符 | 卫生项 |

---

## 一、岗位：md 大幅向原型回齐（发布阻断缩至 4 项、认领说明整节删除、页签 6+1、业务系统页签改口径）

**需求/问题**：岗位 md 19 处改动（原型岗位区两版零改动，全部是 md 单侧修订）：①§一.1 删"不增加草稿状态"句；②§3.1 未发布行删【发布】【删除】悬停提示，已发布行 3→4 个按钮（增【版本管理】）；③§3.3 首发前置条件 7 项→6 项（删认领说明/采集字段，**新增"领用页文案"**）；④§3.7 版本管理整节重写为 `vX.Y.Z` 语义版本 + 重大/功能/**修复**更新三选一 + 自动算号不可手输；⑤§1.3 页签 7 固定→6 标准 + 业务系统"MutationObserver 动态注入、无引用不展示"；⑥§2.1 岗位描述上限改"人格页 500 / 新建弹窗与编辑抽屉 2000"；⑦§2.2 删图标发布校验；⑧**§2.3 岗位认领说明整节删除**；⑨示例问题 AI 生成增"描述为空置灰 + tooltip"；⑩采集字段最多 10 个 + 达上限置灰 + 表头提示；⑪单/多选保存校验"请至少填写一个选项"；⑫Agent 最多 20 个置灰；⑬每泳道技能最多 20 个置灰；⑭§8.1 业务系统引用列表字段改为 业务系统/登录地址/业务页/最近更新时间/操作，删"仅已发布"与搜索句；⑮§8.2 改【＋ 新业务系统】/「引用业务系统」弹窗/【确认引用】/行内仅【查看】不可移除；⑯§8.3 名称跳转节删除；⑰§9.1 阻断校验 7→4 项（名称/描述/示例问题/SOP）；⑱§9.2 "推荐问题"改"示例问题"；⑲CRLF/末尾换行。

**方案**：逐条比对原型最终覆写态（覆写链：1171 列表层→…→2119 `position-final-render-fix`→2281 工作档案层→2670 Agent 表格层→4225 领用页文案卡→4309 图标+业务系统页签→4383 AI 层），结论分三档：

- **与原型一致（md 回齐）**：①②已发布 4 按钮 ④主体 ⑥ ⑦ ⑨ ⑪ ⑭字段 ⑮ ⑯ ⑰ ⑱。上版底账 **Q5、Q6、Q12、Q14、Q15、Q24、Q26、Q27、Q32 完全销项**；Q1/Q2/Q4/Q21/Q31 部分销项。
- **md 超出原型（新引入差异）**：③图标与"领用页文案"仍列为阻断（原型 `validate()` L2129 只 4 项）；⑤"无引用不展示"（原型 L4353 `syncTab` 无条件注入，L4321 `ensure()` 还给无引用岗位自动预置 2 个业务系统——页签恒显）；⑩⑬"达上限置灰"（原型 L1854 副题常驻、无置灰；最终态 Agent 表格层 L2670 无任何上限）；④"修复更新"vs 原型 L1224「修订版本」。
- **原型超出 md（md 删了原型还在）**：删除按钮 `title="删除前需二次确认"`（L1194）；业务系统页签搜索框（L4347）；**领用页文案卡**（L4240：可选、≤6 条×100 字、空态"暂无领用页文案…"、toast"领用页文案已保存/已删除"，底层字段仍是 `claimDescriptions`）——md 删认领说明节后**全文无任何章节定义它**，却在 §3.3 当作发布必填项出现。

**新版内部自相矛盾（12 条，无法"以新版为准"自动裁决）**：(1) §3.3 六项阻断 vs §9.1 四项（图标/领用页文案）；(2) §3.4 L239"至少 1 个采集字段否则阻断"vs §9.1 已删；(3) §1.3 L150 人格页签仍写"认领说明"但 §2.3 已删；(4) "领用页文案"无定义章节，同包一览表 L16 仍写"岗位认领说明（领用页文案）至少 1 条"、L32"发布阻断校验（7 项）"；(5) §2 开头"6 个配置区域"vs 实列 5 节（编号跳过 2.4）；(6) **§3.7 `vX.Y.Z` 不可手输 vs §9.2 L405 仍是 v001–v999 手填正则**（底账 Q7 升级为 md 内部矛盾）；(7) 两套发布流程并存（列表/版本管理侧栏 vs 详情页发布检查弹窗）无说明；(8) "编辑抽屉"全文无定义，§3.2 说编辑进详情页，§1.1 新建"右侧抽屉"vs §2.1"新建岗位弹窗"（原型 L2319 为居中弹窗）；(9) §1.3 无引用隐藏页签 vs §8.1 空状态永不可见；(10) §6.4 同句【＋ 技能】/【添加技能】两名；(11) §9.2 检查清单仍列采集字段、无 SOP 项；(12) §3.1 列表【发布】"打开发布前检查弹窗"vs 原型只校验技能≥1 后进版本管理侧栏（Q11 未销）。**实现细节泄漏**：L157 "MutationObserver 动态注入"。

**影响面**：代码**已符合**：状态筛选、已发布 4 按钮、AI 按钮置灰、Agent 20 上限置灰、`INTAKE_MAX/SKILL_MAX` 常量。**需改**（按"新 md + 原型最终态"）：`views/admin/PositionDetailTabs.vue:705/:707` 删图标与认领说明阻断（→4 项）；`:859-877` + `components/position/ClaimNotesEditor.vue` 「岗位认领说明」→「领用页文案」改可选（待定义）；`:243-258` 采集字段抽屉保存加"请至少填写一个选项"；`:976` 采集字段达 10 置灰、`:1153` ＋技能达 20 置灰；`:1198` 业务系统页签显隐待拍板；`components/position/PositionBizSystemsPane.vue:134/:153-177/:185/:106-124` 列改 5 列、按钮与弹窗改名、删移除与名称跳转（【确认引用】已符合）；`views/admin/AdminPositions.vue:555` 删发布按钮 title（`:564` 删除 title 待拍板）；`:654` + `api/positionMock.js:300/:612` 新建描述上限 500→2000（待拍板）；`:287` "修订版本"vs"修复更新"待拍板。版本管理两套路径（`VersionDrawer.vue` 自动算号 ✓ vs `PublishCheckDialog.vue:79-84` 手填 v001）与原型同，等 (6)(7) 裁决。

## 二、岗位管理：页签改名「用户岗位管理」+ 绑定范围扩到审核中岗位 + 查询区不重置分页

**需求/问题**：①页签「用户岗位分配」→「用户岗位管理」（§二表格/§三标题/默认页签/§4.3.3 回跳，4 处）；②搜索框改"220ms 防抖自动刷新、不重置分页"、状态筛选"切换即刷新、不重置分页"、仅【查询】回第 1 页；③§3.3/§五 绑定岗位下拉由"仅已发布"改"已发布及审核中（审核期间已发布版本持续可用）"；④§4.2 申请列表状态列"用户记录不存在时默认启用"。

**方案**：四处原型均已是新口径（L1562 `assignmentTabs` 「用户岗位管理」；L1645 `setTimeout(renderAssignments,220)` 不动页码；L1652 状态切换不重置；L1675 `pa-query` 回第 1 页；L1599 `availablePositions` 按底层 status 过滤不看 `pendingAction`；L1563 `user?user.status:'active'`）——**md 回齐原型**，底账 **Q34、Q36、Q37、Q40、Q49 完全销项**。2026-09-07 改造轮曾因 Q34 未决而"页签维持 md 现口径"，本版 md 反转，页签改名可落地。两点提醒：(a) md §4.3.3 L124 仍写"任意**已发布**岗位"，与新 §3.3/§五 不一致（内部矛盾）；(b) "审核中"字面含"未发布且首发审核中"的岗位，原型按底层 status 过滤会排除它，md 括注"已发布版本持续可用"暗示只含"已发布且在审新版/停用"——需明确（待确认 2-c）；(c) 原型工程缺陷：L1598 读 `window.positionRows` 全文未赋值，下拉实际恒走 fallback 硬编码 4 项（记 D 类）；(d) "220ms"属实现常量泄漏（轻）。

**影响面**：`views/admin/AdminPositionAssignments.vue:248` 页签 label（注释 `:6/:42/:256`）；`:111` 防抖 300→220；`:76-79`、`:267-273` 搜索/状态改为不重置页码（`useAdminList.js:105-106` `search()` 会置 page=1，仅【查询】保留）；`:96` `listPositions({status:'published'})` 改为含审核中（`api/positionMock.js:241-243` `viewLabelOf` 把 pendingAction 非空归 reviewing）。状态列降级 `api/positionApplicationsMock.js:67` 已符合。ST-7 悬空图对应底账 Q35 本版未修。

## 三、知识库：三处 PRD 回齐原型最终态（删抽屉「提交停用」/ 检索测试标题 / 岗位型可见范围）

**需求/问题**：md §三.3.1 岗位类型可见范围删"创建后不可更改"（专家类型仍保留）；§三.4.1 编辑抽屉、§三.4.2 查看抽屉的已发布态均删【提交停用】（列表行内【停用】及 §4.3 确认弹窗表保留，入口收敛为列表）；§三.7 检索测试弹窗标题由"「检索测试」+ 标签"改为「检索测试 · [知识库名]」。

**方案**：三处原型 0904/0907 最终覆写态代码完全相同（`openKbEditor` L1936 岗位型 scope 不锁；L3226 `removeKnowledgeStopButtons()` 由 MutationObserver 移除抽屉内 `stop-kb` 按钮；L2058 `decorate()` 把名称 tag 合并进标题），属 **md 向原型回齐**。上版底账 **Q79、Q75、Q89 可销项**。

**影响面**：代码 3 处小改——`frontend/src/components/admin/KnowledgeBaseEditor.vue:450`、`:469` 两个「提交停用」按钮删除（`doDelist` 随之清理，列表 `KnowledgeBaseList.vue:255`【停用】保留）；`KnowledgeSearchDialog.vue:104-109` 标题合并为单文本、去 el-tag；可见范围逻辑 `KnowledgeBaseEditor.vue:108-110` **已符合**新口径，零改动。相关测试若有抽屉「提交停用」断言同步。

## 四、知识库：API 数据源请求参数映射——递归嵌套规则由 1 句细化为 7 条

**需求/问题**：md §六.2 原仅一句"支持任意层级嵌套"，新版展开为：递归展开规则；新建 API 源默认展示三级示例 `filters(object) → rules(array) → field/value`（可编辑可删、非强制参数）；子字段行只展示 子字段名/类型/默认值/删除，每层显示"object / array 可继续嵌套"提示 + 底部【添加下一级子字段】；缩进+层级线+分层背景色、展开中的 object/array 下拉强调色；子字段名必填且同父下不重名、object/array 至少一个有效子字段；删父级级联删后代；切基础类型时收起并暂存草稿、切回恢复、最终保存为基础类型不提交子字段。

**方案**：原型 0907 新增覆写层 L2009–2019（`apiSrcTypeChange`/`apiSrcAddSubParam` 真递归、`apiSrcNestedExampleGroup` 示例、`#api-src-recursive-param-style` 样式），0904 原型仅一层子行（底账 Q64 所指）——**本版 md 与原型同步细化，Q64 可销项**。两处 md↔原型微差：①原型示例组在 filters 下**多一个 `enabled(boolean)`** 与 rules 同级，md 只写 filters→rules→field/value；②md 写"新建时默认展示"，原型新建/编辑都注入（L2018 不区分）；③原型强调色选择器只匹配子层 `.api-subparam-type`，顶层 filters 行无强调（原型小缺口）。校验类规则（重名/至少一个子字段）原型不承载，以 md 为准。

**影响面**：代码递归/级联删除/草稿暂存/重名校验**已具备**（`SourceMapParamRows.vue` 自递归、`knowledgeBaseMeta.js:159-179` `validateRequestMap`）。需补 5 点：新建 API 源默认示例组（`knowledgeBaseMeta.js:128-133` `mkRequestMapRows` 仅 query/topK；`KnowledgeSourceEditor.vue:207`）；子层表头/子行收敛为 4 列（`SourceMapParamRows.vue:48-55`、`:65-82` 现仍带必填与客户端字段）；按钮文案「＋ 添加子字段」→「＋ 添加下一级子字段」+ 层级提示（`:103`）；分层背景/连接线/展开态强调色（`:150-156`）；"object/array 至少一个有效子字段"校验（`knowledgeBaseMeta.js:172` 现 children 为空直接放过）；`SourceMappingEditor.vue:51` note 文案随原型 L1937 更新。示例是否含 `enabled` 待拍板（待确认 4）。Q59/Q60/Q61/Q62/Q63 API 侧条目本版未动，维持。

## 五、知识库：MCP 数据源删除「请求参数映射」「响应字段映射」两节——推翻 2026-09-07 刚落地实现

**需求/问题**：md §七 整节删除「4. 请求参数映射」「5. 响应字段映射」（原各一张表 + 预设行规则，"配置方式与 API 数据源一致"），超时→§4、连接测试→§5 重编号；MCP 源只剩 接入方式 / 传输方式 / 检索工具 / 超时 / 连接测试。原型同步：`kmcpMappingMarkup` 整函数删除（0904 L1977–1994）、`kmcpMarkup` 不再拼接映射卡且 note 改为"验证连接与工具调用"（L1985）、`kmcpMissing` 删 query 入参与 6 个响应 key 校验（L2004）、基定义 MCP 死分支两张映射卡清理（L1937）。md 与原型口径一致。

**方案**：以新版为准即需**回退 174baa4 中 MCP 映射半边**（2026-09-07 负责人指示提前启动的改造轮，当日已按 0904 md §七.4/§七.5 实现"MCP 映射与 API 同构共用组件"并验证通过）。属**推翻近期拍板/实施的复发项**，需负责人明确确认后再动（待确认 3）。底账 **Q65 可销项**；Q59/Q60/Q61/Q62/Q63 的"七.4/七.5"半边随之作废（API 半边保留，加注即可）；Q373（副注残留"复用连接器"，原型 L1981 仍在）、Q374（超时归属）**未销**。新版 md 连带残留：L406"修改服务地址、鉴权、工具**或映射**后重置为未验证"、L405"入参有效和结果字段可解析"失去依托、L397"选择已有 MCP 时…"与"仅直接填写"矛盾（Q55 既有未修）、**一览表 §9.3 L273-274 仍保留 MCP 请求/响应映射两行**（一览表本版未更新）。原型残留死代码：L1950 注释、L1962/1963 `kmcpDefaultConfig` 仍初始化 requestMap/responseMap、L1967/1996-1997 写入分支。

**影响面**（确认后执行）：`KnowledgeSourceEditor.vue` 模板 `:806-816` MCP 分支 `<SourceMappingEditor protocol="MCP">` 删除、`:817` note 文案、脚本 `:256-257`/`:307-317` connSig/`:417-418` payload/`:460-463` validateTyped 四处 MCP 映射逻辑与 `mcpRequestRows/mcpResponseRows`、`fieldErrors.mcp*` 状态、头注；`SourceMappingEditor.vue` protocol 分支退化为 API 专用（`:41/:60/:70/:80`）；`SourceMapParamRows.vue` noun/占位措辞；`utils/knowledgeBaseMeta.js` §七.4/§七.5 注释；`api/knowledgeBaseMock.js:113-114` MCP 种子删两字段、`:440-443` 校验、`:499-500` connSignature、**version 4→5**；测试 `sourceMappingEditor.test.js:131-136`（protocol=MCP 用例）删、`knowledgeBaseMock.test.js:53-54` mcpConfig 调整。

## 六、原型工程缺陷：L1985 弯引号 class 使 MCP 检索节交互与样式整体失效（本版新引入）

**需求/问题**：0907 原型 L1985 `kmcpMarkup` 返回值的 4 个 class 属性全部写成中文弯引号 `class=”proto2-form-sec kmcp-root”`（0904 同位置 L2002 为直引号，整份文件仅此一行）。HTML 解析后四个类名全部失效。

**方案**：核实影响：`event.target.closest('.kmcp-root')`（L2000/L2001/L1993）永远为 null → MCP 抽屉内改传输方式/鉴权/Endpoint/超时/工具勾选**全部不写回 config、不重渲**（切 stdio 不出 Command 区、切鉴权不出 Token 区），保存校验读的是默认 config；`.kmcp-root`/`.proto2-form-*`/`.kmcp-test-note` 样式对 MCP 检索节均不生效。这是 0907 原型独有回归，属 **D 类原型工程质量**，demo 代码**不跟随**该行为，记入 02-审查结果新条目转原型侧修正。

**影响面**：零代码改动；阶段二 02-审查结果新增 D 类条目。

## 七、用户技能审核：新增模块 md + 原型页面（代码为旧形态，需整页重做）

**需求/问题**：新增 `05治理/用户技能审核/prd.用户技能审核.md`（178 行）：审核用户端上传技能时被安全检测拦截的记录，与审核中心分工明确（§一）。定义 4 个检测项（对外动作/敏感信息/权限范围/危险操作，内部标识"敏感信息明文凭证"）、5 档结果等级、按"审查尺度（宽松/通用/严格）× 各项最低触发等级"判断是否进人工审核；查询区（搜索名称/描述/提交人、审核尺度、审核状态 待审批/已通过/已驳回、查询按钮）；列表 7 列（提交时间可排序、状态与尺度彩色标签、待审批加【通过】【驳回】）、右上【风险设置】；详情抽屉（基本信息 6 字段、SKILL.MD 原样只读、检测明细 4 项固定展示含位置/代码片段/依据）；通过确认弹窗、驳回弹窗（原因必填 ≤500 字）；风险设置抽屉（当前尺度单选 通用/严格/宽松 默认通用 即时生效；三套模板 Tab、逐项可选等级集合、默认值表、恢复默认/取消/保存设置 + toast）。原型 0904 该导航为死链（navMap 无登记，落兜底 toast），0907 末尾新增 `user-skill-audit` 层（L4413–4560）覆写全局 `render` 分支、劫持导航 onclick、注入监听与 `.tag.blue/.tag.yellow` 全局样式。

**方案**：新版 md ↔ 新原型层大体一致（查询区/列表列/标签色/风险设置选项与默认值表/toast 逐格相同），但存在需选边差异（详列进 02-审查结果）：页面说明文案两套；md"页面高度动态分页"vs 原型无分页；**等级标签文字**——md §2.2 用「低危险/中危险/高危险」而原型界面统一输出「低风险/中风险/高风险」；md 高/严重同为"红色"vs 原型严重风险黑红底白字独立档；检测通过文案 md"未检测到该项相关风险"vs 原型代入内部名（敏感信息项显示"未检测到敏感信息明文凭证相关风险"，违反 md L27 展示名约定）；驳回原因 500 字上限原型未实现；风险设置【取消】原型不回滚（改即生效）；通过弹窗标题/确认键/toast「审核已通过」「审核已驳回」、驳回弹窗说明与占位、抽屉说明文 `SCALE_DESC` 均原型有 md 无——且 `SCALE_DESC` 描述的是"阻断/告警/放行"旧模型，与默认值表（宽松全为"不进入审核"）自相矛盾。原型自身死代码：`levelClass`/`needsManualAudit` 键名用旧命名（低危险/致命）→ 卡片底色永不上色、§2.3 触发逻辑无任何可见体现；`STRATEGIES`/`RISK_LEVEL_DESC`/`.audit-risk-summary` 未使用。

**新版内部矛盾 / 跨文档冲突**（待确认 5、6、7）：①md §2.2「低危险/中危险/高危险/严重风险」vs §2.3/§7.2「低风险/中风险/高风险/严重风险/不进入审核」两套命名；②**字段字典 md** §2.2 风险等级仍为「高风险、中风险、建议修改、检测通过」4 档、风险类型顺序「对外动作、危险操作、权限范围、敏感信息」，且 L61 写"本次不修改用户技能审核模块 PRD"（已过期）、L5/L55"字段字典是唯一配置来源"与新 md 硬编码 4 项×逐项等级集合的数据模型冲突（原型 L1543 字段字典种子同为旧 4 档，原型两层也不一致）；③审核状态本版全站唯此 md 用「待审批」，审核中心/我的申请 md 与原型 base 均用「待审核」；④一览表无用户技能审核节（驳回原因必填≤500、风险设置必选未登记）；⑤md 无异常/空态章节、无截图；§5.1"发起管理后台审核的用户"、"【提交管理后台审核】"用户端按钮名无用户端 PRD 支撑；⑥底账 Q276（我的申请种子 508「用户技能审核规则」businessType OTHER）本版原型 L1536 未动，不销项。

**影响面**：代码现状是 2026-09-02 前按旧口径（"通过/不通过 + 审核意见"制、用途 自用/平台共享）实现——`views/admin/UserSkillReviews.vue`（副标题 `:100`、搜索占位 `:104`、筛选 `:107-115` 含用途无尺度无查询按钮、状态文案 `:39` 审核中/已拒绝、列集合 `:127-165` 无尺度列无排序、操作「查看」「审核」`:157-163`、单一审核弹窗 `:178-204`）、`ReviewSkillDetailPage.vue`（整页而非抽屉；基本信息缺 4 字段 `:57-68`；SKILL.md 走编辑器 `:71-74`）、`components/position/SkillFocusEditor.vue:1161-1167`（仅遍历上报项不补齐 4 项、无序号/位置/代码块、`:188-193` 颜色按 includes 判）、`api/skillReviewMock.js`（种子 `:48-92` 无 location/code、`:149-150` 无 reviewer/rejectReason）、`api/fieldDictMock.js:25` 旧 4 档、【风险设置】抽屉与尺度概念**全站不存在**（grep 零命中）；测试 `skillReviewMock.test.js` 6 处随动。权限/路由 `AdminRail.vue:111`、`router/index.js:152` 已符合。**属整页重做量级**，等负责人拍板形态（抽屉 vs 整页、等级命名、字典冲突）后启动。另：`docs/PRD-review/2026-09-01.md:520` G-3"用户技能审核与审核中心边界未界定"随 §一 闭环；`docs/PRD-review/2026-09-07.md` 记"该导航原型为死链"的表述本版后失效。

## 八、用户：删除"每页固定展示 10 条"

**需求/问题**：md §3 分页规则删该句，其余不变。

**方案**：原型两版用户列表均为 `dynPageSize()` 动态条数（L1549），删句后 md 与原型一致，底账 **Q306 可销项**。

**影响面**：代码 `views/admin/AdminUsers.vue:38-39` `pageSize: 10`（注释"2026-09-01 对齐原型用户页密度"）——新 md 不再约束条数，保留 10 或改全站默认 20 均不违反；建议保留不动（待确认 10 批量项）。

## 九、AI 生成按钮 Prompt 规范：补第 7 行「业务系统编辑器」

**需求/问题**：场景清单新增 `| 7 | 业务系统 编辑器 | connector-example-ai | 示例问题 | 3 条 | name、description |`。

**方案**：原型两版业务系统编辑器本就走统一 AI 层 `configFor` biz 分支（0907 L4403），本版仅规范补录；代码 `components/admin/BizSystemEditor.vue:312-330` 已接 `useAiLiveGenerate`（源=description，一次 3 条）。底账 **Q361 可销项**。规范变量列 name/description 两项而原型/代码只用 description 作源——与第 5/6 行 MCP/API 同况，非新问题。

**影响面**：零代码改动。

## 十、字段字典：仅表格列宽空格重排

**需求/问题**：§2.2 表格 6 行仅 markdown 列宽空格变化，无语义改动。

**方案**：无需处理；但该表与新增用户技能审核 md 的等级/类型冲突见第七节。

**影响面**：零。

---

## 待确认清单（需负责人拍板）

> 批注方式：直接在本清单条目下写决策；销项后标题加【已解决】。编号固定（第 1–12 项），不随编辑器自动重排。

### 第 1 项【已解决】·【复发项·推翻 09-06 拍板】ST-1 `04运行/` 第三次被移除

0903、0904 两版均裁决"保留占位"（对应 `docs/04-原型退役补定义需求/` 第 2、3 项待浦月补定义）。请确认：①继续保留占位（移植 .gitkeep 进 0907）；②还是本版起正式移除该目录（代码侧 运行规格/实例与会话/配额与限流 三页去留另议）。

> ✅ 2026-09-08 负责人裁决：**保留 04运行 占位**（沿用 09-04、09-06 裁决，第三次确认）。已将 `04运行/实例与会话|运行规格|配额与限流/.gitkeep` 移植进 PRD-20260907；提醒浦月回流新版时保留该目录。

### 第 2 项·岗位系改动确认（第一、二节）

> 2026-09-08 PRD-20260908 回流包已替负责人回答 b/c/d/g（依据见《改动记录-20260908.md》第五节）：b ✅ 阻断统一 4 项、采集字段不阻断；c ✅ §2.3 领用页文案整节（≤6 条×100 字、可选、不参与阻断）；d ✅ §9.2 改语义版本自动算号；g ✅ "审核中"= 已发布岗位在审新版/停用，不含未发布首发审核中。e 改口：①业务系统页签 md 改回固定 7 个（= 原型恒显），"无引用隐藏"撤销；④删除 tooltip 待 Q375 真改。**f 需重看**：md 现全场景 2000，与原型人格页/一览表 500 冲突，代码按 html（人格页 500 / 新建弹窗 2000）。a/h 维持；负责人 09-08 另拍复刻口径（布局/控件照 html）后 a 的 13 处按 html 落地。

- a. 回齐原型的 13 处（发布阻断→4 项、认领说明删节、已发布 4 按钮、描述上限双标、业务系统页签字段/按钮/仅查看、AI 置灰、选项非空校验等）建议批量"以新版为准"，代码按第一节影响面落地。
- b. **【新版内部矛盾·二选一】**发布阻断到底是 §9.1 的 4 项还是 §3.3 的 6 项（图标、"领用页文案"是否阻断）；§3.4 采集字段≥1 是否仍阻断。建议：按 §9.1 四项（= 原型最终态），转浦月回改 §3.3/§3.4。
- c. **"领用页文案"定义缺失**：原型 L4240 有可选卡（≤6 条×100 字），md 删认领说明后未补定义。建议：代码按原型改名为"领用页文案"、可选；请浦月补章节。
- d. **【新版内部矛盾·二选一】**版本号：§3.7 语义版本自动算号 vs §9.2 手填 v001–v999（Q7 升级为 md 内部矛盾）。建议：统一语义版本，详情页发布检查弹窗改为只读展示自动算号；转浦月回改 §9.2。
- e. md 超出原型 4 点是否落地：业务系统页签"无引用不展示"（原型恒显）、采集字段/技能"达上限置灰"（原型无置灰）、"修复更新"vs 原型"修订版本"、删除按钮 title 删否。建议：置灰按 md 落地（小改）；页签显隐按 md；更新类型文案按 md「修复更新」；删除按钮 title 随 md 删。
- f. 新建岗位弹窗描述上限 500→2000 是否落地（代码现 500，原型 2000）。建议：按 md。
- g. 岗位管理绑定下拉"审核中"是否含"未发布且首发审核中"的岗位。建议：不含（按底层 status=已发布、不看 pendingAction，= 原型逻辑与 md 括注）。
- h. 页签「用户岗位管理」改名——2026-09-07 因 Q34 未决暂缓，本版 md 反转，建议落地。

### 第 3 项【已解决】·【复发项·推翻 09-07 实施】知识库 MCP 数据源删除请求/响应映射

昨日改造轮刚按 0904 md 落地并验证通过的"MCP 映射与 API 同构"实现需回退（第五节影响面）。请确认以新版为准执行回退；同时提醒 PRD 侧：md L405/L406、L397 残留与一览表 §9.3 两行未同步（转浦月）。

> 2026-09-08 负责人询问"具体什么事"，大白话说明如下（待裁决）：
> - **背景**：知识库里可以挂"MCP 数据源"（让知识检索去调一个外部 MCP 工具）。上一版 PRD（0904）规定 MCP 数据源和 API 数据源一样，要配两张表——「请求参数映射」（我们把检索词 query、条数 topK 按什么参数名发给工具）和「响应字段映射」（工具返回的哪个字段当内容 content、来源 source、相关度 score）。昨天（09-07）你指示按 0904 已确定内容改造，我们就把这两张表在知识库数据源编辑器里做出来了，并且复用了 API 数据源的同一套组件、写了测试、截图比对通过。
> - **本版变化**：0907 版 PRD 把 MCP 数据源的这两张表**整节删掉**了（只保留 接入方式 / 传输方式 / 检索工具 / 超时 / 连接测试），原型 html 也同步删掉了对应界面。也就是说产品侧改了主意：MCP 数据源不再让用户配映射，直接用工具自己的入参出参。API 数据源的两张表**不受影响**，仍然保留。
> - **要做的事**：如果按新版执行，就要把昨天做的 MCP 那一半拆掉——编辑器里 MCP 分支的两张映射表删除、保存/校验/连接签名里对应逻辑删除、mock 种子里 MCP 的映射字段删除并升 version（用户浏览器里的本地缓存会重置为新种子）、删 2 个相关测试用例。API 那一半原样保留。工作量小（半天内），风险低，但属于"昨天刚做、今天推翻"，所以按流程必须你点头。
> - **顺带提醒 PRD 侧**：md 删了两节后还有 3 处残留句提到"映射"（§七.5 连接测试两句、§七.3 引用现有 MCP 一句），一览表 §9.3 也还留着 MCP 映射两行——这些已列入审查结果 Q392/Q393/Q55 转浦月。
> - **两种选择**：①以新版为准，回退 MCP 映射（建议，与 PRD/原型保持一致）；②认为删映射是产品误改，先不动代码、让浦月确认后再定。
>
> ✅ 2026-09-08 负责人指示"两边不要等了、按 PRD-20260908 来"，0908 md 已把 MCP 映射残留全部清理（Q392/Q55/一览表 §9.3）——按①执行回退。

### 第 4 项·API 请求映射默认示例是否含 `enabled(boolean)`

md 只写 filters→rules→field/value，原型多一个 enabled。默认建议：按 md（不含）。

### 第 5 项【已解决】·【新版内部矛盾·二选一】用户技能审核等级命名

§2.2「低危险/中危险/高危险」vs §2.3/§7.2 与原型界面「低风险/中风险/高风险」。建议：统一为「低风险/中风险/高风险/严重风险」并回改 md §2.2（转浦月）。

> ✅ 2026-09-08 PRD-20260908 md §2.2 L35–39 已统一为 低/中/高/严重风险（Q427 销项）。

### 第 6 项【已解决】·【跨文档冲突·二选一】字段字典 md vs 用户技能审核 md

风险等级 4 档（含"建议修改"）vs 5 档；风险类型顺序不同；字典"唯一配置来源"vs 新 md 硬编码逐项等级集合；字典 L61"本次不修改用户技能审核 PRD"已过期。需拍板：字段字典是否仍是该模块等级/类型的数据源（若是，新 md §7.2 逐项可选等级集合无处承接）。代码 `fieldDictMock.js:25` 等待此裁决。

> ✅ 2026-09-08 PRD-20260908 字段字典 md L36/L37/L56/L61：改 5 档、顺序同步、"统一配置来源"、明写"在用户技能审核模块 PRD 中独立定义，字段字典保持同步展示"——数据源 = 用户技能审核 md，字典只读同步；代码 `fieldDictMock.js` 按 5 档改（Q434–Q436 销项）。

### 第 7 项·用户技能审核页面形态

①查看技能——md/原型为右侧抽屉，代码现为新标签整页（复用 SkillFocusEditor 只读）；②审核状态「待审批」（本版唯此 md）vs 全站「待审核」；③页面说明文案取 md 还是原型；④分页取 md"动态分页"还是原型"无分页"（代码现 useAdminList 20 条）。建议：抽屉照 md、状态词提请浦月统一、说明文案按 md、分页维持 20 条。

> 2026-09-08 负责人裁决：**② 全站统一「待审核」**（代码维持现值；转浦月回改用户技能审核 md「待审批」→「待审核」，审查结果 Q429 按此销项）；**③ 页面说明文案取 md**（"审核用户上传的技能，查看用户端安全检测结果，处理需要人工核查的技能。"，Q399 以 md 为准）；**④ 分页取 md"根据页面高度动态分页"**（代码需新增按视口高度算每页条数的能力，原型公式 `floor((innerHeight−330)/62)`、下限 5 上限 30，Q403 以 md 为准）。
> **① 待定**——负责人反问"这两个页面在展示效果上一致，有什么问题么？"。说明：问题不在视觉，而在交互形态与内容两点——(a) md/原型是"留在列表页、右侧滑出抽屉、抽屉底部直接【通过】【驳回】、关闭回列表"；代码是"新开浏览器标签、整页展示、审核操作在页内右栏"，用户路径不同；(b) 代码整页缺 md §5.1 的提交人/提交时间/审核状态/审核尺度四个字段，SKILL.MD 走的是编辑器视图而非"原样只读不渲染"，检测明细未固定 4 项（只列上报项、无序号/位置/代码块）。若负责人认为整页形态可接受，则记"采纳现状：整页"，但 (b) 的内容缺口仍要补，且需通知浦月把 md §五"右侧详情抽屉"改成整页，否则下次导入会再核出。请回复：抽屉 / 整页（内容按 md 补齐）。

### 第 8 项【已解决】·一览表本版未更新

MCP 映射两行残留（§9.3）、无用户技能审核节、岗位发布阻断项数仍写 7 项（Q347）——属 PRD 文档缺陷，转浦月。

> ✅ 2026-09-08 PRD-20260908 一览表已更新（Q347/Q393/Q395/Q359/Q444）；残留：Q395 "选填"标注、L14 岗位描述 500 与岗位 md 2000 新矛盾（见改动记录-20260908 第四节）。

### 第 9 项·用户技能审核 md 缺陷打包转浦月

无截图、无异常/空态章节、用户端按钮名【提交管理后台审核】无出处、§2.2 颜色高/严重同"红色"不可区分、原型 `SCALE_DESC` 旧模型文案自相矛盾。

### 第 10 项·批量确认「以新版为准」（格式/措辞级与已实现一致项）

知识库三处回齐（第三节，代码小改 3 处）；API 递归细化（第四节，代码补 5 点）；用户删 10 条句（代码保留 10 不动）；AI 规范第 7 行（零改动）；字段字典格式重排（零改动）。

### 第 11 项【已解决】·改造启动授权与分批

确认后拟分三批——A 小改（知识库三处 + API 递归补齐 + 岗位管理页签改名等）；B MCP 映射回退（version bump）；C 用户技能审核整页重做（量级最大，依赖 5/6/7 裁决）。

> ✅ 2026-09-08 负责人裁决：**暂不处理，等负责人把原型和 PRD 的问题核对完再启动**。在此之前不动代码（含第 7 项已裁决的 ②③④ 三点，一并等待）。

### 第 12 项·卫生项处置

.bak 删除、.DS_Store 清理、01总览 补 .gitkeep、岗位管理 md 悬空图引用转浦月——建议随版本切换一并执行。

**可销项汇总（供 02-审查结果 0907 版标注）**：

- 完全销项（20，阶段二复核后定稿）：岗位系 Q5、Q6、Q12、Q15、Q24、Q26、Q27、Q32、Q34、Q36、Q37、Q40、Q49；知识库 Q64、Q65、Q75、Q79、Q89；用户 Q306；AI 规范 Q361。
- 部分销项/改写（阶段二定稿 51 条改写，其中实质部分销项）：Q1（认领说明删、领用页文案无定义）、Q2（§9.1 齐/§3.3 未齐）、Q4（数量齐/"无引用隐藏"新差异）、Q14（示例问题侧齐，SOP 的 AI 按钮禁用规则 md §2.5 仍未写）、Q21（§9.1 删/§3.4 未删）、Q31（草稿子句删/主体在）；Q59–Q63 的 MCP 半边作废（API 半边保留）。
- 未销：Q3、Q7（升级为 md 内部矛盾）、Q11、Q13、Q35、Q55、Q276、Q347、Q348、Q373、Q374。
- 建议新增（阶段二落 Q 号）：岗位删除按钮 title 原型独有；领用页文案卡 md 无定义；业务系统页签"无引用不展示"md 独有；达上限置灰 md 独有；业务系统页签搜索框 md 已删原型仍有；"修复更新"vs"修订版本"；原型 `window.positionRows` 未赋值；岗位管理 §4.3.3"任意已发布"残留；知识库 API 示例 `enabled`；原型 L1985 弯引号 class；知识库 md L405/L406 映射残留；一览表 §9.3 MCP 映射两行；用户技能审核 md↔原型全部差异（约 15 条）与跨文档冲突。
