# PRD-20260903 问题记录

> 按新版 PRD 导入流程第二步创建（2026-09-04）。逐条与负责人确认，达成一致后在问题标题加【已解决】；全部解决后文件名加【已解决】。

## Q1. API 编辑器「图标」与「示例问题」去留（新版内部矛盾）

新原型从 API 编辑器移除图标字段与示例问题区；但同版 prd-API.md（未改）仍要求两者必填，一览表 API 节仍列图标必填；且与 2026-09-02 B-5 拍板（API 弹窗加回图标）相反。2026-09-04 负责人指示：**先记录，不改造**——demo 保持现状（有图标、示例问题字段透传不展示），待拍板。

## Q2. 岗位「版本管理」能力去留（新版内部双轨）

新 prd.岗位.md 整节删除版本管理（版本历史启用/禁用能力消失，操作列去掉【版本管理】）；新原型列表侧却残留旧版本管理侧栏代码。2026-09-04 负责人指示：**先记录，不改造**——demo 保持现状（列表【版本管理】按钮与版本侧栏、启停能力保留），待拍板。

## Q3. 岗位发布阻断校验范围（md 7 项 vs 原型 validate 4 项）

新 md 9.1 列 7 项阻断校验（名称/图标/描述/认领说明≥1/示例问题3/SOP/采集字段），新原型 validate() 只查名称/描述/示例问题/SOP 4 项。随 Q2 一并挂账：发布前检查弹窗本轮不实现，demo 发布仍走现有版本侧栏路径。

## Q4. 岗位「审核中」操作按钮口径（新 md 内部两说）

新 md 3.1 明细行写"审核中：【查看】【编辑】（置灰）【撤回】共 3 枚"，同节总则又写"存在审核中操作时，仅展示【查看】和【撤回】"。先记录，demo 维持现状（编辑置灰展示），待拍板。

## Q5. AI 生成规范三处矛盾（Prompt 规范 vs 各模块 md / 规范内部）

① 按钮粒度：专家/MCP/API md 仍写"每条输入行旁一枚"，规范与新原型为"区级一枚一次生成 3 条"；② 规范通用参数 max-tokens 80/max-length 60 与 §3 岗位 SOP"≤400 汉字"矛盾；③ 业务系统 md 有 AI 按钮但规范 6 场景未收录。先记录；demo 现状为区级按钮，本轮 AI 实况生成机制照新原型实现、不动粒度争议。

## Q6. 新岗位分配 md L95 引用截图缺失（ST-5）

`岗位分配-申请审批列表页.png` 在 PRD-20260903 磁盘不存在，引用悬空。待补图或删引用。

---

> 以下 Q7 起为第二步改造中新发现的问题（2026-09-04），多数为「新版 md ↔ 新版原型不一致」——除注明外均**已按 md 落地**，待负责人确认后标【已解决】。

## Q7. 岗位·认领说明区命名与交互（md vs 原型终态）
md 三.2.3 为「岗位认领说明」（新增/保存/取消/删除、空态文案）；原型终态渲染为「领用页文案」（行内编辑、toast「领用页文案已保存」）。已按 md。

## Q8. 岗位·业务系统页签形态（md vs 原型）
md 三.8：【添加业务系统】/可移除引用/列=名称、图标、描述、状态；原型：「＋ 新业务系统」/Picker 标题「引用业务系统」/列=登录地址、业务页、最近更新时间/仅【查看】无移除。已按 md。

## Q9. 岗位·Agent 编辑字段上限（md 40/500 vs 原型 60/300）
已按 md。

## Q10. AI 生成延迟不统一（md 岗位 500ms vs 原型统一模块 420ms）
岗位侧按 md 500ms；共享 aiLiveGenerate（专家/技能/连接器）按原型 420ms——两口径并存，待统一。

## Q11. 知识库 mock 岗位种子与岗位模块不同源
知识库种子岗位范围为「销售顾问/HR 专员」（ps_1/ps_2），岗位模块种子为 经营分析岗等（401-404）→ 岗位工作台「知识」页签按可见范围过滤后现为空态。待拍板统一种子（建议改用 positionId 关联）。

## Q12. 岗位发布检查残留旧口径（随 Q2/Q3 挂账的补充）
冻结的 PublishCheckDialog 仍含「≥1 Agent 且每 Agent ≥1 技能」硬阻断，与新 md「Agent 与技能不阻断发布」冲突；新 md 阻断项「采集字段≥1」未纳入现行轻量前置门；新原型发布弹窗版本号为 v001-v999 而现链路为语义化 vX.Y.Z。均随 Q2/Q3 一并裁决。

## Q13. 岗位申请种子指向未发布岗位（PRD/原型种子矛盾）
原型申请种子 requestedPositionId=403/404 在岗位种子中为未发布，与 md §五「可绑定岗位仅包括已发布岗位」矛盾（「通过」会绑到未发布岗位，已照原型照搬未折中）。另：md 未定义审批「置顶高亮」解除时机（现实现=下一次查询/筛选/翻页解除）。

## Q14. 新原型审批表头 HTML 缺陷
L1562「提交时间」sort 按钮标签未闭合；排序交互按 md §4.1 文字稿实现。

## Q15. 知识库·抽屉「提交停用」按钮（md 有 vs 原型精修层移除）
已按 md 保留抽屉内按钮。

## Q16. 知识库·可见范围可否修改（md 自相矛盾）
§三.3.1 专家/岗位均「创建后不可更改」vs §三.5「变更可见范围需重新发布」；原型为专家锁、岗位可改。已按「专家建后锁、岗位可改且已发布改动回未发布」落地。

## Q17. 知识库·API 数据源原型超出 md
原型含 PUT/DELETE/PATCH、BODY/PATH 位置、透传开关、响应五字段全必填、filters 第三映射、精修层加 Bearer；md 仅 GET/POST、Header/Query、无鉴权/API Key、content 必 source/score 选。已按 md。

## Q18. 知识库·MCP 数据源鉴权枚举不一致
md：无鉴权/Bearer Token/自定义 Header；原型：无鉴权/Bearer Token/API Key，且内联含 stdio 传输（md 未提）。已按 md（无 stdio）。

## Q19. 知识库·上传预处理项数（md 4 项含图片理解 vs 原型 3 项）
已按 md 4 项。

## Q20. 原型缺陷：删除知识库无确认弹窗
md §三.4.3 要求确认；原型直删。已按 md。

## Q21. 知识库·两处文案不一致
删除置灰提示（md「正被知识库引用，请先解除引用」vs 原型「目前被 N 个知识库引用，暂不可删除」）与页面说明文案，均已按 md。

## Q22. 知识库·MCP 超时默认值 md 未给数值
md 只说「使用系统默认值」；已按原型 10000ms。

## Q23. 数据源连排分隔符（标准件「上传 ×2 / API ×1」vs 原型「·」分隔）
现按标准件口径，待拍板。

## Q24. 专家·背景色「固定 7 色」与原型隐藏自定义取色 input 矛盾
已按文案只实现 7 色板。

## Q25. 专家·背景色种子全为默认色
原型种子无 backgroundColor（归一化后全默认），列表着色无视觉差异；如需演示效果请指示给种子配不同色。

## Q26. MCP·示例问题「必填」标注但原型不强校验
区块标「必填，固定 3 条」，原型 validateMcpEditor 不拦空保存。已照原型行为（不拦），属 PRD 内部差异。
