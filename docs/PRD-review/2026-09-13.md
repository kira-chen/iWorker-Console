# 2026-09-13 改动记录

## 负责人 6 项决策执行完毕（测试审计待决策清单收口）

**需求/问题**：2026-09-12《待决策清单-20260912.md》六项，负责人全部批「认可团队建议 A」，两个附带项批「一起删 / 一并删除」。本轮执行落地。

**方案**：按「一件事一条」拆 9 条 commit（`fb2f0cc`…`21b75ad`）：

1. **决策 1（J6 字数上限）**：任务名称此前页面 60 / md 128 / 一览表通用规则 64 三处打架，按 09-08 拍板「md 数值 = 一览表统一规则」定为 **64**，说明取 md 的 **500**；页面、mock、md 三处对齐，并在一览表补上自动化任务的四个子字段行（名称 64 / 一句话指令 2000 / 说明 500 / 提示词 8000）——子字段未登记正是当初三处数字打架的根源。
2. **决策 2（J8② toast 二选一）**：统一为「已提交发布 vX.Y.Z，进入审核」（页面现状，零代码改动），md §二.3 回写；补上当初因 md 自相矛盾而未写的断言。
3. **决策 3（J2 员工端去留）**：整套退役——删 53 个产品文件（约 1.2 万行）、191 条用例，含附带项 Login / ChangePassword / 运行占位页 / ChangePasswordDialog / api/auth；摘除 router 员工端路由段与 Login 守卫、stores/user 兄弟 store 重置、featureFlags 的 FRONT_RUNTIME_ENABLED、request 的 401/1001 分支、AdminRail 账号菜单开关。**按实际引用保留** api/chat.js、utils/intakeForm.js、ChatMarkdown、RouteChip、IntakeFormFields（经效果测试链路仍被管理端使用）。可达性实证：删前 252 个产品文件全可达、0 孤儿；删后 199 个仍全可达、0 孤儿。
4. **决策 4（J3/J4 契约测试 + 报表两页）**：真接口分支是全站范式（23/28 个 api 文件、326 处），保留作研发示例；只删专验该分支 URL 拼装的测试——skillFiles 四件、market 收敛为 1 条（只留仍被审核中心调用的 reviewMarketListing）、adminModel 保留并改注（其函数在 demo 里都有活调用方）。附带项：报表两页连同 api/report.js、EChart.vue、路由段与 echarts 依赖一并删。
5. **决策 5（J12 审核联动）**：审核中心点【通过】/【驳回】后，按 md 联动八个业务模块落态、「我的申请」同步转已通过/已驳回并记审核人与时间；reviewsMock 只调各模块的 applyXxxReviewResult 入口，不碰别家内部数组（kb/mcp 反向 import 成环，分发用动态 import）。审核人取 demo 落盘身份，无硬编码。reviews persist v5→v6。+16 条联动用例、3 次能红验证。
6. **决策 6（J13 摘出三项）**：①「推荐问题固定 4 格」——md 全文无此功能、界面早无入口，却仍在 store/mock/详情页归一透传，整条链清理（positionMock persist v4→v5）；②工作档案配置的 askTier / pendingTtlDays / checklist——md §4.2.1 只定义三个控件，这三者 UI 零渲染（dataTableMock persist v1→v2）；③业务系统「AI 生成示例问题」——09-04 被新机制取代后注释写「保留不删」，此后零调用方，连同 api 包装与模板常量删除。

**影响面**：
- 测试：**160 文件 / 2247 条全绿**（全量连跑 3 次 + 两个固定 seed）；browser 5 文件 29 条绿；build 通过；skip/only/todo 为 0。
- 产品文件：252 → 199（净减 53）。
- 顺修两处质量问题：`adminPositionsOps` 的 featureFlags 局部 mock 未随删除同步；`modelConfigEditDialogSmoke` 的等红字用固定 sleep，单跑绿而全量偶红（element-plus 的 ElFormItem 红字有 100ms 防抖），改为「红字集合连续两轮不变即稳定」的轮询，与机器快慢解耦。
- 提交口径说明：`applyPositionReviewResult`（决策 5）因与决策 6 改同一文件，被 `7cec602` 一并带入，已在决策 5 的 commit 说明中注明。

## 合并 yuepu 当日 PRD 修订（含一次口径反转）

**需求/问题**：开工前按「开工先拉」拉齐远端，发现 yuepu 新推 3 笔 PRD 改动，与我方当日 md 回写在同一批文件上冲突。

**方案**：5 处冲突全部人工解决——状态独立列的措辞取 PRD 方版本（归口在他们），我方新增内容（MCP 撤回按待审类型、分页策略、字数补录等）保留；当日记录两侧小节都留。其中 `ad4abbe` 把字段字典 md 从「点击删除按钮仅从当前编辑草稿中移除」改为「**弹出确认弹窗**，确认后移除」，**与我方上午按旧 md 删掉确认弹窗的改动（J19/K32）方向相反**——按「md 唯一口径、以最新为准」恢复确认弹窗，用例改回「弹确认 + 取消则不删」，代码注释记下两次反复的来龙去脉。

**影响面**：`FieldManagement.vue` 与其用例；9 份 md 的合并结果。**提请负责人留意**：同一天内出现两次「上午按 md 改代码、下午 md 又改回去」，建议明确 PRD 正本的修改归口与通知方式，否则返工会持续发生且每次都要连带改代码与用例。

## 补记：09-12 两笔 §7 PRD 修订确认为正式口径

**需求/问题**：09-12 会话开头 pull 到 PRD 方（yuepu）直接提交的两笔正本修订——`df9b90e`「补齐岗位详情页『自动化任务』页签完整交互规范」、`ed839c3`「§7 更新：提示词 / 执行频率三模式 / 空闲时段提前准备」。按 `.claude/skills/prd-import/SKILL.md:10` 触发铁律，发现 PRD md 有 git 变更应「只报告 + 询问是否启动，修订确认前不动工改造」；当时已向负责人报告，但未等到确认即在测试审计中把这两笔当作正式口径比对代码并落地。

**方案**：2026-09-13 向负责人补确认，负责人明确答复「**这两笔就是正式口径**」。据此销项，不需回退任何改动。

**影响面**：确认后，以下已落地内容的依据成立——
- K1–K9（preKick 默认开启、提示词引导文案与 8000 字上限、三处 toast 与删除确认文案、搜索占位与两处空态文案）；
- K3 / K4 / K5 与 J5（三模式调度补齐、prototype / embedded 开关退役，含「保存丢调度字段」主链路缺陷的修复）；
- 负责人决策 1（任务名称 64 / 说明 500）——其前提正是「md §7.2 为正式口径，仅数值按一览表通用规则校准」。
文档侧：《待决策清单-20260912.md》附录二第 1 项标记销项。

**流程改进仍待定**：本次属「该问未问、事后补手续」。PRD 正本现由 PRD 方直接改、无知会机制（靠 pull 偶然发现），且 09-12、09-13 连续两次出现「按 md 改完代码、md 又改回去」（字段字典删除确认弹窗为例）。已向负责人提出需明确 **PRD 正本的修改归口与知会方式**，待示下。
