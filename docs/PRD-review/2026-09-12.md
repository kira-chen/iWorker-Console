# 2026-09-12 改动记录

## 需求 / 问题

岗位详情页「自动化任务」页签前端已实现完整交互（双栏布局、拖拽排序、脏检查、启停开关、引用工具与技能），但 PRD §7 只有简要字段说明，缺少布局结构、交互规则、字段限制等完整规范。

## 方案

根据前端实现 `PositionSampleTaskStage.vue` + `SampleTaskEditor.vue` 补全 PRD §7：

1. **§7.1 页签布局**：
   - 双栏结构：左侧任务列表（240px 宽）+ 右侧编辑区
   - 左侧列表：拖拽排序、缺指令红标、行内删除、软上限 20 条、空态 / 默认选中第一条
   - 右侧编辑区：未选占位 / 选中后展示 4 区表单、切换脏检查、卡头样式（灰底 + 3px 绿条）

2. **§7.2 基本信息**：
   - 任务名称（*）：必填，≤128 字
   - 一句话指令（*）：必填，≤2000 字，发布前后端硬拦（1003）
   - 说明：可选，≤500 字
   - 启用 / 停用开关：仅编辑态展示，位于卡头右侧

3. **§7.3-7.6**：补充调度计划、详细说明（≤8000 字 + 引导文案 + 字数计数）、引用工具（搜索 + 已添加平铺 + 健康度标签 + 弹窗勾选）、引用平台技能（已选 chips + 候选列表默认收起）

4. **§7.7-7.8**：保存 / 创建样例按钮、必填校验、删除二次确认、toast 文案

## 影响面

- **PRD 文档**：`docs/PRD/数字员工管理端PRD/02岗位/岗位/prd.岗位.md` §7 从 6 小节扩充为 9 小节（约 1700 字）
- **前端代码**：无变更（本次仅补齐 PRD，前端已实现）
- **对齐状态**：PRD §7 现与前端实现一致，消除「文档滞后」隐患

---

## 需求 / 问题（第二轮，自动化任务需求变更）

岗位自动化任务功能设计变更，涉及三处调整：
1. 「详细说明」改名为「提示词」，定位从通用任务描述改为专门描述任务目标、产出格式和推送方式；
2. 执行频率从「每天/每周/每月/仅一次」改为：按周期（每天、每周一、每周一三五、每周五、每月 1 日）、每间隔（小时、天、周）、单次三大类；
3. 新增「空闲时段提前准备」勾选项，控制结果是否在空闲时段提前生成。

## 方案（第二轮）

在 PRD §7 做三处更新：

1. **§7.1**：右侧编辑区 4 区表单说明中「详细说明」改为「提示词」。
2. **§7.3 调度计划**：重写周期类型说明，区分三种模式——
   - 按周期：每天 / 每周一 / 每周一三五 / 每周五 / 每月 1 日（固定预设选项）
   - 每间隔：以小时 / 天 / 周为单位的间隔循环
   - 单次：指定时间执行一次
   - 新增「空闲时段提前准备」勾选框，默认开启，附带两条状态注释文案。
3. **§7.4**：节标题由「详细说明」改为「提示词」，引导文案与用途说明更新为「描述任务目标、产出格式和推送方式」。

## 影响面（第二轮）

- **PRD 文档**：`docs/PRD/数字员工管理端PRD/02岗位/岗位/prd.岗位.md` §7.1、§7.3、§7.4 内容更新
- **前端代码**：待研发按新 PRD 实现（调度类型扩展 + 提示词字段重命名 + 空闲时段开关）
- **对齐状态**：PRD 先行，前端尚未实现新需求
