# 2026-09-12 改动记录

## 需求 / 问题

岗位详情页「自动化任务」页签前端已实现完整交互（双栏布局、拖拽排序、脏检查、启停开关、引用工具与技能），但 PRD §7 只有简要字段说明，缺少布局结构、交互规则、字段限制等完整规范。

## 方案

根据前端实现 `PositionSampleTaskStage.vue` + `SampleTaskEditor.vue` 补全 PRD §7：

1. **§7.1 页签布局**：
   - 双栏结构：左侧任务列表（240px 宽）+ 右侧编辑区
   - 左侧列表：拖拽排序、缺指令红标、行内删除、软上限 20 条、空态 / 默认选中第一条
   - 右侧编辑区：未选占位 / 选中后展示 4 区表单、切换脏检查、卡头样式（灰底 + 3px 绿条）

2. **§7.2 基本信息**：
   - 任务名称（*）：必填，≤128 字
   - 一句话指令（*）：必填，≤2000 字，发布前后端硬拦（1003）
   - 说明：可选，≤500 字
   - 启用 / 停用开关：仅编辑态展示，位于卡头右侧

3. **§7.3-7.6**：补充调度计划、详细说明（≤8000 字 + 引导文案 + 字数计数）、引用工具（搜索 + 已添加平铺 + 健康度标签 + 弹窗勾选）、引用平台技能（已选 chips + 候选列表默认收起）

4. **§7.7-7.8**：保存 / 创建样例按钮、必填校验、删除二次确认、toast 文案

## 影响面

- **PRD 文档**：`docs/PRD/数字员工管理端PRD/02岗位/岗位/prd.岗位.md` §7 从 6 小节扩充为 9 小节（约 1700 字）
- **前端代码**：无变更（本次仅补齐 PRD，前端已实现）
- **对齐状态**：PRD §7 现与前端实现一致，消除「文档滞后」隐患

---

## 需求 / 问题（第二轮，自动化任务需求变更）

岗位自动化任务功能设计变更，涉及三处调整：
1. 「详细说明」改名为「提示词」，定位从通用任务描述改为专门描述任务目标、产出格式和推送方式；
2. 执行频率从「每天/每周/每月/仅一次」改为：按周期（每天、每周一、每周一三五、每周五、每月 1 日）、每间隔（小时、天、周）、单次三大类；
3. 新增「空闲时段提前准备」勾选项，控制结果是否在空闲时段提前生成。

## 方案（第二轮）

在 PRD §7 做三处更新：

1. **§7.1**：右侧编辑区 4 区表单说明中「详细说明」改为「提示词」。
2. **§7.3 调度计划**：重写周期类型说明，区分三种模式——
   - 按周期：每天 / 每周一 / 每周一三五 / 每周五 / 每月 1 日（固定预设选项）
   - 每间隔：以小时 / 天 / 周为单位的间隔循环
   - 单次：指定时间执行一次
   - 新增「空闲时段提前准备」勾选框，默认开启，附带两条状态注释文案。
3. **§7.4**：节标题由「详细说明」改为「提示词」，引导文案与用途说明更新为「描述任务目标、产出格式和推送方式」。

## 影响面（第二轮）

- **PRD 文档**：`docs/PRD/数字员工管理端PRD/02岗位/岗位/prd.岗位.md` §7.1、§7.3、§7.4 内容更新
- **前端代码**：待研发按新 PRD 实现（调度类型扩展 + 提示词字段重命名 + 空闲时段开关）
- **对齐状态**：PRD 先行，前端尚未实现新需求

---

## 测试用例首轮全量审计与改造（新增 /test-audit 技能）

**需求/问题**：负责人要求做一个技能，能从多个视角审视现有测试用例——补齐缺口、更新冲突、删除无效等。背景是 09-11 连续三次「测试全绿但页面是坏的」（分页条 TDZ 白屏 15 页、技能页三列被遮、硬编码列漏扫），说明现有用例既守不住行为，也有相当一部分只是在断言桩被调用。

**方案**：新增 `.claude/skills/test-audit/`（SKILL.md + 视角与操作清单.md）：六个视角（A 产品口径 md↔用例 / B 代码现状 / C 可信度-假绿 / D 可读性 / E 变更增量 / F 结构覆盖）按模块分组派只读 agent 审查，汇总成 `docs/05-测试用例审计/测试用例审计-YYYYMMDD.md`（T 处置清单按「删除失效→更新冲突→修头注→合并拆分→提升断言→补真实挂载冒烟→补缺口」排序；K 代码缺陷清单不改代码交负责人；J 待裁决清单），然后按文件所有权并行实施、主流程复核后按模块各成一条 commit。三方（md/代码/用例）不一致的归因规则：md 唯一口径；用例≠md 且代码=md → 改用例；用例=代码≠md → 记代码缺陷不动；涉近期拍板或整文件删除 → 待裁决。当日跑了首轮全量：7 组审查 → 58 个 T 条目全部执行完 → 10 条 commit（`ee97329`…`36ff5a3`）。

**影响面**：
- 测试：149 文件 / 1840 条 → **185 文件 / 2444 条**（三个随机 seed 全绿，build 通过）；browser 套件首次取基线 5 文件 29 条全绿（原 themeTokens 闸因注释里的 `var(--c-primary)` 会红，已修）。views/admin 无测试页由 10 个降到 1 个（占位页）；新增 ListPagination 真实挂载用例（把 09-11 的 TDZ 重新注入 → 13/13 红）；9 个列表页 + 2 个编辑器补真 Element Plus 冒烟；adminSkillEdit 6 碎片合一、skillFileTree/positionModel 拆分、4 个文件按被测对象改名；「照原型」头注全部改为 md §节出处。各组共 25 次「改坏代码→红→还原」验证。
- 产品代码：零改动（仅 `domainExpertMock.js` 的测试复位函数 +3 行）。
- 交负责人：**K1–K46 代码缺陷**（高优先 3 项：K4 自动化任务三模式保存丢字段——demo 主链路；K34 MCP 停用直落「未发布」而非「审核中」；K35 API 抽屉顶部提示按 Q114 应有而无）与 **J1–J22 待裁决**（高优先：J1 状态独立列 8 份 md 未随 09-11 拍板回改；J2 员工端残留代码与 108+ 条用例整体去留；J3/J4 历史后端契约测试去留；J5 SchedulePicker prototype 开关退役；J6 §7.2 任务名称/说明上限 md 与代码不符是笔误还是要改）。均在审计报告九、十节，未裁前用例按现状保留、只加头注标注。
- 文档：根 CLAUDE.md docs 目录清单加 `05-测试用例审计/`；技能更新须负责人确认的约定写入 SKILL.md。
