# PRD-review 2026-09-02

## 裁决：全模块对齐轮差异清单首批销项（负责人拍板）

**需求/问题**：负责人对 2026-09-01 全模块 PRD 对齐轮汇总的 5 条优先待拍板项给出裁决/质询。

**方案（逐项结论）**：
- **P4（岗位编辑形态）——已裁决**：岗位新建/编辑确认使用当前代码版本的完整独立页面（PositionDetailTabs 9 子菜单工作台），不做原型式抽屉。工程跟进：工作台接口 mock 化（使 demo 下可打开可交互），本日实施；原型操作列【查看】因工作台无只读态暂仍不设，如需另议。
- **G-2（审核历史）——释疑后销项**：负责人质询「当前哪有审核历史」；说明：改造前审核中心列表为四态全集+状态筛选（可翻已通过/已驳回记录），2026-09-01 轮按原型删为只出待审核。该能力此前未被使用，删除成立，销项。
- **W-3（图标统一规则）——已裁决**：全站图标规则统一按 md 执行（图标库弹窗+上传≤5MB+方形裁剪 256×256+互斥替换+只读置灰+统一异常文案），本日实施于 IconPickerPopover（全站消费方同步生效）；AI 生成入口未裁撤、保留。
- **S-9（工具引用序列化）——已向负责人解释，待回复**：原型 `@tool(工具名)` vs 站内 `@tool[code]`（解析链契约），建议保留现状（视觉等价）。
- **B-5（API 弹窗图标先例复核）——已向负责人解释，待回复**：N4-① 旧裁决依据为原型基版；原型后置补丁层实际给 API/业务系统抽屉注入了图标字段。建议 API 弹窗把图标加回与业务系统/MCP 一致。

**影响面**：本节为裁决记录；P4/W-3 的实施影响面见本文件后续分节（实施完成后补记）。

## 全站：图标统一规则落地（W-3 拍板实施）

**需求/问题**：负责人拍板全站图标按 md 统一规则执行。现组件（IconPickerPopover）为 popover 内联网格 + 上传≤1MB 无裁剪，与规则不符；且各列表页图标 URL 判断七处各写一套、均不识别裁剪产物 data: URL，技能/专家列表甚至只按文字渲染图标。

**方案**：
- IconPickerPopover 重写：两枚入口【从图标库选择】（独立弹窗网格）/【上传图标】（类型校验 PNG/JPG/JPEG/WebP/GIF/SVG + ≤5MB，异常文案逐字「请选择图片文件」「图片不能超过 5 MB」）→ 方形裁剪弹窗（拖动 + 缩放滑杆 100%–300% + 重新选择/取消/使用该区域）→ canvas 输出 256×256 PNG dataURL（GIF 取首帧、SVG 栅格化）；互斥替换天然满足；新增 readonly 置灰；AI 生成入口保留（未裁撤）；不再调 uploadIcon 后端接口（纯前端产物，契合 demo）。裁剪几何与校验抽纯函数层 utils/iconCrop.js。
- 新建 utils/iconDisplay.js 统一 iconIsUrl（含 data:image/ 前缀），收口 7 处各自实现（AdminPositions/AdminMcp/AdminApis/AdminBizSystems 列表、McpEditor/BizSystemEditor/SkillFocusEditor 编辑器）；技能、专家列表补图片分支渲染（此前 data: 图标会显示为原始字符串）。

**影响面**：components/position/IconPickerPopover.vue（重写）、utils/iconCrop.js、utils/iconDisplay.js（新增）、上述 9 个消费文件（判断收口/补 img 分支）；props/emits 契约未变，5 个消费方零改动或仅注释更新。测试：iconCrop 10 例 + IconPickerPopover 8 例新增，全量 128 文件 / 1490 例全绿，vite build 通过。

**遗留**：api/position.js 的 uploadIcon（≤1MB 旧口径）已无消费方，是否删除待定。

## 岗位：工作台 mock 化落地（P4 拍板实施）+ 版本号口径统一 + 种子自洽治理

**需求/问题**：P4 拍板后，9 子菜单工作台成为岗位正式编辑页，但其接口在昨晚对齐轮未 mock——demo 下点「编辑」进工作台即加载失败。实施中另暴露两个问题：① 工作台发布弹窗仍硬校验旧 N5 三位数字版本号（vNNN），与列表版本抽屉/PRD 的语义化 vX.Y.Z 冲突（建议号会报格式错）；② 技能 mock 种子的 refNames 是幻影引用方（「销售顾问岗位」等不存在的岗位/专家名），与岗位/专家 mock 的真实引用不对账。

**方案**：
- 工作台全链路 mock：扩展 positionMock（岗位详情树/Agent 增删改/技能拉入与迁移/发布），新建 dataTableMock（工作档案 9 端点）、sampleTaskMock（自动化任务 9 端点），与列表页 4 岗种子、unifiedSkillMock 技能本体同源联动（引用变化双向回写计数与 refNames）；listSkills 选择器状态口径修正；知识/运行/效果测试 Tab 为既有「开发中」占位不发请求。新建岗位 → 跳工作台全程可用。
- 版本号口径统一（旧 N5 vNNN 废止）：validateVersionLabel/versionIncrementHint 改语义化 vX.Y.Z（样例文案「请填语义化版本号，例如 v1.2.0」），useVersionPublish 的建议号/历史最大推导改 semver patch 口径，「v999 上限」概念删除（atMax 改「无法自动生成建议版本号」通用文案），两处发布弹窗文案同步。
- 种子自洽治理：sk_301/302/304/305/307 的 refNames 改为与 positionMock/domainExpertMock 实际引用同源；sk_305 随治理改为已发布 v1.0.0（补齐客户成功岗引用闭环，同时扩大技能选择器可选池）。

**影响面**：api/positionMock.js（扩展）、dataTableMock.js、sampleTaskMock.js（新增）、position.js/dataTable.js/sampleTask.js（VITE_POS_MOCK 分流）、utils/positionModel.js、composables/useVersionPublish.js、PublishCheckDialog.vue、PositionPublishVersionDialog.vue、TargetPublishSection.vue（文案）、api/unifiedSkillMock.js（种子）；测试新增 3 文件 30 例 + 版本口径 4 文件重写。验收：全量 131 文件 / 1514 例全绿、build 通过、工作台 9 Tab 浏览器冒烟零错误。

**遗留/备忘**：
- 岗位发布前检查「每个 Agent 含 ≥1 技能」会拦下含空 Agent 的种子岗（401/402）——如实呈现检查项，演示通过路径需现场为空 Agent 挂技能（非缺陷）。
- 工作台旧 PRD（N5 vNNN 版本号）与新版本管理 PRD 的矛盾按后者统一，旧口径正式废止。
- effectTest 效果测试台仍为 flag 关闭的占位；uploadIcon 后端接口已无消费方，删除与否待定。

## 全站：mock 数据 localStorage 持久化 + 用户技能审核 mock 补齐（外发版本包可用性）

**需求/问题**：`版本包-20260901.zip` 外发后接收方反馈两问题：① 打开专家页即 404——查明为打包时点（9-1 20:42）早于当晚 mock 改造完成（21:46–22:44），包内是无 mock 分流的旧代码；② mock 数据只存内存，刷新即回种子态，「保存不了数据」。另全面排查发现导航可达页面中「治理·用户技能审核」（skillReview.js）是唯一无 mock 分流的残留缺口，demo 下必 404。

**方案**：
- 新建 `api/mockPersist.js` 公共持久化工具：各 mock 模块经 `attachPersist(key, {version, snapshot, restore})` 把内存状态镜像到 localStorage（前缀 `iworker-demo-mock:`），每个写操作末尾调 `persist()`。不可用兜底铁律：无 localStorage（node 单测/隐私模式）静默退化纯内存；版本不符/JSON 损坏/restore 抛错→丢弃存量回种子并清 key；写入失败（配额满）仅告警不影响交互。URL 带 `?resetMock=1` 一键清空回种子。**日后改 mock 种子结构须同步把该模块 version +1**（跨模块联动镜像如技能 refNames↔岗位引用，建议两 key 同步 +1 一起作废）。
- 17 个 mock 文件逐一盘点可变状态与全部写点后接线 15 个（domainExpert 12 写点 / fieldDict 1 / reviews 3 / myApplications 3 / positionAssignment 2 / adminUser 9 / adminModel 10 / bizSystem 10 / knowledgeBase 12 / mcpConnector 9 / sampleTask 6 / dataTable 6 / apiConnector 10 / unifiedSkill 16 / position 15，合计 124 个写点）；loginLogMock、feedbackMock 经盘点确认纯只读不接线。Map/Set 均为函数局部或已转数组处理；瞬态（连接器「验证中」、文档「解析中」）恢复时归一化到稳定态防卡死；跨模块引用（position↔unifiedSkill↔fieldDict）靠 ES import 依赖序天然保证恢复顺序。
- 补齐 `api/skillReviewMock.js` + `skillReview.js` 三接口 VITE_GOV_MOCK 分流（列表/详情/审核）；种子 4 条、id 直用 unifiedSkillMock 技能 id（sk_301/305/308/309）使详情页文件树可解析；riskItems 形状对齐 SkillFocusEditor（typeName/levelName/description）。字段管理·风险类型/等级 CRUD 无页面调用方，不 mock（头注释已标known限制）。
- README 更新：mock 层已接管+持久化说明、`?resetMock=1` 重置、「必须 npm run dev 运行（dist 下 mock 关闭）」提示。

**影响面**：新增 mockPersist.js、skillReviewMock.js 及两者单测（7+6 例）；17 个 mock 文件接线改动；skillReview.js 分流；README。行为面：demo 数据在使用者本机浏览器内跨刷新/重开持久（按域名+端口隔离，不出本机；换端口如 5174 视同新站点回种子态）。验收：全量 133 文件 / 1527 例全绿，vite build 通过，dev 服务器冒烟正常；重打 `版本包-20260902.zip` 外发。

**遗留/备忘**：员工端页面（chat/tasks/space/settings 等）与报表（report.js，菜单已隐藏）、效果测试（effectTest，flag 关闭）仍无 mock——不在管理后台导航可达面内，暂不影响外发 demo;若后续要演示员工端需另行 mock。

## 裁决：S-9 / B-5 销项（负责人拍板）

**需求/问题**：全模块对齐轮差异清单最后两项待裁决：S-9（技能正文工具引用序列化）、B-5（API 弹窗图标先例复核）。

**方案（结论与落地）**：
- **S-9 —— 接受现状，销项**：技能正文工具引用保持 `@tool[code]`（唯一标识）存储、编辑器胶囊显示工具名，视觉与原型 `@tool(工具名)` 等价；不照原型改存显示名（避免重名无法定位、改名断引用、解析链重写）。无代码改动。
- **B-5 —— 加回图标，销项**：API 编辑弹窗恢复「图标」必填字段（推翻 2026-09-01 N4-① 旧结论——该结论依据原型基版源码，原型后置补丁层 addConnectorIconField 实为 API 抽屉注入图标）。布局照原型注入位置：基本信息首行「名称 | 图标」，所属服务提供系统移至次行；交互复用 IconPickerPopover（与业务系统/MCP 同款，含 W-3 统一裁剪规则）；校验补「请选择或上传图标」；buildPayload 原有 icon 透传即生效，列表页图标自此有配置入口（此前 mock 种子有图标但弹窗无处设置）。

**影响面**：components/admin/ApiEditor.vue（图标字段 UI+校验+样式，头注释更新）。验收：相关测试 32 例 + 全量 1527 例全绿、build 通过、浏览器冒烟确认编辑抽屉「名称|图标」行正常渲染与回显。至此 2026-09-01 全模块对齐轮的 5 条优先待拍板项（P4/G-2/W-3/S-9/B-5）全部闭环。

### 追记：出厂数据机制（持久化二期）

**需求/问题**：负责人希望把自己本机摆好的演示数据随包分发，作为接收方打开时的原始数据；浏览器 localStorage 无法从文件系统直接读取，需要导出→烘焙链路。

**方案**：mockPersist.js 增加两能力：① `?exportMock=1` 打开一次即把本机全部 mock 数据导出为 `iworker-demo-data-YYYYMMDD-HHmm.json` 下载；② 新增 `mockSeedOverrides.json`「出厂数据」文件（默认 entries 为空 = 行为不变），恢复优先级改为 **localStorage 存量 > 出厂数据 > 代码种子**，出厂数据同受各模块 version 戳约束、恢复失败同样兜底回种子；纯内存模式（禁用存储）也应用出厂数据。制作流程：演示浏览器摆好数据 → exportMock 导出 → JSON 放入 mockSeedOverrides.json → 重打包。`?resetMock=1` 语义相应变为「清空本机改动、回出厂态」。

**影响面**：api/mockPersist.js、api/mockSeedOverrides.json（新增）、mockPersist.test.js（+5 例出厂数据用例）。全量 133 文件 / 1532 例全绿，build 通过。出厂数据已于当日 11:36 烘焙（负责人导出 iworker-demo-data-20260902-1135.json，含 unifiedSkill 1 模块——导出仅含发生过写操作的模块，其余模块两侧均走代码种子、效果一致），随 `版本包-20260902-1137.zip` 外发。

### 追记：全页面真浏览器验收（Playwright 扫描）

**需求/问题**：负责人要求验证所有页面数据可正常读取、展示、写入。

**方案**：Playwright + Chromium（复用项目缓存二进制）在独立 5199 端口 dev 实例上全量扫描：24 个路由页（列表 17 + 占位 4 + 深页 3：岗位工作台/技能编辑/审核详情）逐页断言无「加载失败」文案、零真实 /api 请求、零控制台错误、期望内容与表格行存在；写入面经页面同源直调 api 层验证 7 模块（专家/岗位/字段字典/技能/用户/知识库/用户技能审核），刷新后回读断言持久化生效；另验出厂数据加载（全新浏览器档案首开即含烘焙数据）与 ?resetMock=1 回出厂。

**影响面/结论**：32 项检查全部通过，零问题。期间排除一个假阳性：用户列表按最后登录倒序、未登录新用户排末页（每页 10 条），属正常分页语义，改用页面同款搜索接口断言。脚本存于会话 scratchpad，未入库。

## 运行：运行规格页启动（列表 + 抽屉，k8s Pod 资源模板管理）

**需求/问题**：负责人启动「运行规格」页设计与开发，基于 k8s 做 Pod 资源管理（增删改查/列表+右侧抽屉），并提供交互截图为基准（04运行 目录无 prd md 与原型 render，截图为唯一对齐依据）；要求尽可能复用已标准化组件。此前该路由挂「规划中」占位页。

**方案**：
- 领域建模：运行规格 = 一档 k8s Pod 资源模板（demo 语义 requests=limits，QoS Guaranteed）+ 运行策略。字段 ↔ k8s 映射（cpu→resources.cpu、内存→memory、临时磁盘→ephemeral-storage、出网禁止→NetworkPolicy 断外网）在抽屉以弱色提示展示，demo 不真连集群。
- 列表页（AdminRuntimeSpecs.vue，全标准件拼装：PageHeader/ListToolbar/ListStates/ListPagination/StatusTag/COL）：说明条承接截图 D17 口径（FDE 只见「规格名+能力边界说明」）；表格卡「运行规格模板」九列照截图——规格（名称+说明两行）/CPU·内存/临时磁盘/任务超时/空闲回收/并发/出网（允许绿·禁止红）/在用岗位（数量+默认/需审批/已审批标签，悬停岗位名单）/操作（编辑·删除）；底部「N 个规格 · M 个岗位已配置」汇总+标准分页；行序照截图。
- 抽屉（RuntimeSpecEditor.vue，DrawerEditor 收壳三态）：顶部时间行 → 基本信息（规格名称 必填≤64 唯一｜设为默认；能力边界说明 必填≤200，hint 注明 FDE 唯一可见）→ 资源配置（CPU 核/内存 Gi/临时磁盘 Gi，k8s 字段名弱提示）→ 运行策略（任务超时/空闲回收/并发上限/出网/使用需审批）→ 在用岗位（编辑态只读 tag+审批态）。护栏错误按 field 就地红框。
- mock（runtimeSpec.js + runtimeSpecMock.js，开关 VITE_RUN_MOCK）：截图 5 档种子逐字；护栏=名称唯一、默认全局唯一（设新默认自动取消旧默认、唯一默认不可取消）、默认规格不可删、被岗位使用不可删（提示携岗位名单）；在用审批汇总（需审批+有待审→需审批标签，全过→已审批）。
- 路由 runtime-specs 换正式组件（name/path 不动）。

**影响面**：views/admin/AdminRuntimeSpecs.vue、components/admin/RuntimeSpecEditor.vue、api/runtimeSpec.js、runtimeSpecMock.js（新增），router 一行换组件；api/__tests__/runtimeSpecMock.test.js 5 例。全量 1537 例全绿、build 通过、浏览器截图核对列表与抽屉。

**待拍板差异 / 遗留**：
- R-1 截图中的「D17」编号（副标题与说明条内）按 PRD 批注理解，页面正文未展示编号本身，仅保留其文案口径；如需原样露出「D17」字样请示下。
- R-2 「在用岗位」为示意绑定数据（含岗位 mock 4 个真实岗位名+若干示意名，合计对齐截图 15）：岗位侧尚无「运行规格」选配字段，两模块暂不联动；待岗位工作台加规格选配后从 positionMock 同源接通（含「使用需审批」的审批流落地）。
- R-3 截图操作列仅「编辑」；按「增删改查」诉求补了「删除」（带默认/在用双护栏）。「设为默认」放在抽屉内 switch（列表未加快捷入口）。
- R-4 抽屉「查看」只读态组件已支持（DrawerEditor readonly），列表暂未放【查看】入口（与截图一致）；需要时一行接通。

## 运行：运行规格两点修正（负责人拍板：绑定对象=用户、取消默认规格）

**需求/问题**：负责人对首版运行规格页两点修正——①规格与**用户**相关而非岗位：短期版本为每个用户分配 Pod；②不需要「默认规格」设置。

**方案**：
- 绑定对象全面改为用户：列表「在用岗位」列→「在用用户」（数量+需审批/已审批标签，悬停用户名单）；抽屉「在用岗位」区→「在用用户」（副注「短期版本按用户分配 Pod」）；删除护栏改「被 N 个用户使用（名单），需先为这些用户改配其他规格」；底部汇总改「N 个规格 · M 个用户已配置」；说明条改「为每个用户分配 Pod 运行环境」；「使用需审批」语义改「为用户分配该规格需管理员审批」。
- 数据同源治理：usedUsers 用 adminUserMock 的 13 个用户种子做**全量分配、每人一档不重复**（轻 4 / 标准 6 / 重 2 含 1 待审 / 高敏离网 0 / 专属 1 = 13），替换首版示意岗位名；种子文案中「岗位」措辞同步改「用户」。
- 默认规格概念整体移除：抽屉「设为默认」开关、列表「默认」标签、mock isDefault 字段与「默认唯一/默认不可删/唯一默认不可取消」三条护栏全部删除。

**影响面**：runtimeSpecMock.js、RuntimeSpecEditor.vue、AdminRuntimeSpecs.vue、runtimeSpecMock.test.js（重写为用户口径 5 例，含 13 人全量分配唯一性断言）。全量 1537 例全绿、build 通过、浏览器截图核对。

**遗留更新**（覆盖首版 R-2）：用户管理侧尚无「运行规格」分配字段与审批流入口，规格↔用户绑定暂为 mock 静态数据；待用户侧「分配规格」交互拍板后与 adminUserMock 双向联动。
