# 2026-09-07 改动记录

## 流程：prd-import 技能改版（负责人定义新流程）

**需求/问题**：负责人重新定义 PRD 导入流程：①新版文件夹投放 docs/prd/PRD-yyyymmdd；②版本变动审查产出《改动记录-yyyymmdd.md》入 docs/01-新版本PRD改动记录/（三段式格式，永久留档）；③md↔原型一致性审查产出《PRD与原型一致性审查结果-yyyymmdd.md》入 docs/02-PRD和原型一致性审查/。四项衔接点经负责人拍板：改动记录兼并待确认清单职责；取消独立问题记录台账（决策直接批注两份文件）；改造/验证阶段保留、改为拍板后启动；补建 0904 首份改动记录。

**方案**：重写 .claude/skills/prd-import/SKILL.md 为五阶段（版本变动审查→全量一致性审查→负责人审核与版本切换→拍板后改造→验证提交），并固化 2026-09-06 实跑口径（并行审查代理分组、最终覆写生效态、七类聚合模板、【上版日期已存在】标识、不主动 push、发现新文件夹先报告不自动启动）。目录落地：负责人已自行将审查文件夹更名 02-PRD和原型一致性审查/（git 随动入库）、预留 03-PRD和原型待决策差异点/（暂不启用）；新建 01-新版本PRD改动记录/ 并补建《改动记录-20260904.md》（由已删对比分析改写，git 49b1e2c 可溯，待确认清单标注 09-06 裁决结果全销）。CLAUDE.md docs 目录清单、移交文件指向、memory（prd-version-workflow）同步更新。

**影响面**：.claude/skills/prd-import/SKILL.md 重写；docs/ 目录结构（01/02/03 编号体系）；CLAUDE.md 一处；零代码改动。遗留一项待下轮确认：2026-09-04 拍板的"每轮合并建时间线文件"未纳入新流程，下轮导入时提请负责人定去留。

## 改造轮：PRD-20260904 已确定内容对齐（负责人指示提前启动）

**需求/问题**：负责人指示"参考 PRD 中已确定的内容，先对前端代码做一轮改造优化"——即把原定等下周合并的三项已授权改造提前执行（371 条待浦月核对的差异不在本轮范围）。

**方案**：
1. **知识库数据源编辑器重构**（PRD-20260904 md §六/§七 新口径，实施 agent 完成）：API 源——请求方法五枚举默认 POST、鉴权 无鉴权/API KEY 多参数表（复用 ParamRowsEditor+validateApiAuthParams）/Bearer（遮罩+留空保留）、请求映射结构化预设行 query/topK（固定不可删，object/array 递归子字段）、响应映射预设行 content/source/score、保存按连接签名重置未验证；MCP 源——删「引用现有」模式（EXISTING/mcpId/toolName 废弃）、传输方式 streamable-http（Endpoint+无/Bearer/API Key 鉴权）/stdio（Command 五枚举下拉+Arguments+环境变量表）、检索工具多选 ≥1（测试成功获取清单）、映射与 API 同构（新共用组件 SourceMappingEditor+递归 SourceMapParamRows）、超时 10000/1000–120000。mock 种子按新结构重写、密钥全遮罩、persist version 3→4。
2. **「岗位分配」→「岗位管理」改名**：AdminRail 菜单 label、路由 meta.title、角色权限树页面名与 FDE 角色种子（adminUserMock version 1→2）、AdminRail/RoleEditor 测试期望同步。页签「用户岗位分配」维持 md 现口径不动（底账 Q34 未决矛盾）。
3. **工作档案规则名改自由输入**（PT-C7）：DossierRuleListEditor 受控下拉（filterable+allow-create+编目字段联想）改纯 el-input；keySuggestions prop 与父级联想链路清理。

**改造中新发现**：10 条 md↔原型差异（字段标签、鉴权默认值、API KEY 形态等），均按 md 落地；其中 7 条底账已有对应条目，3 条真正新增已追加为底账 Q372–Q374（B 类，头部计数 371→374、新引入 30→33）。

**影响面**：utils/knowledgeBaseMeta.js、api/knowledgeBaseMock.js、api/knowledgeBase.js、components/admin/KnowledgeSourceEditor.vue（重排）+ SourceMappingEditor.vue/SourceMapParamRows.vue（新）、components/admin/AdminRail.vue、api/adminUserMock.js、router/index.js、components/position/dossier/DossierRuleListEditor.vue、components/position/PositionDataTableStage.vue、views/admin/AdminPositionAssignments.vue（注释）；测试 5 处更新+1 新增。冻结区（上传配置/文档抽屉/检索测试/列表页/发布链路）未动。知识库与组织 mock 快照将重置回种子。验证：全量 vitest 1635 用例全绿、vite build 通过、Playwright 冒烟零错误、抽屉截图与原型骨架逐区块比对通过。
