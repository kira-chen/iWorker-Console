# 2026-09-10 改动记录

## 工程大原则入册 + 冗余治理第二批待决清单全量闭环

**需求/问题**：①负责人口述工程大原则，要求记录并作为后续一切工作与方案的评判基准；②负责人对第二批治理遗留的待拍板/确认清单（P1-P5、C1-C3，见 2026-09-09.md 末节）批示「按团队建议执行」（P4 前一日已单独批「先不处理」）。

**方案**：
1. **大原则写入根 CLAUDE.md 新设「工程大原则」节**（项目简介之后、重要约束之前）：本工程 = 前端代码开发 + 浏览器数据轻量化存储实现产品 DEMO，双目标 = 完整表达产品经理的功能设计想法 + 以代码示例让研发容易读懂；与两目标均无关的投入默认不做，技术债治理以可读性收益为取舍标准。
2. **清单逐条闭环**（裁决批注见 2026-09-09.md 末节清单原位）：P1 筛选拼写保留现状、P2 报表页失败态不统一、P3 用户端失败态不收编、C1 失败帧知会销项不回退——四条均零代码改动；C3 体验观察立项留档为「体验优化批次」候选（重点 4 条优先），暂不动工，启动时按 PRD 对齐流程逐条走；P5 随 P4（09-10 已批缓做）搁置。
3. **C2 当日落地（唯一代码改动）**：`src/__tests__/moduleGraph.test.js` 具名导入守卫新增 `parseImportNames`——切分导入名前先剥 import 大括号内的 `//` 与 `/* */` 注释（修复 09-09 批 2-2 实施时踩到的注释并入导入名误报），自检用例补 2 条含注释 fixture 防回归。

**影响面**：根 CLAUDE.md（+1 节）、1 个测试文件（守卫解析器，非生产代码）、2026-09-09.md 清单批注。**vitest 149 文件 1834 条全绿、build 通过**。至此第二批治理 9 条待决项全部闭环：7 条维持现状/留档、1 条缓做（P4）、1 条落地（C2）。下一批候选池：PositionDetailTabs 拆分（问题驱动，前置条件已注）、体验优化批次（C3 留档 4 条重点）、工程债台账 3 条（撤回文案同源化、displayView 死重 spread、知识页签种子域对齐）。

## 「全部启动」批次：体验优化 + 工程债 + 大文件拆分（问题驱动条款提前触发）

**需求/问题**：负责人指示候选池全部启动、加快速度。含：C3 体验优化（最终用户 11 条观察 E1-E11）、工程债 3 条（D1-D3）、P4 大文件拆分（负责人指示覆盖前一日「缓做」拍板）+ 拆分前置的 802KB 归因实证。

**方案**：双路并行——A 路（主工作区）做体验+工程债，W 路（隔离 git worktree）做归因+拆分，改动落点物理隔离零冲突；各自 vitest/build/Playwright 自证后由主流程逐路复核合并。**注：本批 PRD 查证基于现行 PRD-20260908（任务下发时误写 20260828，该目录已不存在，根 CLAUDE.md 铁律节的版本引用待负责人确认后更新）。**

A 路结果（commit 7c62f4e）：落地 7 条——E1 专家标签防截断、E5 时间列 fixed-right（原型明文列宽未压）、E6 部分（ExpertEditor 查看态正文色）、E8 星火 mock 描述大白话化（PRD 0 命中确认自造，conn version 1→2）、E11 空值长横统一、D1 撤回文案同源、D2 死重 spread 收敛、D3 知识库种子岗位域对齐 401/403（kb version 5→6，知识页签演示有数据）。**按 PRD 铁律不擅动 6 条**（E2/E4/E7/E9/E10 均查到 PRD/原型明文依据 + E3 只调查），列入下方裁决单。测试改动 2 处均为行为变化直接对应。

W 路结果（commit 46d089e）：①**802KB 归因实证推翻方案原推断**——真凶是 PositionSampleTaskStage→SampleTaskEditor→MarkdownEditor→md-editor-v3 链路约 695KB，SkillMilkdownEditor 仅 458 字节（已回填方案文档勘误 6）；②PositionDetailTabs 1933→973 行，人格/采集/知识/Agent技能四页签抽为 components/position/ 子组件，子组件直用 store 只收 isReadonly，跨层接线 provide/inject；测试零改动、16 张拆前后截图 13 张逐像素一致（余 3 张为光栅化噪声）、深链 16 条全过、chunk +0.13% 符合预期。

收尾（本 commit）：移交项 E6（position-detail.css 全局规则：pd-card 内 disabled 控件真实内容正文色、placeholder 保持弱色——实走验证 403 查看态 fill-color 与 body 一致）与 E11 残余 5 处短横统一；方案文档勘误 6 回填归因结论。

**影响面**：A 路 11 文件 + W 路 5 文件（新建 4 子组件）+ 收尾 3 文件 + 2 docs。**vitest 149 文件 1834 条全绿、build 通过、chunk 803484 字节（+0.13%）**；实走抽验 403 查看态正文色 / 知识页签 3 行数据 / 零 console error。

**待负责人裁决单（体验项，均因 PRD/原型明文而未动，改任何一条需同步修订 PRD）**：
- [ ] R1（E2）「待审核」（申请结果四态，prd.我的申请 L34）与「审核中」（发布三态）同屏易混：a.维持现状 b.PRD 层统一措辞
- [ ] R2（E3）菜单「岗位」/「岗位管理」易混（原型侧栏明文）：a.「岗位」→「岗位配置」 b.「岗位管理」→「岗位分配」 c.「岗位库」+「岗位分配与审批」
- [ ] R3（E4）版本抽屉打开即显必填红字（prd.技能 L230+原型三处双明文）：是否改「提交时才提示」
- [ ] R4（E5 残留）两列表横向滚动仍在（原型 colgroup 明文列宽）：是否放开压缩列宽
- [ ] R5（E7）「Top K/召回/耗时」（prd.知识库 L160/163/312 明文）：是否大白话化
- [ ] R6（E9）新建技能弹窗 zip 主体+手动创建 link 式（原型 L1408 明文）：是否升级手动创建为可见按钮
- [ ] R7（E10）通用技能引用列显「—」（prd.技能 L45 明文）：是否改「不参与引用」

## 归档文件统一迁入 Archive/（不入库）+ CLAUDE.md 失实项记录

**需求/问题**：负责人两条指示——①CLAUDE.md 的 PRD 版本引用失实问题「先记录」；②各类已归档不再使用的文件统一移入 Archive/ 文件夹，且 Archive/ 不提交 GitHub。

**方案**：①记录：该问题连同另两处已由《docs/文档存废盘点-20260909.md》§五 记录在案（1. 对齐基准仍写 PRD-20260828 应为 PRD-20260908 且原型文件已改名；2. 「md↔原型不一致记待裁决」铁律与新确立的「原型逐步退场、md 为唯一口径」冲突；3. 03/04 目录说明滞后；README.md 三处同病）。**CLAUDE.md 与 README 的修订待负责人明确批准后执行，本日只记录不动。** ②迁移：docs/归档/ 全部 9 份（8 份失效留痕 + 原 README）迁 `Archive/docs-归档/`，根目录版本包 zip 迁 `Archive/`；.gitignore 增加 `Archive/`；docs/归档/ 原位留一份指路 README（迁移说明 + git 恢复命令 + 当前口径指引）。盘点判定「保留不动」的两项（PRD 包内已修改问题清单、调研讨论分路明细）与在用文档均未动。

**影响面**：git 侧 8 份归档文档从仓库移除（迁出前均完整入过 git，随时可经父提交恢复）+ README 改写 + .gitignore；**代价如实说明：在册文档中 8 处指向 docs/归档/ 的历史引用对仅有 GitHub 仓库的协作者成为断链**，由指路 README 兜底（负责人指示优先于盘点原「保留入库防断链」策略）。零代码改动，vitest/build 不受影响。

**追加（负责人二次裁决，文件夹级处置）**：①`docs/归档/` 整个文件夹删除（含指路 README）——8 处在册历史引用成为无指引死链，内容仅存本地 `Archive/docs-归档/` 与 git 历史；②`docs/PRD-review/` 早期日份（2026-09-01~09-07 共 6 份）迁 `Archive/PRD-review-早期日份/`，库内仅保留 09-08 起三份；台账约定不变，后续照常按日写入本目录。迁出/删除文件均可经本提交的父提交恢复。

## prd-import 技能改版（负责人指示）

**需求/问题**：负责人指示按近两日工作模式更新 prd-import 技能，两项根本变化：①新导入 PRD 不再包含交互原型 html；②版本开发转 GitHub 多人协同。

**方案**：重写 `.claude/skills/prd-import/SKILL.md`（2026-09-10 版）——阶段二由「md↔原型一致性审查」改为「**md↔代码现状一致性审查**」（02 目录沿用、产出文件名体现新基准；代码侧以页面实际渲染+mock 语义为准，实走取证）；原型专属环节（最终覆写态提取、骨架提取、截图并排比对）全部退役，含原型旧流程存 git 历史 09-07 版备查；视觉基准改为站内既有范式+设计令牌+改前基线截图。协同模式入流程：动工前 pull、高风险批走隔离 worktree、批次闭环全绿才 commit（显式路径）、push 仅按负责人指令推阶段版本+门禁。另吸收两条实战教训入任务书模板：架构师方案先行且盘点以实数为准（旧审计数字两次实证失实）、大改动批次加多角色验收；修正旧句「03 目录暂不启用」为已在用。

**影响面**：仅 1 个技能文件；不触碰代码与测试。注：根 CLAUDE.md「PRD 对齐铁律」节仍是原型时代旧表述（盘点 §五 在案），与新版技能并存期间以技能与盘点为准，待负责人批准后修订 CLAUDE.md。

## CLAUDE.md / README / daily-update-log 失实口径修订（负责人批准）

**需求/问题**：负责人批准修订盘点 §五 记录的失实项（此前只记录未动）。

**方案**：①CLAUDE.md「PRD 对齐铁律」节改版（原 09-01 版废止）：对齐基准 = 现行版本 PRD 各模块 md（当前 PRD-20260908），**md 唯一口径**，原型退场；md↔代码不一致默认按 md 落地并记录、与近期拍板冲突的提请裁决；视觉基准 = 站内令牌+既有范式。②docs 目录说明更新：02 目录审查对象改 md↔代码现状、03 目录已在用、补登 04 与复盘目录。③prd-import 流程描述同步 09-10 改版。④历史归档 zip 已不存在，两处描述改为「Archive/ 本地保留 + git 历史找回」。⑤daily-update-log 技能两处基准表述同步。⑥README 三处（L8/L24/L54）同步。

**影响面**：CLAUDE.md、README.md、daily-update-log 技能，零代码改动。残留检查确认全仓再无 PRD-20260828/原型 v2/历史归档 zip 的失实引用。

## PRD 目录扁平化 + git 直改模式确立（负责人三条指示）

**需求/问题**：负责人拍板 PRD 维护模式转变——不再投放日期文件夹整包替换，改为直接基于 GitHub 版本更新。三条指示：①PRD-20260908 的内容上移到 prd 层并删除空文件夹；②prd 目录更名 PRD；③prd-import 技能配套改版；完成后推送 GitHub。

**方案**：①②目录改造（git mv，120 个文件保 rename 历史）：`docs/prd/PRD-20260908/数字员工管理端PRD/` → **`docs/PRD/数字员工管理端PRD/`**（长期正本，git 直改，不再按版本换目录；大小写改名走两步 mv 兼容 macOS 大小写不敏感盘）。③prd-import 技能二次改版（git 直改模式）：触发改「PRD md 出现 git 变更」；阶段一文件夹比对改 **git diff 基线对比**（基线 commit 记在上一份 01-改动记录尾部，批次闭环后推进）；「版本切换/删旧文件夹」环节取消，改为基线推进；01/02 产出改按修订批次日期命名；旧整包导入流程存 git 历史 2026-09-10 上午版兜底。引用面同步：CLAUDE.md（铁律基准句、PRD 修订流程句、docs 目录说明）、README 三处、daily-update-log、pm.md、designer/product/frontend 三个角色定义（顺带把其中残存的 PRD-20260828+原型双基准旧口径一并换新）、frontend 源码 2 处现行规则注释路径 + SkillFocusEditor 1 处历史出处改措辞；源码中「2026-09-08 PRD-20260908 对齐」类历史批次名注释按事件名保留不动。

**影响面**：120 个 PRD 文件 rename + 11 个文档/技能/角色/源码注释文件；零逻辑代码改动。后续 PRD 修订操作方式：直接改 `docs/PRD/` 内 md 提交（本地或 GitHub 网页均可），知会后按新版 prd-import 流程处理。**本批 git diff 基线：以本次推送后的远端 main 为 PRD 正本起始基线。**
