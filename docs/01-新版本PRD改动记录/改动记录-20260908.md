# 改动记录-20260908（PRD-20260907 → PRD-20260908）

> 新版 PRD 导入流程阶段一产出（2026-09-08 增量核对，5 路并行只读复核：岗位系 / 知识库+API+一览表§9 / 专家·技能·模型·字段字典 / 治理域 / 组织域+根文档+连接器承接）。本文件只记版本间变动与待确认项；md↔原型全量一致性体检另见 `docs/02-PRD和原型一致性审查/PRD与原型一致性审查结果-20260908.md`（阶段二产出，391 条）。
> **本版性质**：产品经理（浦月）按我方 0907 审查结果回改 md 的"回流版"，随包附《已修改问题清单.md》（申报 58 条修改 + 4 条"确认保留不改"）。负责人 2026-09-08 指示：**本版之后的 md/原型/代码修订由团队自行处理，不再回流产品经理**。

**总体结论**：**md 单侧定点修订，原型零改动**——不是纯增量。原型 html 与 0907 逐字节相同（md5 `78760d22…`），17 份文档改写（15 份模块 md + 一览表 + AI 规范），MCP、业务系统两份 md 未动，**模型 md 逐字节未改但清单申报 Q226 已修（申报不实）**。改动方向以 **"md 向原型回齐"为主**（用户 15 条、用户技能审核 13 处、岗位 30 余处、知识库 11 处、治理 4 处、专家 2 处），少量"md 补写原型未有的规则"（一览表 §八点五/§十、AI 规范 §7、审核中心"其他"）。核验结论：申报 58 条中 **43 条完全落地并与原型一致、9 条部分落地、3 条未落地（Q226 / Q375 / Q80）、4 处改错（Q220 / Q275 / Q276 审核中心侧 / Q93）**；另有 **18 条未申报改动顺带销项**、**31 处未申报改动新引入差异或格式缺陷**。**三处推翻/绕过负责人 09-08 拍板**：`04运行/` 第四次被移除（拍板①）、用户技能审核页面说明与分页句改成原型口径（拍板③④）、字段字典 L61 单方面回答了待确认第 6 项。因代码是按 0907 md 落地的，本版所有"回齐原型"改动都意味着**代码要跟改**（影响面见各节）。

**结构级差异**：

| # | 项 | 说明 | 定性 |
|---|---|---|---|
| ST-1 | 包根多一层 `数字员工管理端PRD/` | 真正的 PRD 根在 `PRD-20260908/数字员工管理端PRD/`；包根另放 `PRD与原型一致性审查结果-20260907.md`（与我方 02 目录文件逐字节相同，PM 未在其上加任何批注）与 `已修改问题清单.txt`（与包内 `.md` 同内容仅表头格式差） | 结构变化 + 冗余副本（卫生项） |
| ST-2 | `04运行/` 第四次被移除 | 09-04、09-06、09-08 三次拍板"保留占位"，09-08 上午刚向 0907 包移植 `.gitkeep`；回流包再次不含 | **复发项**（推翻 09-08 拍板①） |
| ST-3 | `01总览/` 空目录无 `.gitkeep` | 第三次复发 | 卫生项（复发） |
| ST-4 | 原型 html 逐字节相同 | 0907 全部 28 条 D 类原型缺陷（含 L1985 弯引号致 MCP 检索节失效 Q391）原样存在；原型行号与 0907 底账一致 | 零改动 |
| ST-5 | `.obsidian/` ×2（包根 + PRD 根，各 4 个 json） | 新带入的编辑器配置 | 卫生项 |
| ST-6 | `03能力/知识库/prd.知识库.md.bak` 第四次带入 | md5 `db724152…` 与 0903 旧稿相同 | 卫生项 |
| ST-7 | `.DS_Store` ×1（包根） | — | 卫生项 |
| ST-8 | 岗位管理 md L94 引用 `./岗位分配-申请审批列表页.png` 不存在 | 第三版沿袭（Q35） | 缺陷（旧版遗留） |
| ST-9 | `05治理/用户技能审核/` 仍仅 1 个 md 无截图 | Q433 未处理 | 缺陷（旧版遗留） |
| ST-10 | 新增 `已修改问题清单.md` | PM 侧修改台账，作为本次核验的申报基线；申报与实际不符 5 处（Q226 未落地、Q375 未落地、Q2 描述"删采集字段"实为"删图标"、Q276 误挂审核中心、Q80 仅换措辞） | 增量（资料） |
| ST-11 | 行尾 | 岗位 md 仍 CRLF（431→458 行）、模型/用户/角色 md 仍 CRLF；用户技能审核 md 由 CRLF 转 LF | 卫生项 |
| ST-12 | 专家 md 新引用 `专家-发布确认.png` | 图为旧流程"居中弹窗 + 【提交审核】"，与 md §3.3 侧栏流程、原型均不符（Q458） | 缺陷（新引入） |

---

## 一、岗位：md 再向原型回齐 30 余处 + 领用页文案补定义 + 工作档案/自动化任务两节整体重写（申报 5 条，实际改动 22 处）

**需求/问题**：清单申报 Q382（新建入口居中弹窗）、Q381（领用页文案节 + 认领说明改名）、Q2（发布阻断 6→4）、Q375（删除按钮悬停提示）、Q21（采集字段不阻断）。实际 diff 22 处：①§一.1/§1.1 新建入口改"居中弹窗（岗位名称 + 岗位描述），创建后提示'岗位已创建，请完善岗位配置'"；②§二.1 删"页面不分页"句；③§3.3 前置条件删岗位图标、领用页文案 → 4 项；④§3.7 删"编辑抽屉内【发布】"；⑤§1.3 页签由"6 标准 + 业务系统 MutationObserver 动态注入/无引用不展示"改为**固定 7 个**；⑥§2 补列举 6 区域；⑦**§2.1 岗位描述上限由"人格页 500 / 弹窗与抽屉 2000"改为统一 2000**；⑧**§2.3 新增「领用页文案」节**（可选、≤6 条×100 字、空态/toast 逐字取自原型）；⑨§3.4 "采集字段不参与发布阻断"；⑩**§4 工作档案整节重写**（左栏档案卡片 + 右栏 基本信息/编目信息/档案详情，抽取方式/置信度阈值/用户确认、字段类型/字段用途/唯一 ID、归纳方式四项）；⑪§5.1 知识页签字段改"知识库名称、描述、数据源、文档数量、状态、操作"；⑫**删 §5.2 新建知识库**、§5.3 删【编辑】与"【← 返回】"；⑬§6.1 删拖拽；⑭§6.2 Agent 名称 40→64、职责描述 500→2000；⑮§6.3 【删除】→【确认删除】；⑯§6.4 技能卡片字段改"技能名称、技能分类、工具数量"、删"本岗位已引用"；⑰**§7 自动化任务整节重写**（7.1 基本信息 / 7.2 调度计划 / 7.3 详细说明 / 7.4 引用工具 / 7.5 引用平台技能），删原 7.3 发布规则；⑱§9.2 版本号改语义版本一句、删采集字段清单行；⑲一览表岗位节必填 4 行/选填 3 行、"发布阻断校验（4 项）"。**Q375 申报未落地**（§3.1 无悬停提示句，与 0907 逐字相同）。

**方案**：逐条对照原型最终覆写态（人格页 L1853→L2093→L2327→L4243→L4328；校验 L2129 `validate()`；发布弹窗 L2132 `openPub()`；新建 L2319/L2321；页签 L2122+L4351；工作档案 L2281 wp3 层→L2709→L2786→L4012；Agent L2678 表格→L4043 抽屉；自动化任务 L2679 静态 `taskPane`→L2930→L3373→L3586；知识页签 L1871→L4018），结论三档：

- **回齐原型（销项）**：Q1、Q2、Q21、Q347、Q380（岗位半边）、Q381、Q382、Q386、Q387 共 9 条完全销项；Q4（页签显隐半边）、Q30（字段半边）、Q383（拖拽/"本岗位已引用"/技能行字段半边）、Q7/Q348（md 内部矛盾半边）部分销项改写。
- **新引入 md↔原型差异**：⑦岗位描述 2000 vs 原型人格页 `maxlength="500"`（L2093）vs 一览表 L3/L14/L361 仍 500——**0907 已销项的 Q12 复发**（新立 Q445）；⑩工作档案：新增走"右侧弹出表单"vs 原型居中弹窗"新建工作档案"（L2297）、取消提示"已取消为保存修改"（错字）vs "已取消未保存修改"（L2694）、字段类型 6 项 vs 原型 7 项（多"标签（枚举）"）、唯一 ID"复选框"vs ✓/✕ 图标、抽取方式"单选"vs 只读+复选、编目/规则上限 8 被删（Q25 改写）；⑭Agent 名称 64/职责 2000 vs 原型 60/300 且职责必填（Q25）；⑰自动化任务：原型左栏任务列表/【＋ 新增样例任务】/【保存】toast、字段必填与上限、引导语与平台技能说明文案 md 未写（新立 Q450）；⑱语义版本 vs 原型详情页弹窗仍手填 v001–v999（Q7/Q348 改写为单侧差异）。
- **新版内部矛盾/格式缺陷**：§5.1 L305 空态仍写"点击'新建知识库'添加"、§11 L450 仍"查看 / 编辑 / 检索测试"、L452 仍"【← 返回】"（新立 Q449，并与知识库 md 跨文档不一致）；§4 标题层级错乱（`####  左侧` / `### 1./2./3.` 与 `### 4.` 同级、4.1–4.3 消失、4.4 悬空）、§7 无空行与非列表行、§9.2 缩进退级、§5 跳号、弯引号与残留反引号（新立 Q452）；§9.2 清单仍缺"岗位 SOP"项（Q3 改写）。原型独有细节补录：领用页文案表单占位/【取消】【保存】/满 6 条按钮隐藏（Q446）、新建弹窗标题/按钮/空值 toast（Q447）、图标区块必填星号（Q448）、知识页签工具栏与检索测试无状态限制（Q453）、自动化任务按钮无处理器/预览静态（Q451）。

**影响面**（代码按 0907 md 落地，以下均需跟改）：`views/admin/PositionDetailTabs.vue:703-709` 删图标/认领说明阻断（→4 项），`components/position/PublishCheckDialog.vue:5` 去"采集完整"；`PositionDetailTabs.vue:17-18/:61/:173/:188/:318/:859` + `utils/positionModel.js:157-158` 「岗位认领说明」→「领用页文案」改可选、文案照 §2.3；`AdminPositions.vue:167` toast 改"岗位已创建，请完善岗位配置"；`PositionDetailTabs.vue:1071/:1082/:1090/:1103/:603` 知识页签删新建/编辑入口、"类型"列→"描述"+"文档数量"；`:1169/:1172` Agent 名称/职责 maxlength 40/500→64/2000；`components/position/AgentLane.vue:57` 确认键文案、`:91-130` 拖拽按 md 移除；`SkillCard.vue:5` 补分类与工具数量；工作档案 `PositionDataTableStage.vue:565-651`、`dossier/DossierRuleListEditor.vue:11/:50/:107`、`utils/dossierConfig.js` 核字段类型/唯一 ID/新增流程/取消保存文案（上限 8 去留待裁决）；自动化任务 `PositionSampleTaskStage.vue`、`SampleTaskEditor.vue:133-155/:275-310` 结构已对齐、核新文案；岗位描述上限 `utils/positionModel.js:155` `DESCRIPTION_MAX_LEN=500` **待第 5 项裁决**；`AdminPositions.vue:564` 删除按钮 title 维持（Q375 未落地）。页签恒显 `PositionDetailTabs.vue:1198` 已符合；语义版本 `VersionDrawer.vue` 已符合。

## 二、岗位管理：三处措辞收口（防抖改产品语言、§4.3.3 与 §五"审核中"界定）

**需求/问题**：Q380 §3.1 "220ms 防抖"→"输入停顿后自动刷新"；Q386 §4.3.3 "任意已发布岗位"→"已发布及审核中的岗位"；Q387 §五 补"审核中指已发布岗位在审新版或停用审核…未发布且首发审核中的岗位不进入下拉选项"。

**方案**：与原型 L1645/L1609/L1599 意图一致，三条完全销项（Q380 与岗位 md §1.3 MutationObserver 段删除一并消）。残留：§五 L134 末两句语义重叠且"已停用"非岗位三态（新立 Q454）；原型 L1598 `window.positionRows` 未赋值恒走 fallback（Q385 承接）；悬空图引用 Q35 承接。

**影响面**：`AdminPositionAssignments.vue:114` 防抖 300 无需改；`:96` `listPositions({status:'published'})` + `api/positionMock.js:242-243` 把 `pendingAction` 非空归 reviewing → 已发布且在审岗位被排除，需改为"底层 status=published 不看 pendingAction"（与 0907 第 2 项 g 建议一致）。

## 三、知识库 + API：11 处回齐原型、1 处改错、一览表 §9 两处同步（申报 12+1+2 条）

**需求/问题**：知识库 md 申报 12 条（Q55/Q56/Q100/Q66/Q86/Q91/Q90/Q93/Q96/Q81/Q80/Q392）、API md Q123、一览表 Q393/Q395；另 4 条声明"确认保留 md 不改"（Q68/Q101/Q102/Q127）。diff 逐 hunk 核对：15 处知识库改动全部可归入申报 Q 号，无游离改动。

**方案**：**完全销项 8 条**（Q55 §七.3 删"选择已有 MCP 时"；Q66 删图片理解；Q81 "已引用"；Q86 数据源范围"默认且仅展示"；Q90 岗位页删新建/编辑 = 原型 L4021 `removeUnwantedControls`；Q91 跳转清空筛选 = L1877；Q96 状态改选填 = L1939 无星标；Q392 映射残句清零）+ Q123 + Q393。**部分销项 3 条**：Q56 默认 API KEY 已齐、"无鉴权"vs 原型"不鉴权"残留（降 B）；Q100 术语 Embedding 模型/检索策略/Top-K 已齐、"保存前提示…确认后重新处理"原型无确认动作（降 C）；Q395 一览表补校验句、但必填列仍"选填"vs 同行"query（必填）"。**未落地 1 条**：Q80 文档数仅换措辞，原型仍用静态种子 `docs`、新建一律 0。**改错 1 条**：Q93 把原型缺陷写进 md——§八.1 L417 新增"MCP 保存后概要展示'已连通 · [工具名]'"（= 原型 L2005 未测试即"已连通"），与 §四.2 L202"新建保存后未测试前显示'未验证'"、§七.5 L405"修改…后重置为未验证"、§四.3 L219 互斥（升 F）。**新引入**：§三.8 L169 引言仍"两个入口创建和管理"vs L172 岗位页只能查看/检索测试（Q456）；知识库 md L177"检索测试跳转至知识库模块"vs 原型 `search-pos-kb` 原地弹窗、岗位 md §5.3 亦写原地（Q455）；一览表 §9.1 L262 状态仍 ✅、§9.2 L273 仍"默认无鉴权"未随知识库 md 同步（Q349/Q350 升 F）；顿号/分隔符级微差补录（Q457）。4 条"不改"：md 未加注记、原型未补字段，条目原样保留待负责人定闭环方式（连带一览表镜像 Q334/Q335/Q339/Q340/Q344）。

**影响面**：`utils/knowledgeBaseMeta.js:61-69` `sourcesText` 改全量计数（Q81）；`api/knowledgeBaseMock.js:221` `docCount` 是否保留 ENABLED 过滤待定（Q80）；`components/admin/KnowledgeSearchDialog.vue:5/:29/:128-133` 删额外 option（Q86）；`views/admin/PositionDetailTabs.vue:565-568/:1070-1072/:1082/:1103/:599-610` + `KnowledgeBaseList.vue:12-13/:124-128/:137-139` 删岗位页新建/编辑入口与自动切筛选（Q90/Q91）；`KnowledgeSourceEditor.vue:589/:597/:603/:474-482/:8`、`KnowledgeSourceDocsDrawer.vue:8`、`knowledgeBaseMeta.js:6` 术语改 Embedding 模型/检索策略/Top-K（Q100）；`knowledgeBaseMeta.js:202-205` `authType` 默认 `'API_KEY'`（Q56）；`KnowledgeSourceList.vue:63-69`/`knowledgeBaseMock.js:477/:526-528` 概要值维持"未验证"口径待第 9 项裁决（Q93）。已符合：Q96、Q66、Q123。

## 四、专家 / 技能 / 模型 / 字段字典：专家两处未申报回齐、技能分类改向原型死层、模型申报不实、字段字典预占第 6 项

**需求/问题**：专家 md 申报 Q201（图片语法 + 补引发布确认图）；**未申报**：§二.1 "页面不分页"→"列表支持分页，每页条数按窗口高度动态计算"、§三.2 背景色补"仅在新建/编辑态展示，查看态不展示"。技能 md 申报 Q220：技能分类 8 项→3 项（办公效率、数据分析、内容创作）、分类筛选"仅市场技能时展示"，5 处同改。模型 md 申报 Q226"提供商枚举修正为 8 项、删讯飞/自定义"——**文件逐字节未改**（L233-234 仍"讯飞 MaaS / 自定义"，L245 本就是 8 项）。字段字典申报 Q434（风险等级 5 档）、Q435（风险类型顺序）、Q436（"唯一"→"统一"配置来源、删"本次不修改用户技能审核 PRD"句、新句"在用户技能审核模块 PRD 中独立定义，字段字典保持同步展示"）。

**方案**：专家两处均与原型一致（L1061 `pageSlice`+L1546 `dynPageSize`；`openExpertViewer` 五层无背景色）——**Q193、Q187 销项**、Q196 改写；副作用：技能 md L38、模型 md L39 仍"页面不分页"而原型三模块同构分页（新立 Q461）；原型 `paintExpertList` 列表态着色为 md 未写（Q459）；补引的 `专家-发布确认.png` 是旧流程弹窗图，与 §3.3 侧栏流程和原型均不符（Q458）。**技能 Q220 改错**：3 项 + "仅市场技能时展示"只存在于被覆盖的基础层 L216/L344；生效层 L662 `renderSkills` 无条件渲染分类筛选且取 L655 8 项，编辑页 L233、新建弹窗 L1408→L1394、字段字典种子 L1543、种子重映射 L659 全是 8 项——md↔原型由"种子越界"升级为"枚举整体不符 + 显隐规则不符"，且与字段字典 md L34、一览表 L58 的 8 项形成**跨文档三文两说**（新立 Q460）。**模型 Q226 未落地**，原样承接（真正差异在原型 L245 presets 写入的 provider 不在下拉 8 项内 + 种子 `provider:'自定义'`，改 md 不消）。**字段字典**：Q434/Q435 跨文档半边消除（集合/顺序与审核 md 一致，仅等级排列降序 vs 升序）、但新与原型字段字典种子 L1543（4 档含"建议修改"、旧顺序）不符（F→A 改写）；Q436 过期句已删但 L5/L56/L58 + §三 编辑能力（字典是来源）vs L61（审核 PRD 独立定义、字典仅镜像）仍互斥——且 **L61 单方面回答了《改动记录-20260907》待确认第 6 项**（负责人尚未拍板）。

**影响面**：专家分页 `AdminExperts.vue:65-66/:423-428` + `composables/useAdminList.js:39-51` 需新增按视口高度算每页条数（原型 `floor((innerHeight−330)/62)`、5–30），与拍板④用户技能审核同一能力，宜在 `useAdminList` 统一提供；背景色 `ExpertEditor.vue:225` 已符合。技能分类**暂不动，等第 4 项裁决**——若取 3 项涉及 `api/fieldDictMock.js:22`（bump version）、`AdminSkillsUnified.vue:494-535`、`SkillCreateDialog.vue:84-92/:285/:323`、`AdminSkillEditPage.vue:327-332/:1172`、种子 `unifiedSkillMock.js:156`/`mockSeedOverrides.json:272`/`domainExpertMock.js:64-65`、测试 `fieldDictMock.test.js:13`/`adminSkillsUnified.test.js:198`。字段字典等第 6 项裁决——若按 md：`fieldDictMock.js:24-25`（bump version）、`skillReviewMock.js:64` "建议修改"、`SkillFocusEditor.vue:187-191` 补"严重风险"色。模型零改动。

## 五、审核中心 / 我的申请 / 用户反馈 / 访问审计：业务类型加"其他"、我的申请按钮集改错、两处文案回齐

**需求/问题**：审核中心 Q263（§3.1 业务类型统一裸名）、Q276（§二/§3.1 加"其他"）；我的申请 Q276（加"其他"）、Q275/Q274（§4.3/§4.4 底部改【关闭】【提交审核】、§3.1 操作列补"（等同于前往修改…）"、§五 状态表改"前往修改"）；用户反馈 Q286（"用户名"→"用户"）；访问审计 Q293（"均使用蓝色标签"）。

**方案**：Q263、Q274、Q286、Q293 完全销项。**Q276 审核中心侧改错**：原型审核中心下拉 L1565 `typeOptions` 固定 7 项、`typeLabel` 无 OTHER、种子无 OTHER——"其他"是 md 单侧新引入（新立 Q462）；我的申请侧列内已一致（`myBusinessLabel` 有 OTHER、种子 id 508）、下拉 L1562 仍 7 项（Q276 改写降 C）。Q263 副作用：md 技能改裸"技能"后，原型 L1555 `typeLabel` 仍输出"技能·平台创建/用户上传"，差异方向反转（Q262 改写）。**Q275 改错**：原型查看态 L1753 `myAppViewFooterHtml` 对已驳回/已撤回是**三按钮**「关闭」「前往修改」「重新提交」（与 0907 md 一致），编辑态 L1754 才是「关闭」「提交审核」；0908 md 用编辑态按钮集覆盖了查看态，并把"打开编辑页"挂到【提交审核】（原型该键不开编辑页），§五还删掉了"重新提交"直提路径——同一 md 出现【重新提交】/【提交审核】/【前往修改】三种名字指向同一链路（新立 Q463）。

**影响面**：审核中心/我的申请下拉加"其他"：`utils/reviewMeta.js:72-80/:82-88/:129-137`、`UnifiedReview.vue:185-187`、`MyApplications.vue:205-207`（种子 `myApplicationsMock.js:35` 已有 OTHER；`reviewsMock.js:30-37` 无）；审核中心技能列去后缀 `reviewMeta.js:93-101`（或 md 回改，待选边）；我的申请按钮集 `MyApplications.vue:91-105`、`AdminSkillEditPage.vue:942-953/:999` 现为原型三按钮，**等第 7 项裁决**。用户反馈 `AdminFeedback.vue:262`、访问审计 `AdminLoginLogs.vue:93-95` 已符合。

## 六、用户技能审核：申报 3 条、实际改 16 处——E 类 9 条原型细节全部补进 md，但推翻拍板③④、拍板② 未落实

**需求/问题**：申报 Q427（等级命名统一"X风险"）、Q428（高风险红/严重风险深红）、Q430（新增 §八 异常与空状态）。**未申报 13 处**：§一 L8 页面说明改为原型文案"审核用户上传的自定义技能，处理高风险检测记录，并设置风险尺度。"；§三 筛选补"选择后即时刷新列表"；§四 **删"列表根据页面高度动态分页"句**；§五 补抽屉标题"查看技能"/【关闭】/待审批追加【通过】【驳回】；§5.1 提交人/提交时间措辞改（删"【提交管理后台审核】"）；§5.2 补"（暂无内容）"；§6.1 补弹窗标题"通过审核"/确认键"确认通过"/toast"审核已通过"；§6.2 补说明文/占位"请输入驳回原因（必填）"/标红聚焦/"确认驳回"/红色样式/toast"审核已驳回"；§七 补抽屉说明文；§7.2 默认"通用"Tab、"敏感信息（敏感信息明文凭证）"→"敏感信息"、表格末尾多一空行；【取消】改"变更即写入，取消不回滚"。审核状态仍全篇"待审批"，§五新增 1 处。

**方案**：13 处未申报改动全部为 md 回齐原型 `user-skill-audit` 层（L4413–4560），逐字核对一致——**一举销项 18 条**（Q399/Q400/Q401/Q403/Q404/Q406/Q418–Q423/Q425/Q426/Q427/Q428/Q430/Q431）。但：**L8 页面说明改成原型文案 → 推翻负责人 09-08 拍板③"取 md 原文案"**；**L74 删动态分页句 → 推翻拍板④"取 md 动态分页"**，且本模块成治理域唯一无分页规则页（审核中心/我的申请/用户反馈/访问审计均动态分页，专家 md 本版也改成动态分页）；**"待审批"未改 → 拍板②"全站统一待审核、转浦月回改"未落实**（Q429 改写，5 处）。副作用：§6.2 L131 新收原型说明"需修改后重新提交"vs §6.3 L140"由用户端基于新版本重新提交"两说（Q407 升 F）；L179【取消】"变更即写入"vs L180【保存设置】"保存当前 Tab 的配置"并置（Q466）；§八"加载失败→【重试】"原型无（Q465）；§7.2 空表格行（Q464）；Q424 "抽屉顶部说明文"位置不符且 `SCALE_DESC` 逐尺度描述仍缺。一览表 Q444 §十 已补且与 md 一致（微瑕 Q467："可选等级见 §7.2"无所指、"500 字"vs"500 字符"）。

**影响面**（代码仍为旧形态，整页待改造，等拍板第 7 项①与第 11 项）：`views/admin/UserSkillReviews.vue:99-100` 说明文案（第三套）、`:107-115` 筛选缺尺度与【查询】、`:39/:108-110` "审核中/已拒绝"三套状态词、`:55/:168` 分页 20 条 vs 拍板④/L74、`:126` 空态文案、`:178-204` 单一弹窗需拆通过/驳回两弹窗（500 字必填标红）；`ReviewSkillDetailPage.vue:168-189`；五档等级 `api/skillReviewMock.js:48-92`、`SkillFocusEditor.vue:187-192`、`fieldDictMock.js:25`；风险设置抽屉**代码无实现**需整体新建。

## 七、用户 md 15 条 + 角色 1 条：全部"md 回齐原型"，逐字核对通过，代码文案要整体跟改

**需求/问题**：Q302 "全部角色"→"全部用户分类"；Q303 搜索占位"搜索用户名、显示名或邮箱"；Q305 补查询按钮；Q307 "角色"→"初始角色"；Q308 用户名占位"3–32 个字符"；Q309 合并校验提示"请输入 3–32 个字符"；Q310 "邮箱（选填）"/占位 `name@example.com`/删"一键清除"；Q311 "请输入有效邮箱"；Q312 密码提示尾字；Q313 新建态确认键【新建】；Q314 "用户已新建"/"用户信息已保存"；Q316 设置角色弹窗文案；Q319 删除确认文案；Q320 "用户已删除"；Q438 "＋ 新建用户"。角色 Q333 图片语法。

**方案**：13 条完全销项（含 U+2013 破折号、全角 ＋/（）逐字节核对与原型 L238/L250/L251/L252/L261 一致）；Q305 部分（md 未写"回第 1 页 + 筛选条件同样生效"，L720）、Q438 部分（L23/L30/L123/L135 四处仍无"＋"前缀）。新引入：§三.4 L166 编辑态仍"邮箱…可修改或清除"、一览表 L245 同（Q470）；§三.1 L137 "均展示【保存】"与后半"新建态为【新建】"自相抵触（Q471）；漏审补录：md L105 删除确认键"危险操作样式"原型无（Q468）、原型新建态默认勾选"普通用户"md 未写（Q469）。Q333 全包 `![[` 清零，销项。全包 `grep '!\[\['` 仅剩清单自述两处。

**影响面**（全部为 md 回齐原型 → 代码文案改）：`views/admin/AdminUsers.vue:183`（搜索占位）、`:191`（"全部用户分类"）、`:180-217` 新增【查询】按钮（照 `AdminRoles.vue:7-8/:40` 模式）、`:120`（删除确认文案）、`:126`（"用户已删除"）；`components/admin/UserEditor.vue:128`（占位）、`:68-69`（合并规则）、`:64`（"请输入有效邮箱"）、`:133-134`（label/占位，`clearable` 去留待 Q470）、`:139`（"初始角色"）、`:155`（密码提示）、`:186`（`isEdit?'保存':'新建'`）、`:111`（两套 toast）；`UserRoleDialog.vue:69`（文案 + 需取 `user.username`）；`api/adminUserMock.js:168`（错误文案同步）。测试无文案断言，不破。角色零改动。

## 八、AI 生成按钮 Prompt 规范 §7 + 一览表 §八点五/§十/岗位节/§9

**需求/问题**：AI 规范补 §7「业务系统编辑器—示例问题」（system/user/变量来源表）、汇总表第 7 行去空格（Q443）。一览表：岗位节必填 4/选填 3、"4 项"（Q347）；新增 `## 八点五、用户`（Q359）；§9.2 补校验句（Q395）、§9.3 删 MCP 映射两行（Q393）；新增 `## 十、用户技能审核`（Q444）；字数规则例外补"驳回原因 500 字符"。

**方案**：Q443、Q359、Q444、Q393、Q347（项数半边）销项。§7 是规范补录，原型 L4403 biz 分支存在、业务系统示例问题区 L845 注入未被移除；但 §7 带入既有差异：变量 name+description vs 原型只取 description（Q366 改写）、20 字约束 vs 原型 60 截断（Q371 改写）、可触达场景 5/7（Q362 改写）。一览表新引入：章节号"八点五"、L3 导语例外仍 2 项 vs L361 3 项、§十 前后缺 `---`（Q472）；§十"可选等级见 §7.2"无所指（Q467）；**岗位描述 500 / SOP 4000 三处未随岗位 md 改 2000 同步**（并入 Q445）；§9.1 状态 ✅、§9.2 默认无鉴权未随知识库 md 同步（Q349/Q350）；一览表全文无"背景色"行（随 Q459 补录）。

**影响面**：AI 规范 §7 零代码改动（`BizSystemEditor.vue:312-330` 已与原型同构）；一览表为文档间同步，无代码影响。

## 九、包结构与卫生：`04运行/` 第四次消失、回流副本、编辑器配置

**需求/问题**：见结构级差异表 ST-1/2/3/5/6/7/11。

**方案**：`04运行/` 第四次被移除（Q332 改写为"推翻拍板①"）；`01总览/` 空目录复发；`.obsidian/` ×2 新带入；`.bak`/`.DS_Store` 沿袭；包根多一层目录并塞回我方审查结果副本（PM 未批注）与清单 `.txt` 副本。既然后续团队自主处理，建议版本切换时由我们直接整理：扁平化去掉 `数字员工管理端PRD/` 一层、移植 `04运行/*/.gitkeep` 与 `01总览/.gitkeep`、删副本与卫生文件。

**影响面**：文档层，零代码。

---

## 待确认清单（需负责人拍板）

> 批注方式：直接在本清单条目下写决策；销项后标题加【已解决】。编号固定（第 1–14 项），不随编辑器自动重排。负责人已指示"后续任务团队自己做"，因此本清单**不再有"转浦月"项**——凡 md 修订均由团队执行，只需负责人定方向。

### 第 1 项·【复发项·推翻 09-08 拍板①】ST-2 `04运行/` 第四次被移除

09-04、09-06、09-08 三次裁决"保留占位"。建议：**维持保留**，版本切换时由团队向 0908 包移植 `04运行/实例与会话|运行规格|配额与限流/.gitkeep`（不再依赖回流包保留）。请确认。

### 第 2 项·【推翻 09-08 拍板③④·二选一】用户技能审核 页面说明文案与分页句被改成原型口径

- ③ 页面说明：拍板取 md 原文案"审核用户上传的技能，查看用户端安全检测结果，处理需要人工核查的技能。"，0908 md L8 已改为原型文案"审核用户上传的自定义技能，处理高风险检测记录，并设置风险尺度。"。两句无功能差异，请二选一：**a. 维持拍板**（团队把 L8 改回，Q399 复活）/ **b. 采纳 0908 md**（= 原型 = 代码待改文案）。
- ④ 分页：拍板取 md"根据页面高度动态分页"（已给公式），0908 md L74 已删该句（= 原型无分页）。建议 **a. 维持拍板**：把句子加回 L74（与治理域其余 4 页、专家 md 新口径一致，`useAdminList` 统一实现一次即可），Q403 复活。
- 本文件与 02-审查结果按"md↔原型已无差异"暂将 Q399/Q403 销项，若选 a 则两条复活（Q 号不复用，续编）。

### 第 3 项·【拍板② 未落实】"待审批"→"待审核"

拍板②转浦月回改，本版未改（L53/L70/L72/L80/L89 五处）。建议：**团队直接改 md**，Q429 随之销项，代码维持"待审核"。请确认。

### 第 4 项·【新版跨文档矛盾·二选一】技能分类 3 项 vs 8 项（Q220 / Q460）

技能 md 5 处改为 3 项 + "仅市场技能时展示"，但字段字典 md L34、一览表 L58、原型生效态（L662/L233/L1408/L1543/L659）、代码（`fieldDictMock.js:22` 等）全是 8 项且分类筛选恒显。建议：**按 8 项**（回退技能 md 5 处，零代码改动）；若按 3 项则字段字典/一览表同改 + 代码 6 处 + 3 个测试 + 种子含"智能创作/行业专业"的数据清洗。

### 第 5 项·【新版三方矛盾·Q12 复发】岗位描述上限（Q445）

0908 岗位 md §2.1 统一 2000；原型人格页 `maxlength=500`、新建弹窗 2000；一览表三处 500；代码 `DESCRIPTION_MAX_LEN=500`。建议：**恢复 0907 口径"人格页签 500 / 新建弹窗 2000"**（= 原型 = 代码现状 = 一览表），改 md L170 一句。若取统一 2000：改常量 + 一览表 L3/L14/L361 + 测试。

### 第 6 项·【预占未决拍板】字段字典 vs 用户技能审核 等级/类型数据源归属（0907 第 6 项延续，Q436）

0908 字段字典 L61 已单方面写"在用户技能审核模块 PRD 中独立定义，字段字典保持同步展示"，但 L5/L56/L58 与 §三 编辑能力未同步改。请定：**a. 接受"审核 md 独立定义"**（团队改 L5/L56/L58，§三 对风险两字段改只读或删除两行；代码 `fieldDictMock.js:24-25` 改为镜像 5 档并 bump version）/ **b. 字典仍是数据源**（回改 L61，审核 md §7.2 改为读字典）。建议 a。

### 第 7 项·【改错·二选一】我的申请 已驳回/已撤回 详情底部按钮（Q275 / Q463）

0908 md 改为【关闭】【提交审核】两按钮并把"打开编辑页"挂到【提交审核】；原型查看态与代码现状均为三按钮【关闭】【前往修改】【重新提交】（编辑态才是【提交审核】）。建议：**回改 md 到三按钮**（= 原型 = 代码，零代码改动），并统一 §一/§3.1/§五/§七 按钮名（Q463）。

### 第 8 项·【改错】审核中心 / 我的申请 业务类型"其他"（Q462 / Q276）

md 两模块下拉均加"其他"；原型审核中心无、我的申请仅列内有。建议：**以 md 为准**，代码两处下拉加 OTHER 选项（`reviewMeta.js:72-88/:129-137`），审核中心种子补一条 OTHER（bump version）。审核中心技能列"技能·平台创建/用户上传"后缀（Q262）建议随 md 去后缀。

### 第 9 项·【改错】知识库 MCP 保存后概要"已连通 · [工具名]"（Q93）

L417 新句把原型缺陷（未测试即"已连通"）写进 md，与 L202/L219/L405 互斥。建议：**删 L417**，保留"新建保存后未测试前显示'未验证'"口径（= 代码现状）。

### 第 10 项·岗位详情三页签重写后的原型独有细节（Q25 / Q450 / Q451 / Q446 / Q447 / Q453）

工作档案（新增走弹窗、字段类型 7 项、唯一 ID 图标、编目/规则上限 8）、Agent（名称 60/职责 300 必填 vs md 64/2000 选填）、自动化任务（左栏列表、字段必填上限、引导语文案）三处原型细节 md 未收。建议：**按 md 落地**（md 数值 = 一览表统一规则），原型独有细节挂账不追；上限 8 保留代码现状（`DossierRuleListEditor.vue:11`）并补写进 md。

### 第 11 项·申报未落地 3 条与"确认保留不改"4 条的闭环方式

- Q226（模型）：md 未改；真差异在原型 presets（D 类）——建议 md 不动、条目改写为原型缺陷。
- Q375（岗位删除按钮 title）：清单称补写但未写——建议团队补一句进 md §3.1（= 原型 = 代码现状）。
- Q80（文档数）：措辞改、差异未变——挂账。
- Q68/Q101/Q102/Q127"保留 md 不改"：代码按 md 已实现（API/MCP 图标与示例问题、文件格式三档），差异只剩原型缺——建议条目加"不改：产品确认保留 md"注记后标【已解决】，一览表镜像 Q334/Q335/Q339/Q340/Q344 同办。

### 第 12 项·卫生项处置

`.obsidian/` ×2、`.bak`、`.DS_Store`、包根审查结果副本与清单 `.txt` 副本、`01总览/.gitkeep`、岗位管理悬空图引用（Q35）、用户技能审核无截图（Q433）、专家发布确认图（Q458）、CRLF——建议随版本切换由团队一并清理，不再单独请示。

### 第 13 项·版本切换

建议：删除 `docs/prd/PRD-20260907/`（git 可溯）；`PRD-20260908/` 扁平化（内容提到 `PRD-20260908/` 根、删 `数字员工管理端PRD/` 空壳与副本）、移植 `04运行` 与 `01总览` 占位；`docs/prd/` 收敛为单版本。切换前置：第 1 项确认。

### 第 14 项·批量确认「以新版为准」（回齐原型且与代码方向明确的项）

用户 15 条（第七节影响面 4 文件 14 处）、知识库 8 条（第三节）、治理 4 条（Q263/Q274/Q286/Q293，代码已符合 2 条）、专家 2 条（分页需 `useAdminList` 新能力；背景色已符合）、岗位回齐项（发布阻断 4 项、领用页文案改名可选、知识页签删新建/编辑、Agent 删拖拽/【确认删除】/卡片字段/64·2000、新建 toast、岗位管理绑定范围）、用户技能审核 13 处（随第 2 项与 0907 第 7 项①一并进整页改造）。改造启动仍按 0907 第 11 项"暂不处理，等核对完"——请确认是否随本版核对完成而解除。

**可销项汇总（供 02-审查结果 0908 版对照）**：

- 完全销项（61）：岗位系 Q1、Q2、Q21、Q347、Q380、Q381、Q382、Q386、Q387；知识库/API/一览表 Q55、Q66、Q81、Q86、Q90、Q91、Q96、Q392、Q123、Q393；能力域 Q187、Q193、Q201；治理域 Q263、Q274、Q286、Q293、Q399、Q400、Q401、Q403、Q404、Q406、Q418、Q419、Q420、Q421、Q422、Q423、Q425、Q426、Q427、Q428、Q430、Q431、Q444；组织域 Q302、Q303、Q307、Q308、Q309、Q310、Q311、Q312、Q313、Q314、Q316、Q319、Q320、Q333、Q359、Q443。（Q399/Q403 视第 2 项裁决可能复活。）
- 改写（42）：Q3、Q4、Q7、Q11、Q13、Q14、Q18、Q25、Q30、Q348、Q383；Q54、Q56、Q80、Q93、Q97、Q98、Q100、Q349、Q350、Q395；Q188、Q196、Q220、Q434、Q435、Q436；Q262、Q275、Q276、Q279、Q407、Q424、Q429；Q305、Q332、Q355、Q362、Q366、Q371、Q438、Q439。
- 新增（28，Q445–Q472）：岗位 Q445–Q453、岗位管理 Q454、知识库 Q455–Q457、专家 Q458–Q459、字段字典 Q460、技能/模型 Q461、审核中心 Q462、我的申请 Q463、用户技能审核 Q464–Q466、一览表 Q467/Q472、用户 Q468–Q471。
- 未销且申报不实：Q226、Q375、Q80。
