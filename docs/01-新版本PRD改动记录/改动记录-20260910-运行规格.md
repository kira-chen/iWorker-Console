# 运行规格 PRD 改动记录（2026-09-10）

## 1. 批次范围

- 模块：`04运行 / 运行规格`
- 基线：`origin/main@335aff7`
- 分支：`feature/b20260910-02-runtime-specs`
- 本批只提交运行规格页面、运行规格 mock/API、对应测试、PRD 与默认初始化 YAML；不包含“实例与会话”“配额与限流”。

## 2. PRD 变化

1. 将产品项目中已评审的《运行规格产品需求》纳入 GitHub PRD 正本。
2. 运行字段由旧页面的“任务超时、实例并发、临时磁盘”调整为研发当前支持的 9 个底层字段：CPU request/limit、内存 request/limit、临时存储 request/limit、Pod 就绪等待超时、空闲回收、最大存活时长。
3. 补充“个人配置 ＞ 岗位规格 ＞ 平台默认”的规格生效优先级，以及岗位批量配置、管理员个人例外、用户申请审批之间的边界。
4. 补充唯一默认规格、删除限制、配置范围、生效用户统计与默认排序规则。
5. 明确交付边界：本仓页面和浏览器本地 mock 用于产品方案演示与研发参考，不代表后端关系或审批能力已经实现；可直接用于初始化的是同目录 9 字段 YAML。
6. 按现行仓库规则不引入截图和 HTML 原型，PRD Markdown 为需求唯一口径。

## 3. 代码变化

- 重构运行规格列表、编辑/查看抽屉与配置范围窗口。
- mock 数据与接口支持规格增删改查、岗位范围、个人例外、用户申请及生效优先级计算。
- 接入浏览器 `localStorage` 持久化，刷新 Chrome 后保留本地演示数据。
- 默认按最近更新时间倒序，支持切换升序/降序。
- 增加运行规格 mock 行为测试。

## 4. 配置交付

`默认运行规格初始化模板.yaml` 固定字段名和结构，研发可在现场确认数值后写入初始化代码。现场只允许调整字段值，不删除、改名或增加未约定字段。

## 5. 基线推进

本批完成并合并后，以合并后的远端 `main` commit 作为运行规格模块下一次 PRD 修订基线。
