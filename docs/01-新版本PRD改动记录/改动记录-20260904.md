> ⚠ **本文件已归档（2026-09-09）**：该版本导入流程已闭环，仅作历史留痕，**不得作为当前口径依据**。当前基准见 `docs/prd/PRD-20260908/` 与《PRD与原型一致性审查结果-20260908.md》。

# 改动记录-20260904（PRD-20260903 → PRD-20260904）

> 新版 PRD 导入流程阶段一产出（2026-09-06 增量核对；2026-09-07 按新流程从已删除的《PRD-20260903→PRD-20260904-对比分析.md》改写补建，原件见 git 历史 49b1e2c）。**待确认清单已于 2026-09-06 全部裁决完毕**，各条标注见末节。

**总体结论**：非纯增量，属小版本定点修订——冲突点 25 条 + 增量 12 条，集中在四个主题：①「岗位分配→岗位管理」全局改名；②知识库数据源（API/MCP）结构性重写（本版唯一大改造点，md 与原型两侧口径一致）；③多处 PRD 向已实现代码回齐（岗位版本管理回归、API Bearer+多参数鉴权入 md、专家背景色入 md——属利好）；④岗位工作档案规则名改自由输入。未触及：技能/模型/MCP 连接器/治理五模块/用户/角色的 md 及原型对应区。

**结构级差异**：

| # | 项 | 说明 | 裁决（2026-09-06） |
|---|---|---|---|
| ST-1 | `04运行/` 目录再次被移除 | 0903 周期负责人已拍板保留并占位，本版又不含——复发项 | ✅ 保留占位（沿用上版裁决），三子目录 .gitkeep 已移植进 0904 |
| ST-2 | `prd.知识库.md.bak` 再次出现 | 核实为 0903 旧稿备份 | ✅ 冗余已删（git 历史可查） |
| ST-3 | `01总览/` 未带占位文件 | 空目录 | ✅ 已补 .gitkeep |
| ST-4 | 原型文件名未变 | 同名替换（+4K 定点修改） | ✅ 无需处理 |

## 岗位：版本管理回归 md（含新版内部矛盾）

**需求/问题**：0903 版岗位 md 曾整节删除版本管理；0904 把【版本管理】写回操作列并新增 §3.7 版本管理整节（侧栏形态/发布与版本管理双入口/共用校验）。

**方案**：与冻结保留的 demo 实现一致——版本管理确认保留，现状即正确（0903 挂账 Q2 随之销项）。但新版内部矛盾：§3.1 分状态表未同步（仍写"已发布共 3 个按钮"）、§3.3"发布前检查弹窗"旧句未删，与 §3.7 并存两套发布口径。

**影响面**：零代码改动；内部矛盾已记入 02-审查结果（Q7/Q32 等）移交浦月。

## 岗位分配 → 岗位管理：全局改名

**需求/问题**：md 文件更名 prd.岗位管理.md（H1 整行删除、正文术语混用）；侧边栏入口改「岗位管理」；原型导航/navMap/页签/权限 mock 共 5 处同步改名。0903 曾明确"入口名不变"，本版反转。

**方案**：以新版为准。影响已实现代码：AdminRail 菜单 label 现为「岗位分配」，需随动改名+测试。

**影响面**：待改造项（等下周代码合并后启动）；md H1 删除+术语混用属文档缺陷，已移交浦月。

## 知识库：数据源（API/MCP）结构性重写——本版主改造点

**需求/问题**：md 六/七两章与原型数据源表单区统一重写为新口径。

**方案**（新旧对比要点）：API 源——请求方法 GET/POST 扩为 POST/GET/PUT/DELETE/PATCH（默认 POST）；鉴权改 无鉴权/API KEY 多参数表格（四位置）/Bearer，与 prd-API.md 统一；请求/响应映射表格化预设行（query/topK 固定行；content/source/score 预设不可删）、JSONPath 移除。MCP 源——删「引用现有 MCP」仅存直接填写（§七.3 残留引用句属文档缺陷）；传输方式两分支 streamable-http（鉴权 无鉴权/Bearer/API Key+Header 名≤128）/ stdio（Command/Arguments/环境变量表）；超时独立节；检索工具改多选；映射表格化。

**影响面**：KnowledgeSourceEditor（668 行，按 0903 实现）需整体返工——已授权、等下周代码合并后启动。原型侧未完全跟进 md 新口径的部分（约 13 条新差异）已记入 02-审查结果移交浦月。

## API 连接器：鉴权章改写（PRD 回齐实现）

**需求/问题**：prd-API.md 鉴权章重写：新增 Bearer Token 模式、API KEY 改多参数表格（参数名/描述/客户端填写/位置/参数值）、保存校验加强；一览表 API 节同步。

**方案**：与 2026-09-01 已实现代码口径一致，属 PRD 回齐（0903 挂账 Q1 鉴权半边销项）；但原型 API 编辑器未跟进新表格口径（5 条新差异，02-审查结果 Q56-Q65 段）。

**影响面**：demo 零改动；原型侧差异移交浦月。

## 专家：背景色条款新增（PRD 回齐实现）

**需求/问题**：专家 md 新增背景色条款（固定 7 色值清单）。

**方案**：与已实现 7 色板一致（0903 挂账 Q24 销项）；小缺口：md 未标必填与默认值、一览表专家节未同步、原型查看态不展示背景色——已记入 02-审查结果。

**影响面**：demo 零改动。

## 岗位工作档案：规则名改自由输入

**需求/问题**：原型档案详情规则名由受控下拉（限编目字段）改自由输入 input（syncRuleNameOptions 成死代码）。

**方案**：以新版为准，属小改造。

**影响面**：岗位详情页工作档案区（等下周代码合并后与其他改造同批）。

## 待确认清单（2026-09-06 已全部裁决）

1. 【已解决】批量确认「以新版为准」：文档措辞收敛与 PRD 回齐实现类（MD-C3/C4、LB-C1～C5/C9/C13 等）——✅ 负责人按既往口径批量确认。
2. 【已解决】改造启动授权：知识库数据源重构 + 岗位管理改名 + 工作档案规则名——✅ **等下周代码合并后启动**，与 Q27/Q28/Q29 同批。
3. 【已解决】复发项 ST-1（04运行）——✅ 保留占位，沿用上版裁决。
4. 【已解决】挂账联动：Q2 随本版销项（版本管理有 PRD 依据）；Q1 图标半边维持挂账移交浦月。
5. 【已解决】新版内部缺陷候选（岗位 §3.1/§3.3 残留、岗位管理 H1、知识库 §七.3 残留句、原型 kmcpMissing 死校验等）——✅ 已随全量审查记入 02-审查结果移交浦月。

> 版本切换已于 2026-09-06 完成：PRD-20260903 删除，PRD-20260904 入库为唯一版本。
