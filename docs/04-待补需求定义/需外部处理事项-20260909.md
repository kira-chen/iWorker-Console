# 需外部处理事项清单（2026-09-09）

> 状态：**已闭环（2026-09-17）**——4 项已解决，第 4 项转 `产品经理待办任务/slchen.md` #1 跟踪
> 责任人：陈森亮
> 闭环条件：最后 1 项有结论（做 / 不做）
> 关联：—

> 背景：PRD-20260908 一致性整改（A/B/C/E/F/G 六类共 363 条）已全部处理完毕。整改过程中沉淀出下列**团队自身无法闭环、需要外部输入**的事项，按负责人 2026-09-09 要求单独成册。
>
> 与既有文档的分工：《补定义需求清单-20260907.md》记录"原型和 md 都没定义"的页面级空白；**本清单记录整改决议衍生出的新需求与待补输入**。二者互不重复。
>
> 每项标注：**谁来做**、**卡住了什么**、**不做的后果**。

---

## 一、需产品经理（浦月）补写的内容

### 1. AI 生成按钮的 prompt 正文（Q363–Q366 决议衍生）

- **决议**：AI 生成示例问题 / SOP 时，除描述外还要把**对象名称**一起传给模型（技能名称、岗位名称、专家名称+职责描述+分类、连接器名称）。
- **现状**：《AI生成按钮Prompt规范.md》§1–§7 的**变量定义已经齐全**（name / description / intro / roleDesc / category 等均已列出），差异只在原型实现侧没传。因此 md 侧无需改动，**代码按规范实现即可**。
- **需要浦月做的**：规范里每个场景的 system / user prompt **正文措辞**，是否需要随新增变量重写。例如专家场景现在写的是"根据简介生成"，实际要传四个变量，措辞是否要调整为"结合专家名称、简介、职责描述与分类生成"。
- **卡住什么**：不影响代码落地（变量与调用方式已明确），只影响生成质量。
- **优先级**：低，可与后续 md 版本一并处理。

### 2. 岗位申请「已处理」记录的展示规则（Q45 决议衍生）【已解决】

- **决议**：2026-09-09 负责人补充裁决——按「**所有审批都要展示**」+「**待审核的审批靠前展示**」的策略设计。
- **已落地**：团队据此完成产品定义并写入 `02岗位/岗位管理/prd.岗位管理.md`——§4.1 改为展示全部四种状态（待审核 / 已通过 / 已驳回 / 已重新绑定）、待审核分组置顶且组内按提交时间排序、新增审核状态筛选、区分"无任何申请"与"筛选无结果"两种空态；§4.2 补「审核结果」「处理时间」「处理人」三列，操作列区分待审核与已处理；新增 §4.3.4「已处理记录」小节（不可二次操作、驳回原因在标签上悬停查看、同一用户多次申请各自成行、长期保留不自动清理）；§4.3.3 补记录去向；§五 补追溯规则；§六 空态两行同步。
- **状态**：本项已闭环，不再需要外部输入。

---

## 二、需负责人后续拍板的内容

### 3. D 类 29 条原型缺陷的最终处置【已解决】

- **现状**：2026-09-08 已裁决"**先挂起**"，条目保留在底账 D 类，未做任何处理。
- **性质**：这 29 条都是原型 html 自身的 bug、死代码、状态机缺陷（例如 MCP 检索节因中文弯引号导致整节交互与样式失效、`window.positionRows` 全文未赋值导致下拉恒为硬编码值、自动化任务多个按钮无事件处理器等）。
- **需要决定的**：既然口径已定"原型逐步退场、不再修改"，这 29 条是否可以**整类关闭**（标注"原型缺陷、随原型退役一并作废"），还是需要在原型退役前逐条确认无产品含义。
- **卡住什么**：不卡代码（这些是原型侧问题，代码按 md 实现即可），只影响底账能否收敛为 0。
- **建议**：整类关闭，理由是原型已确定不再维护，逐条复核投入产出低。
- ✅ **2026-09-09 负责人裁决：D 类整类关闭**。29 条已全部在底账标【已解决】并附裁决注，注明「代码阶段不得照原型实现，一律以 md 为准」。至此底账 392 条零未解决。

### 4. 管理端登录方案（Q183 决议提及）

- **决议原文**："管理端登录后续会切换成其他方案，这个问题暂不考虑。"
- **现状**：业务系统 md 原有一句"登录凭证按敏感信息管理"，但整份 md 与一览表都没定义登录凭证是什么字段。本轮已按决议改为"登录态由用户在客户端首次登录后托管，管理端不录入登录凭证"。
- **需要后续明确的**：新的登录方案定了之后，业务系统连接器是否需要重新引入凭证配置（若需要，md §三.2 与一览表 §七 都要补字段定义、必填性、校验与脱敏规则）。
- **优先级**：低，待新方案确定后回看。

---

## 三、需与并行开发线协调的内容

### 5. 代码侧待办的承接（本轮整改的全部实现工作）

- **现状**：本轮整改的口径是"**原型不改、md 为准、代码按 md 实现**"。363 条已解决条目中，绝大多数的落点是**代码**，而非文档。
- **规模**：粗略分类——
  - 列表页统一分页策略（技能、模型、角色、知识库、专家等）；
  - 各模块普遍缺失的加载中 / 加载失败 + 重试 / 空态三态；
  - 图标配置统一规则（图标库 / 上传 / 裁剪 / 5MB 校验）在多个模块是占位实现；
  - 各类保存校验、连接测试失败分支（原型里恒定成功）；
  - 岗位发布阻断由 4 项扩为 6 项、Agent 技能上限放宽到 100、Agent 与技能页签改二级表格；
  - 审核快照（仅岗位 / 专家 / 技能）、知识库纳入发布审核、审核中不可删除。
- **需要协调的**：另一条会话线正在做"原型复刻批次"，已提交一批、工作区还有数十个前端文件未提交，与上述范围**高度重叠**。
- **建议**：由负责人指定先后——是等复刻批次全部提交后再按本轮 md 统一改造，还是划分模块并行。**在此之前本线不动代码。**

---

## 四、状态汇总

| 项 | 责任方 | 是否卡住落地 | 优先级 |
|---|---|---|---|
| 1. AI prompt 正文 | 产品经理浦月 | 否 | 低 |
| 2. 岗位申请已处理记录规则 | ~~产品经理浦月~~ → 负责人已裁决、团队已落地 | 否（已解决） | — |
| 3. D 类 29 条处置 | ~~负责人~~ 已裁决整类关闭 | 否（已解决） | — |
| 4. 管理端登录方案 | 负责人 / 后续方案 | 否 | 低 |
| 5. 代码改造与并行线协调 | 负责人 | **是** | **高** |

> 按 2026-09-08 "本版之后不再回流产品经理"的口径，第一节两项团队只登记、不外发；如需转交浦月，请负责人转达。
