# 提交 `0de8fe9` 的遗留问题清单（请 yuepu 修复后提交）

> 提交：`0de8fe9 新增岗位-业务系统页签，统一所有列表页排序按钮为黑色箭头 ↓/↑`（2026-09-10 16:01）
> 发现方：另一路并行会话在按规范「开工先 pull」时发现。**已隔离验证：把该 commit 单独检出来跑，同样失败，与合并无关。**
> 当前状态：全量测试 **22 条失败**（1804 条中）。其中 **1 类是线上级 bug（页面白屏）**，其余是测试断言与新行为不一致。

---

## 〇、先看这条：两个页面会白屏（P0，必须修）

**问题**：`computed` 未 import 就使用，页面一打开即抛 `ReferenceError: computed is not defined`，整页渲染失败。

| 文件 | 现状 | 应改为 |
|---|---|---|
| `frontend/src/views/admin/AdminPositionAssignments.vue:29` | `import { h, ref, reactive, onMounted, onBeforeUnmount, watch } from 'vue'` | 在其中**加上 `computed`** |
| `frontend/src/views/admin/AdminBizSystems.vue:18` | `import { ref, reactive, onMounted } from 'vue'` | 在其中**加上 `computed`** |

两处都是本次新增的排序箭头用到的：
- `AdminPositionAssignments.vue:154` `const appSortArrow = computed(...)`
- `AdminBizSystems.vue:109` `const sortArrow = computed(...)`

**影响**：岗位管理页、业务系统页**在浏览器里直接白屏**（不只是测试挂）。`adminPositionAssignments.test.js` 那 16 条失败全部由此引起——改完这一行，16 条应一次性恢复。

> 提示：`npm run build` 不会报这个错（构建期不检查运行时变量），只有跑测试或真打开页面才会暴露。建议提交前跑一次 `npm run test`。

---

## 一、已由对方修好的部分（你 pull 下来即可，无需重做）

发现方已修复以下 **7 处测试基础设施问题**，均只动测试文件、不碰你的业务代码，并在每处都写了说明注释。这些是你本次改动的**连带影响**，不是你写错：

1. **`featureFlags` mock 缺字段**（4 个文件）
   你新增了 `FRONT_RUNTIME_ENABLED`，但这 4 个测试的 mock 仍只导出 `EFFECT_TEST_ENABLED`，导致引用它的组件加载即报错：
   `positionDetailTabs` / `positionCompletenessG1` / `positionAgentTab4C` / `adminPositionsOps`。
   → 已在 mock 中补上 `FRONT_RUNTIME_ENABLED: false`。

2. **新页签把真实 router 拖进测试环境**（3 个文件）
   `PositionBusinessSystemTab.vue` → `api/admin` → `api/request` → `src/router/index.js`，而这些测试整模块 mock 了 `vue-router`，缺 `createRouter`/`createWebHistory` 工厂，导致**整个套件加载即崩、一条都跑不了**。
   → 已按站内既有口径（见 `components/admin/__tests__/expertEditor.test.js:44-51`）补最小桩。

---

## 二、需要你判断的断言失败（6 条，改代码还是改断言由你定）

这些失败的共同原因是「**你的新行为**与**旧断言**不一致」。每条都要先判断：是断言过时了该更新，还是行为改错了该回退。**发现方没有替你改，因为改错方向的代价比不改更大。**

### 2.1 岗位详情页签集合（`positionDetailTabs.test.js`，3 条）

```
AssertionError: expected [ '人格','采集字段','工作档案','知识', …(3) ]
                to deeply equal [ '人格','采集字段','工作档案','知识', …(4) ]
AssertionError: expected '' to contain '开发中'
```

- 断言期望 8 个页签（含「运行」「效果测试」），你的改动删掉了这两个、新增了「业务系统」，实际渲染 7 个。
- 另两条是「运行/效果测试仍为开发中占位」的断言，页签没了自然取不到。

**⚠️ 需要特别确认（这条最重要）**：
「运行」「效果测试」两个页签是 **2026-09-09 项目负责人明确指示保持空置**的（见 `docs/PRD-review/2026-09-09.md`：「01总览/04运行 三个占位页与岗位详情『运行』『效果测试』两页签按负责人指示保持空置」）。

而「业务系统」页签在 **prd.岗位.md §1.3 写的是固定 6 页签、不含业务系统**；负责人 2026-09-10 就页签集合拍板过「**维持现状 8 个，页签数量上 html 原型不作准**」（见 `docs/PRD-review/2026-09-10.md`）。

**你这次的改动同时推翻了这两条既有裁决**。建议先与负责人确认再决定：如果确认按你的新方案走，则更新断言；如果不是，则需要回退页签改动。

### 2.2 MCP 列表列结构（`adminMcp.test.js`，1 条）

```
AssertionError: expected [ '服务','传输方式','工具数','引用情况', …(31) ]
                to deeply equal ArrayContaining{…}
```
排序箭头改造后表头文本内容变了（多出箭头字符），断言用 `ArrayContaining` 精确匹配列名因而不符。判断：是否只需在断言里剥掉箭头字符。

### 2.3 用户列表排序（`adminUsers.test.js`，1 条）
`列头排序切换 → 回第 1 页重查（原型 user-sort 置 userPage=1）`——排序交互改造后，「切换排序回到第 1 页」的行为没有保持。判断：是行为该保留（改代码）还是规则变了（改断言）。

### 2.4 角色列表列宽（`adminRoles.test.js`，1 条）
`角色名称 185 / 用户数量 110 左对齐 / 页面权限 min 360 / 时间 165；角色名 .rl-name`——列宽或类名断言与改造后不符。

---

## 三、建议的修复顺序

1. **先补两个 `computed` import**（第〇节）→ 跑 `npm run test`，预期 22 条失败降到约 6 条；
2. **就页签集合问题与负责人确认**（第 2.1 节）→ 确认后再改断言或回退；
3. 逐条处理剩余 3 条（2.2 / 2.3 / 2.4）；
4. 提交前确认 `npm run test` 全绿 + `npm run build` 通过。

## 四、协作提示

- 发现方已修的 7 处在**测试文件**里，与你的业务改动无文件冲突，pull 后直接可用。
- 项目约定（根 `CLAUDE.md`）：**每次 commit 前全量测试 + 构建须绿**；推送前先 `git fetch` 比对、有分叉先 merge 跑绿再推。本次问题正是这一步没走到。
- 若对第〇节以外的任何一条判断有疑问，建议先问负责人，不要凭猜测改断言——断言是既往裁决的载体，改掉等于推翻裁决。
