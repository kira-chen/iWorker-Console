# 浦月（yuepu · GitHub yue0403）的待办

> 用法：任何人发现**需要 浦月 处理**的事项，往「待处理」表追加一行（序号 = 两表现有最大序号 + 1，状态填「未处理」）。
> 处理完后**不要手改这份文件**——在完成那次 commit 的说明里写 `关闭待办 yuepu#序号`（或 `closes yuepu#序号`），
> post-commit 钩子会自动把该行搬到「已处理」表并并入本次提交；CI 会核验搬没搬。
> 问题详述要写清：现状 / 依据（md 章节或 commit）/ 期望结果，让人不用回头找上下文就能动手。
> 追加前先 `git pull`，避免序号撞车；撞了以先合入 main 的为准，后者改号。

## 待处理

| 序号 | 状态 | 发起人 | 问题详述 |
|---|---|---|---|
| 19 | 未处理 | 陈森亮（2026-09-23） | **低优先级技术债三条（不影响演示，随手改即可）。** ①**`domainExpertMock` 有第二份硬编码技能分类，与 `unifiedSkillMock` 对不上**：`domainExpertMock.js:58-62` 里 sk_304「合同风险检查」挂 `category: '办公效率'`、sk_307「竞品信息汇总」挂 `'内容创作'`，而 `unifiedSkillMock.js:136` 的 sk_304 是 `'行业专业'`、`:175` 的 sk_307 是 `'数据分析'`——同一 id 两个模块两个分类，该列表被 `domainExpertMock.js:353-355` 的 `listExpertSkillCandidates` 当搜索字段用。不是 `3318cbb` 引入的（早就如此）且侥幸没残留已删除的旧分类值，但这次改分类没把它纳入范围，下次还会漏；同文件 `:15` 头注还写着「专家分类选项不在本文件重复定义——同源取 fieldDictMock」，自相矛盾。**请裁**：是历史落差照原型保留，还是与技能模块对齐。②**提示词 8000 字上限两侧是裸数字**：`SampleTaskEditor.vue:111` 组件内局部 `const SOP_MAX = 8000` vs `sampleTaskMock.js:359` 裸 `> 8000`，数值当前一致非活 bug，但没进 `positionModel.js:134-144` 的集中定义区；同类踩坑前科见 `positionMock.js:715` 注释（示例问题卡 60 而 UI 放到 300 的活 bug）。③**技能名称输入框无 `maxlength`**：`SkillFocusEditor.vue:754-758` 是裸 input，64 字上限只在保存时 toast 拦（`AdminSkillEditPage.vue:599`、`unifiedSkillMock.js:424`），与同页描述 `:914` `:maxlength="2000"`、示例问题 `:939` `:maxlength="300"` 做法不一致，能输到 200 字才被拦。④**`mockSeedOverrides.json` 残留已删除的旧分类值**：`:272` `"category": "智能创作"`、`:372` `"知识与学习"`，当前无害（`:5` `"v": 1` ≠ 当前 version，`mockPersist.js:117` 直接跳过），但这是代码库里唯一残留的旧分类，一旦有人重导出出厂数据或对齐 v 就会被塞回去——属定时炸弹，清掉为宜。 |
| 18 | 未处理 | 陈森亮（2026-09-23） | **新必填只落 UI 不落 mock（研发照 mock 实现会漏校验）。** 本批新增的必填项里，只有自动化任务提示词是两侧都做的正例（`SampleTaskEditor.vue:444-446` + `sampleTaskMock.js:358` 同口径 throw），其余几处 mock 侧是空的——说明不是统一取舍而是标准不一致：①**技能图标**：UI 在 `AdminSkillEditPage.vue:608-611` 保存函数里拦，而 `unifiedSkillMock.js:440` 只 `if ('icon' in payload) s.icon = payload.icon || ''` 零校验；同一个 `updateSkill` 里 name（`:423`）、description（`:429`）、exampleQuestion（`:436`）**都有 throw，唯独新加的 icon 没有**，且 `createSkill`（`:376`）/`importSkillZip`（`:405`）都以 `icon: ''` 建技能，任何不走该页面的路径都能存进无图标技能。②**岗位三项新必填（图标 / 领用页文案 / 采集字段）mock 侧全无校验**：UI 齐备（`positionModel.js:506-508` 三个 `blocking: true`），但 `positionMock.js:729-730` 只校验「最多 6 条 / 每条 300 字」没有「最少 1 条」、`:756` intakeSchema 直接 map 赋值、`submitPublish` 也不复核——保存不拦是有意的（注释写明），但**发布动作在数据层也不复核**，等于给研发一个「API 层可发布无图标、无领用页文案、无采集字段的岗位」的参考实现。③**岗位 `updatePosition` 与 `createPosition` 图标口径矛盾**：`positionMock.js:721` `p.icon = payload.icon || p.icon`（永远清不掉、该分支也永远拦不到空）vs `:309` `icon: payload.icon || ''`（`61dbd19` 特意去掉 `'♟'` 兜底「让必填不形同虚设」）——同一类兜底没一起改。④**三个连接器「示例问题」红星是纯视觉**：`McpEditor.vue:766` / `ApiEditor.vue:561` / `BizSystemEditor.vue:527` 都画了红星，而 `mcpConnectorMock.js:391-392`、`apiConnectorMock.js:669-671` 只 trim 不 throw，种子默认值就是 `['', '', '']`（`61dbd19` 说明里已列入遗留）。依据：CLAUDE.md 工程大原则第二条「代码示例要让研发读懂」——mock 是参考实现，校验缺口会被照抄。期望：①②③ 补 mock 侧校验并与 UI 同口径；④ 要么补校验要么摘红星，二选一。 |
| 20 | 未处理 | 陈森亮（2026-09-23） | **【致命】四个 mock 的写入时间戳用 UTC，种子是 +08:00，两种格式混在同一个 `localeCompare` 里排序 → 「最近更新」列排反。** 现状：`apiConnectorMock.js:496`、`mcpConnectorMock.js:26`、`adminModelMock.js:27`、`bizSystemMock.js:25` 均为 `const nowIso = () => new Date().toISOString()`（`Z` 结尾的 UTC），而种子写的是 `'2026-08-24T15:40:00+08:00'`；排序方 `mcpConnectorMock.js:363`、`bizSystemMock.js:180`、`adminModelMock.js:254`、`AdminApis.vue:170-177` 都是 `String(a.updatedAt||'').localeCompare(...)` 字典序。**实测证据（2026-09-23 穷举验证）**：同一天内 UTC 串与 +08:00 串的 560 个比较组合中，**164 组（29%）排序结果与真实时序相反**。干净反例：`X='2026-09-23T02:00:00.000Z'`（真实 02:00Z）与 `Y='2026-09-23T09:00:00+08:00'`（真实 01:00Z），X 比 Y **晚 1 小时**，但降序 `localeCompare` 把 Y 排在最前 → **排反**。失效条件是「UTC 串的小时数 < 同日 +08:00 串的小时数」时字典序按字面比小时数、完全无视时区偏移。（注：若两串小时位恰好让字面序与真实序同向则看不出问题，所以是间歇性错序、不是必现，这也是它一直没被发现的原因。）触发：在 MCP / API / 模型 / 业务系统任一列表页编辑保存任意一行，再看「最近更新时间 ↓」，刚改的行会排到更早的行后面。**这不是口径未统一，是一次已完成的迁移漏了四处**：`utils/datetime.js:33` 早有 `nowIsoLocal`，`adminUserMock.js:25`、`skillReviewMock.js:22`（注释写明「2026-09-09 收编：本地 nowIso 复制品改引 utils/datetime 单一真相」）、`sampleTaskMock`、`domainExpertMock` 四个模块都已迁完，唯独这四个漏网。期望：四处改 `import { nowIsoLocal as nowIso } from '@/utils/datetime'`，照搬即可、无需决策。 |
| 21 | 未处理 | 陈森亮（2026-09-23，**已更正**） | **【已撤销原结论，改为：删除一个误导人的死文件】** ~~原报「PositionConnectorTab.vue:340 传错 prop 名致业务系统【查看】变新建」~~——**09-23 Playwright 实走复现失败**：真实页面抽屉标题是「查看业务系统」，字段全有值且置灰只读（系统名称=客户管理系统 CRM、登录地址=https://crm.example.com/login…），底部只有【关闭】，**行为完全正确**。**根因是我审错了文件**：`src/components/position/PositionConnectorTab.vue` **零 importer**（已 grep 全仓确认），线上真正渲染的是 `PositionBusinessSystemTab.vue`（`PositionDetailTabs.vue:77` import、`:601` 使用），后者传的是正确的 `:biz-id`。那个 `:editing-id` 笔误只存在于死文件里，用户永远碰不到。**保留本条的意义变成**：`PositionConnectorTab.vue` 是 `505c0a4` 之后被取代的废弃文件，内容与线上件不同（它确实没有【移除】按钮），**已经实际误导了一轮审查**，建议 `git rm` 删掉（git 历史可取回）。 |
| 22 | 未处理 | 陈森亮（2026-09-23） | **【致命】`route.query.keyword` 未做 String 归一，重复同名参数直接崩页（5 处）。** 现状：vue-router 的 query 值是 `string \| string[]`，同名参数重复即为数组。`AdminPositions.vue:105` `query.keyword = route.query.keyword` 配 `:88` `keyword: query.keyword.trim()`；同形 `AdminModels.vue:218`+`:205`、`AdminMcp.vue:288`+`:120`、`AdminApis.vue:255`、`AdminBizSystems.vue:141`+`:120`。触发：访问 `/admin/positions?keyword=a&keyword=b`（手改地址栏 / 旧书签 / 外部拼链）→ `TypeError: ...trim is not a function`。AdminPositions / AdminModels 在 `params()` 里 trim，**onMounted 首拉即抛 → 整页白屏**且点「重试」永远失败（重试走同一个 `params()`）；其余三页点【查询】崩。入口真实存在：`AdminLoginLogs.vue:226` 的访问审计【查看】就向这批页面派发 `?keyword=`。**对照组证明是漏写**：`AdminVersions.vue:53`、`UnifiedReview.vue:77`、`UserSkillReviews.vue:138` 都写了 `String(...)`。期望：五处补 `String()`，与对照组一致。 |
| 23 | 未处理 | 陈森亮（2026-09-23） | **【严重】删岗位 / 删 Agent 只清自身模块，对六类跨模块引用零级联（同一件事，建议一并修）。** ①**运行规格永久删不掉**：`positionMock.js:327` `deletePosition` 不碰 runtimeSpec，而 `runtimeSpecMock.js:108` 显示用 `positionIds.map(...).filter(Boolean)` 静默丢弃已删岗位、`:252` 删除守卫却按**原始长度** `if (s.positionIds.length) throw err('该规格已配置给 N 个岗位')` → 列表显示「0 个岗位」、点删却报「已配置给 1 个岗位」，编辑器下拉里又选不出已删岗位，**UI 无任何解除路径，规格永久锁死**（种子 `runtimeSpecMock.js:21,23` 就绑着 401/402，删掉即触发）。②`claimedUserCount` 是硬编码种子、全仓无写点（`positionMock.js:48/62/76/95`），而删除护栏 `AdminPositions.vue:471` 读它 → 把用户绑到种子计数为 0 的 404 岗后仍可删除。③承②：`positionAssignmentMock.js:46` 的 `positionName` 变 null 但 `r.positionId` 仍是 401，既不显示岗位名也不走「未绑定」分支，`UserPositionEditDialog` 回填时 el-select 显示裸数字，且已持久化。④删 Agent 后 `samplesByPosition` 的 `execAgentId` 悬空（`positionMock.js:806-818` 不碰），`SampleTaskEditor.vue:796` 显示裸 id（`ag_3`），用户不改该项保存即把悬空引用写回。⑤专家 `positionIds`、⑥连接器 `referencedByPositions`（`apiConnectorMock.js:51-52` 存的是含 positionName 的**冻结副本**）同样不清理 → 专家列表「N个岗位引用」虚高、连接器三页显示已删/已改名岗位的陈旧名。**反向佐证这是遗漏而非 demo 取舍**：同一个文件的 `skillRefVO`（`positionMock.js:601`）对已删技能做了「（技能已删除）」墓碑兜底，做法现成。期望：按上述六处补级联或墓碑，六处算一件事、一条 commit。 |
| 24 | 未处理 | 陈森亮（2026-09-23，**已大幅更正**） | **【原五条里四条撤销，只剩一条待核】** 09-23 Playwright 实走逐条复现，结果与原报出入很大——**原结论同样是读了死文件 `PositionConnectorTab.vue`（零 importer）得出的**，线上件是 `PositionBusinessSystemTab.vue`：①~~没有【移除】按钮，引用只进不出~~ → **撤销**。线上件三个区域都有【移除】+ 确认弹窗「确定移除「X」与该岗位的绑定关系？」+ toast「已移除」（`PositionBusinessSystemTab.vue:108-114/193-199/278`），完全符合 md。只读态（URL 带 `?view=1`，从列表【查看】进入）隐藏【移除】是 `v-if="!isReadonly"` 的**设计如此**；编辑态进入则有【移除】【保存】【发布岗位】【＋新增】。②~~别人一提停用，引用就从岗位列表消失~~ → **撤销**。实走完整链路：提交停用审核（状态→审核中）后该行**仍在**；再到审核中心【通过】（状态→未发布）后该行**依旧在**，名称/描述/登录地址/时间照常。③候选没过滤 type、④登录地址列做成了业务页数量列、⑤新增按钮合并与时间列不可排序——**这三条基于死文件，需在 `PositionBusinessSystemTab.vue` 上重新核实后才能算数，暂不作为结论**。**本条真正剩下的待办**：见 #42（岗位私有 MCP/API 两个区域恒空，这是实走中发现的真问题）。 |
| 25 | 未处理 | 陈森亮（2026-09-23） | **【严重】专家「引用情况」语义反了 + 缺「技能数」列。** 现状：`prd.专家.md:47`「技能数：展示当前引用的市场技能数量」、`:48`「市场专家有引用时展示『N 个应用引用』」——是两个不同的数。而 `AdminExperts.vue:343-411` 列序里**没有「技能数」列**，`:384-386` 市场专家的「引用情况」取的却是 `row.skillCount`，该值在 `domainExpertMock.js:239` = `skillIds.length`（= 它引用了几个技能）。后果：一个引用了 3 个技能、实际 0 个应用引用的市场专家，界面显示「**3个应用引用**」——数字张冠李戴。期望：补「技能数」列取 skillCount；「引用情况」改取应用引用数（mock 需补该字段）。 |
| 26 | 未处理 | 陈森亮（2026-09-23） | **【严重】AI 生成的 500ms 定时器无清理无 stale 守卫 → 跨记录串数据。** 现状：`utils/aiLiveGenerate.js:163-168` `setTimeout(() => { busy.value=false; apply(generate(ctx)); ... }, delayMs)`——无取消句柄、回调内不复判当前对象；`isReadonly` 只在 `run()` 入口判一次。消费点五处：`McpEditor.vue:564`、`ApiEditor.vue:100`、`BizSystemEditor.vue:331`、`ExpertEditor.vue:185`、`SkillFocusEditor.vue:219`。触发：点【AI 生成】后 500ms 内关抽屉、立刻打开另一条记录（列表页编辑器常驻挂载、组件不卸载，所以回调照常执行）→ **把 A 记录生成的内容写进已载入 B 的表单**并弹成功 toast。期望：run 时记录当前对象标识 + 保存 timer 句柄，回调内比对不一致即丢弃；组件卸载 / visible 关闭时 clearTimeout。 |
| 27 | 未处理 | 陈森亮（2026-09-23） | **【严重】`ExpertEditor` 的 watch 只盯 `visible` 不盯 `expertId` → 治理侧会审到错的对象。** 现状：`ExpertEditor.vue:516-529` 是 `watch(() => props.visible, ...)`，而同族三个编辑器 `McpEditor.vue:333`、`ApiEditor.vue:300`、`BizSystemEditor.vue:272` 全是 `watch(() => [props.visible, props.xxxId], ...)`——只有专家这份少盯一个 id。触发：①`AdminExperts.vue:133` 新建保存后就地切 `editingId` 想翻编辑态；②`GovObjectDetail.vue:181-187` 常驻渲染，审核中心**连看两个专家申请**时 visible 不回落。后果：`detail.value` 残留上一个对象 → 时间条、状态标签、`locked` 判定全是上一条的，**审核人对着错的对象点通过/驳回**。期望：改成盯 `[props.visible, props.expertId]`，与同族三处一致。 |
| 28 | 未处理 | 陈森亮（2026-09-23） | **【严重】`RuntimeSpecEditor` 加载失败态可提交，把规格覆盖成默认值。** 现状：`RuntimeSpecEditor.vue:258` `if (props.readonly || saving.value) return`——**漏判 `loadError`**（同文件 `:153 askDiscard` 判了）；而 `DrawerEditor.vue:115` 的错误态只 `v-else` 掉正文，`:135` 底部保存按钮仅受 `submitDisabled` 约束，本组件又没传该 prop。触发：编辑规格 → `getRuntimeSpec` 失败（抽屉显错误态）→ 不点重试直接点【保存】。后果：`form` 仍是 `resetForm()` 的默认值（cpu=2/mem=4/disk=20/positionIds=[]）且校验全过 → **把该规格真实配置整体覆盖成默认值并解绑全部岗位，不可逆**。附带 `:239-240` `JSON.parse(baseline.value || '{}')` 使变更对比全 NaN，确认框显示 `NaN 核` / `NaN Gi`。期望：`save()` 补 `loadError` 判定（或给 DrawerEditor 传 `submitDisabled`）。 |
| 29 | 未处理 | 陈森亮（2026-09-23） | **【严重】组织域两处护栏形同虚设。** ①**角色改名后可删掉最后一个系统管理员**：`adminUserMock.js:275-286` `updateRole` 只改 `r.name`，不改 `r.code`、也不回写 `users[].roles`（建角色时两者同值，`:268` `code: name, name`）；而最后管理员护栏 `:205-207` 是 `const ADMIN = '系统管理员'; if (u.roles.includes(ADMIN) && ...)` 按**字面量**比对 → 把「系统管理员」改个名，`u.roles` 里的旧名与常量对不上，护栏直接失效。②**删角色只看示意值 `userCount`**：`:302-312` `if (r.userCount > 0) throw ...` 后直接 `roles.filter`，而种子角色 305「审计观察员」`userCount: 0`（`:58`）、实际 `:65/:69/:75` 三人绑着它（文件头注 `:19` 自陈「不与用户行数强一致」），UI 守卫 `AdminRoles.vue:136` 读同一字段 → 放行删除，三人 `roles` 指向已删角色、筛选下拉里再也筛不出来，后续新建同名角色会把三人「自动」重新绑上。期望：护栏改按 code 判定、`userCount` 实时派生（与 #11 同源，建议一并修）。 |
| 30 | 未处理 | 陈森亮（2026-09-23） | **【严重】`sampleTaskMock` 快照结构改过两次但 version 始终是 5（漏 bump，同 #16 同类）。** 现状：`sampleTaskMock.js:286` `version: 5`；git 实证两次结构变更的 diff 里 `version:` 出现次数均为 0——`e2c40ab` 新增 `execType/execAgentId/execModel` 进 `normalizeUpsert`（`:347-349`）、`a1a1888` 把默认值 `'AGENT'` 改成 `'SKILL'`。后果：09-17~09-20 间用过 demo 并保存过任务的浏览器，版本号没变 → 直接 restore 不回种子，旧默认 `AGENT` 被当成用户显式选择带回，「执行动作」显示与 PRD 相反；`execAgentId` 还可能指向已删 Agent（见 #23④）。期望：version +1 并补说明注释。**另建议**：这已是本月第二次漏 bump（#16 是第一次），可考虑加一条护栏用例或在 mockPersist 头注写死「改种子/改结构必 bump」的检查清单。 |
| 31 | 未处理 | 陈森亮（2026-09-23） | **【中】PRD 一致性五条（字数上限缺失 + 页面文案）。** ①**MCP 服务地址缺 500 上限**：`prd-连接器-MCP.md:269` 与一览表 `:152` 都写「最多 500 个字符」，而 `McpEditor.vue:833-838` 无 `maxlength`、`defValidate.js:231-234` 只校验非空 + URL 正则无长度分支——**UI 与校验两侧都缺**；对照知识库侧的 MCP Endpoint 反而做了（`KnowledgeSourceEditor.vue:778` maxlength=500 + `knowledgeBaseMock.js:543` 校验），两个 MCP 入口口径不一。②**API 地址缺 500 上限**：`prd-API.md:156`、一览表 `:187`，而 `ApiEditor.vue:606-607` 无 maxlength、`:332-334` 与 `apiConnectorMock.js:618-620` 都只有正则。③**API 描述 2000 只落输入框不落保存校验**：`ApiEditor.vue:548` 有 `maxlength`，但 `:327` 只判空、`apiConnectorMock.js:611-623` 也无长度校验；而 MCP（`defValidate.js:209-210`）与业务系统（`:153-155`）都做了 2000 校验——同一规则三个模块只做了两个。④**业务系统登录地址缺 UI maxlength**：`prd-业务系统.md:103` 与一览表 `:226` 写 1024，校验齐全（`defValidate.js:160`）但 `BizSystemEditor.vue:507` 输入框没有 `maxlength`，而同抽屉内业务页 URL `:587`、名称 `:599`、描述 `:611` 都有。⑤**连接器页面说明文案与三份 md 都不符**：MCP/API/业务系统三份 md 同一口径（`prd-连接器-MCP.md:10` 等）要求「平台全部连接器 —— 通用连接器 / 岗位私有 / 市场连接器 统一管理」，而 `AdminConnector.vue:62` 写的是「为平台配置可用的连接器，同时服务于个人用户和 FDE 工程师」；全仓 grep「平台全部连接器」零命中。期望：①②③④按 md 补，⑤按 md 改文案。 |
| 32 | 未处理 | 陈森亮（2026-09-23） | **【中】PRD 自相矛盾 / md 缺口六条（纯 md 侧，请你定）。** ①**专家背景色必填性**：`prd.专家.md:177` 把背景色列进基本信息必填序列（7 色板），但一览表 `:49-59`「二、专家」9 行**没有背景色**、`:62` 校验行也未提；代码按模块 md 实现为必填（`ExpertEditor.vue:389-392`/`:661`）→ 建议一览表补一行，代码不动。②**技能「默认安装」展示条件**：模块 md `prd.技能.md:177`「仅『市场技能』展示开关」vs 一览表 `:76`「仅平台支持时展示，复选框」——措辞与控件类型都不一致（实际是 `el-switch`）。③**岗位 md 节号断裂**：`prd.岗位.md:170` 正文顺序是「名称、图标、描述…」，但小节编号 §2.1 名称 → **§2.2 缺失** → §2.3 描述 → §2.4 图标；代码按正文顺序渲染（`PositionPersonaTab.vue:93/112/130`），是 md 编号异常。④**字段字典「分组说明」无定义**：`prd.字段字典.md:13` 要求分组头展示「分组说明」，但 §2.2 表格 `:32-36` 只给三列、未定义文案，前端只好自造（`FieldManagement.vue:31/38`「平台技能相关的可配置字段」等）→ 请补正式文案。⑤**技能空态少一态**：`prd.技能.md:31/126` 只定义「无查询结果」，未定义「首次暂无数据」；而模型页（`AdminModels.vue:235-237`）按 `hasFilter` 分两态、专家页（`AdminExperts.vue:331`）另有引导文案，三模块口径不齐。⑥**审核中心两处 md 缺**：`prd.审核中心.md:103` 只写「业务快照缺失→阻止审核」，未写列表行是否也拦（代码 `UnifiedReview.vue:134-145` 两处都拦，比 md 更严且更正确）；`:67-71` 规定了驳回弹窗标题/字数/提示，但未规定确认按钮文案与占位（代码为「确认驳回」+「请输入明确的驳回原因」）→ 建议补进 md 使文字稿可独立复现。 |
| 33 | 未处理 | 陈森亮（2026-09-23） | **【中】注释说谎六处（按工程大原则第二条，注释误导比没注释更伤）。** ①`McpEditor.vue:98` 注释写「与业务系统连接器同口径 **60** 字」，而 `const QUESTION_MAX = BIZ_QUESTION_MAX` 实为 **300**（`defValidate.js:119`）；同源残留还有 `defValidate.js:138`、`:193`（「每条 ≤60」）、`:212-214`（引用 md 旧行号「最多 60 字符」）。②`KnowledgeSourceEditor.vue:12` 头注写「名称（≤50 必填）」，实际 `:606` 与 `knowledgeBaseMock.js:488` 都是 64。③`FieldManagement.vue:115` 注释写「删除…不弹确认」，`:116-134` 实际弹了 `ElMessageBox.confirm`（代码正确、注释过期）。④`AdminRoles.vue:49-50` 注释指控「角色 md『不分页』与之冲突」，但 `prd.角色.md:37` 早已改成「支持分页…客户端分页」，且注释指向的 `02-审查结果` 已随 09-17 改版停用。⑤`ClaimNotesEditor.vue:14` 注释写 hint「每条最多 **100** 个字符」，实际渲染 `CLAIM_NOTE_LEN`=300。⑥`PublishCheckDialog.vue:10` 注释写升级类型「修订版本 / 功能更新 / 重大更新」，实际 `POSITION_BUMP_OPTIONS`（`positionModel.js:486`）是「修复更新」；另 `PositionPersonaTab.vue:74/83` 注释引「md §2.4 / §2.5」而 md 实际是 §2.6 / §2.7（与 #32③ 节号断裂同源）。⑦`AdminModels.vue:354-361` 两个 docblock 叠在同一个 `publish` 上，前一个描述「上架即生效」的**旧行为**、后一个描述「提交审核不立即生效」的现行为，自相矛盾 → 删前者。期望：逐条改注释对齐现状，纯文本改动、无功能风险。 |
| 41 | 未处理 | 陈森亮（2026-09-23） | **【说明，非缺陷】连接器 + 知识库这块还有一小片没审到，下轮补。** 09-23 对该范围做了两轮（第一轮单点、第二轮交叉复核），入册结论 #31、#34–#40 都是**读过源码逐条核实**的。但复核者主动声明：`KnowledgeBaseList.vue`、`KnowledgeSourceList.vue` 两个列表页，以及三个连接器列表页的**确认弹窗文案**，本轮**没有完整走过**。另有 4 条线索因未经核实**故意没有入册**：①`AdminApis.vue:540` 的 `empty-text` 疑似死代码（外层 `:534` 的 `v-if` 是否遮蔽未确认）；②API 删除弹窗是否该有「被 N 个技能引用」（按 md 字面代码是对的，见 #37③）；③API md `:41` 的【↻ 重新验证】是按钮文案还是图标描述（md 表述含糊）；④MCP 验证列的「状态圆点」是否由 `HealthTag` 自带（需实走截图确认，属视觉一致视角）。期望：下轮补审这两个列表页 + 确认弹窗文案，并把上述 4 条线索核实掉。 |
| 34 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【高】知识库图标必填有缺口：mock 不查、发布门禁不查，且「查看态发布」完全绕开表单校验。** 现状：`prd.知识库.md:87` 图标必填（2026-09-21 负责人拍板由「否」改「是」）、一览表 L295 同；但 ①`knowledgeBaseMock.js:304-328` 的 `validate()` **无 icon 分支**，`create():332` 是 `icon: payload.icon \|\| ''`；②`utils/knowledgeBaseMeta.js:380-385` 的 `publishBlockReason` ①段**只查 name / description**（本人已开文件确认）；③`KnowledgeBaseEditor.vue:316-322` 查看态 `doPublish` 走的正是 `publishBlockReason(detail)` 而**不调 `formRef.validate()`** → **无图标的知识库既存得下、也发布得了**。**这不属已知 #18**（#18 说的是「必填只落 UI 不落 mock」的连接器面），这里多一层：md §三.6 的发布完整校验是独立条款，同样漏了，且查看态发布是真实可达路径。另 `update():348` 用 `payload.icon ?? r.icon`，`??` 不拦空串，传 `icon:''` 会**静默清空**已有图标。期望：mock `validate()` 补 icon 必填；`publishBlockReason` ①段补 `if (!String(row.icon\|\|'').trim()) return '请选择知识库图标'`；`update` 的 `??` 改 `\|\|` 或显式判空串。 |
| 35 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【中】MCP「连接器类型」漏校验，不选会静默落成「市场连接器」。** 现状：`prd-连接器-MCP.md:243`「连接器类型：必选单选…新建时必须选择」、一览表 §5.1 #5 必填；但 `utils/defValidate.js` 的 `validateMcpForm`（`:204-271`）**全函数无 `form.type` 分支**——本人已 grep 确认：全文件 `form.type` 只在 `:148`（业务系统分支）出现一次，API 则在自己组件 `ApiEditor.vue:325` 校验，**三件套里唯独 MCP 两处都没有**。而 `McpEditor.vue:585` 照样把 `type: form.type` 塞进 payload，`mcpConnectorMock.js:443` 再 `payload.type \|\| 'PLATFORM'` 兜底 → **不选类型 = 静默落成市场连接器**，且 `McpEditor.vue:742` 模板早备好了 `:error="fieldErrors.type"` 红框位、永不点亮。期望：`validateMcpForm` 补 `if (!form.type) errors.type = '请选择连接器类型'`，文案与另两处逐字一致。 |
| 36 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【中】MCP 列表空态判定漏 `applied.type`，按类型筛无结果时显示「还没有 MCP 服务」误导人。** 现状：`AdminMcp.vue:503` 判定写的是 `applied.keyword \|\| applied.state ? '没有符合条件的 MCP 服务' : '还没有 MCP 服务 · 点「新建 MCP」登记第一个'`，而 `applied` 在 `:56` 定义了 **keyword / type / state 三个键**，判定只取了两个。触发：只按「连接器类型」筛选且无命中 → 落进首次空态分支，管理员看到「还没有 MCP 服务 · 点『新建 MCP』登记第一个」，会以为库里一条都没有。依据 `prd-连接器-MCP.md:35`「无查询结果：展示空结果状态，保留当前搜索和筛选条件」。期望：判定补 `applied.type`。 |
| 37 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【中】API 侧两处校验缺口 + 一处与 MCP 体验不齐（可与 #31③ 合并做）。** ①**示例问题 300 上限只落 UI 不落校验**：`ApiEditor.vue:580` 有 `:maxlength="QUESTION_MAX"`，但 `:328-331` 只判「3 条非空」、无 `> 300` 分支；而 `defValidate.js:219-220`（MCP）与 `:196-197`（业务系统）**都有**该分支——API 是三件套里唯一缺的。②API 描述连「必填」都没落 mock：`apiConnectorMock.js:611-623` 无描述校验（与 #31③ 同处，合并处理）。③**删除弹窗无「被 N 个技能引用」提示**：MCP md `:189` 明确要求「该 MCP 被 N 个技能引用」且 `AdminMcp.vue:438-443` 实现了，API md `:76` 正文没写、代码 `AdminApis.vue:426-430` 与 md 逐字一致——**按 md 代码是对的**，但同一页面三个页签体验不齐，**请你裁**是补 API md 还是维持。 |
| 38 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【中】md 侧需修订四条（纯 md，请你裁；交叉复核发现）。** ①**引用清单弹窗标题**：三份 md（MCP `:51`、API `:39`、业务系统 `:33`）都写「弹窗标题**固定为**『被技能引用』」，而代码已于 09-23 按类型分流（岗位私有→「被岗位引用」、市场连接器→「被技能引用」，见 b7ebdc7）。这次分流是**刻意修正**——原先按钮写「N 个岗位引用」、弹窗却标「被技能引用」，三方对不上。按铁律 md 是唯一口径，但此处 md 明显是没跟上的一方，**建议改 md** 为「按引用方类型展示」。②**状态筛选选项顺序两套**：MCP md `:15`「已发布、审核中、未发布」vs API md `:12` 与业务系统 md `:12`「未发布、审核中、已发布」，代码忠实复刻了这个分歧（`AdminMcp.vue:84-88` vs `AdminApis.vue:78-82`）→ 同一页面三个页签下拉顺序不一致。③**搜索框清空是否自动刷新两套口径**：MCP md `:26`「点击【查询】按钮刷新」vs API/业务系统 md `:19`「清空搜索框时列表自动刷新」，代码也各自对齐各自的 md（`AdminApis.vue:457` 有 `@clear="search"`、`AdminMcp.vue:472-478` 没有）→ 三页签交互不一致。④**MCP md L238-245 markdown 结构错位**：图标统一规则的引用块被 L243「连接器类型」条目从中间割断，渲染会断成两个引用块；对照 API md `:114-119`、业务系统 md `:93-98` 同段落是完整的，只有 MCP 这份错位。②③建议统一口径后代码同步。 |
| 39 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【中】检索测试「数据源范围」下拉超出 md，且代码注释引了不存在的依据。** 现状：`prd.知识库.md:162` 写「数据源范围｜下拉选择，**默认且仅展示**『全部已启用数据源』」，而 `KnowledgeSearchDialog.vue:143-147` 实际渲染三组 `el-option`（全部 / 每个启用源 / 置灰的停用源），`runTest():81` 真把 `sourceId` 下发、mock `knowledgeBaseMock.js:451` 也支持按单源过滤——**链路完整、功能合理**，属「代码做了 md 没写」。另：该处代码注释 `:49` 自称依据「md §三.7」，但该节 L152-167 全文无此句——**注释引了不存在的依据**，比没注释更误导。期望：建议**补 md**（可选单源、停用源列出置灰）而非砍功能；同时订正那条注释。 |
| 40 | 未处理 | 陈森亮（2026-09-23，交叉复核新增） | **【低】过期注释批量清理（与 #33 同类，合并做）。** 交叉复核又查出 6 处，其中一处危害明显高于其余：①**`defValidate.js:213` 把 md 原文引错成「§三.3 L242…最多 60 字符」**，md 实为 `:247`、**300**——后人照注释「修」代码会引入真 bug，这条最该先改；②`defValidate.js:138`、`:193` 同样写「每条 ≤60」（实际 300）；③`api/bizSystemMock.js:14`「exampleQuestions 固定 3 条（每条 ≤60…）」；④`BizSystemEditor.vue:329` 注释「点击进『生成中…』约 **420ms**」，而 `utils/aiLiveGenerate.js:36` 是 `AI_LIVE_DELAY_MS = 500`，且 `:35` 明写「2026-09-06 拍板全站统一 500ms，原型 420ms 口径废止」；⑤`AdminMcp.vue:253-255` 注释称「md §二.5『固定 10 条』与之冲突」，但 md `:170` 早已改为「按窗口高度动态计算」，冲突不存在；⑥`AdminBizSystems.vue:5-6` 头注写「5 列…不分页」，实际 7 列且 `:455` 有分页；⑦`ListPagination.vue:6,11` 注释「共 N 条 · 每页 X 条」与 `:106` 实现「共 N 个数据」不符。期望：与 #33 一并清理。 |
| 42 | 未处理 | 陈森亮（2026-09-23，实走发现） | **【高】岗位私有 MCP / API 两个区域永远是空的——store 从没给这两个键赋过值。** 现状（Playwright 实走 + 本人 grep 双重确认）：`PositionBusinessSystemTab.vue:60/68/99` 读 `store.basic?.connectorMcpIds \|\| []`、`connectorApiIds` 同款，而 `stores/position.js` **只初始化了 `businessSystemIds`**（`:109` 从 data 取、`:134` 兜底空数组），另两个键**从未被赋值**、恒为 `undefined` → `\|\| []` 恒空。实走确认：**401–404 全部四个岗位**的「岗位私有 MCP」与「岗位私有 API」两个区域都显示「暂无绑定的私有 MCP / API」，只有业务系统区域有数据。这是旧 #7④「岗位绑 MCP/API 只存内存」**唯一仍然成立的那一半**（另一半「保存后丢失」已因连接器改版消解）。附带：三个 loader 在 setup 里裸调用、没挂 `store.basic` 的 watch，store 异步填充完成时会错过首次加载。期望：`stores/position.js` 补这两个键的初始化与回填，mock 侧 payload 一并透传（md 岗位 §8.1 L498 / §8.2 L516 点名了 `connectorMcpIds` / `connectorApiIds` 两个字段名）。 |
| 43 | 未处理 | 陈森亮（2026-09-23，实走发现） | **【中】`DrawerEditor` 公共件的错误态不禁用保存按钮——全站 17 个编辑器继承同一缺陷。** 现状：`DrawerEditor.vue` 的 loading / error / body 三态 `v-if` 只切换**抽屉主体**，页脚插槽在这个开关**之外**，`:disabled="submitDisabled"` 默认 `false`。实走证据（Playwright 构造加载失败）：抽屉体显示「加载失败 + 重试」，页脚仍渲染可点的【保存】绿色按钮（`disabled: false`），点下去**无 toast、无报错、无任何反馈**——一个「活着但完全失效」的按钮。全站 **17 个**组件用了 `DrawerEditor`（McpEditor / ApiEditor / BizSystemEditor / ExpertEditor / KnowledgeBaseEditor / RoleEditor / VersionEditor…），其中**只有 `VersionDrawer.vue` 自己算了 `submitDisabled`**，其余全部继承。**这使 #28 升级为公共件问题**：#28 说的 RuntimeSpecEditor「错误态可提交把配置覆盖成默认值」只是其中后果最重的一个实例（且 #28 当前从 UI 不可达——`/admin/runtime-specs` 路由无 `:id` 段、specId 只来自列表刚返回的行，手工点不出来，故 #28 单独看优先级低）。期望：在 `DrawerEditor` 里把 error 态并入 `submitDisabled`，一处修全站收口，比逐个组件补判 `loadError` 更省且不漏。 |
| 44 | 未处理 | 陈森亮（2026-09-23，实走发现） | **【中】API 连接器列表「最近更新时间」是分段排序、跨段不排（#20 在 API 页签的真实形态）。** 实走现象：`/admin/connector` → API 页签，表头写着「最近更新时间 ↓」，实际顺序是 `08-24 16:10 / 08-24 14:28 / 08-23 18:40 / 08-22 10:35 / **09-01 09:30** / 08-31 16:05 / 08-31 15:20`——前 4 行（原始种子）与后 3 行（星火系列，后期追加）**各自内部降序，两段之间完全没排**，`09-01` 的行排在 `08-22` 后面。编辑第 6 行「星火知识库问答」保存后时间变当天最新，却只挪到该**分段头部**（index 4），没到 index 0。MCP 与业务系统页签**无此问题**（实走验证：改一行后正确跳到 index 0）。成因方向：`AdminApis.vue` 是全站唯一未接入 `useAdminList` 的列表页（按「服务提供系统分组」双层结构自己手写 `sortApis(apis, psId)` **按组内排序**），跨组不合并排序。**与 #20 的关系**：#20 的「UTC vs +08:00 混用」在数据层确实成立（穷举 164/560 组排反，见该条），但实走没能在 MCP / 业务系统页签复现出可见错序；API 页签这个可见的错序是**分组结构**导致的，两者是不同问题，建议分别处置。 |
| 45 | 未处理 | 陈森亮（2026-09-23，实走发现） | **【中】停用审核通过后状态显示「未发布」，与「已停用」语义不符，且状态变更不刷新时间戳。** 实走现象：业务系统「客户管理系统 CRM」走完整链路——提交停用审核 → 审核中心【通过】→ 连接器列表状态列显示 **「未发布」**，操作列变成【查看】【编辑】【发布】【删除】。用户**无法区分「从未发布过」与「已被停用」**两种完全不同的状态。同时该行「最近更新时间」停留在 `2026-08-24 15:40` 不变——**状态变更没有刷新时间戳**（对比：改描述会刷新）。**请你裁**：①三态（未发布/审核中/已发布）是既定设计，停用后回落「未发布」是否就是预期？若是，建议 md 里写明「停用通过后回未发布态」免得反复提报；②若需区分，则要引入第四态或加标记，属 PRD 变更。另②状态变更刷不刷新 updatedAt 也请一并定。 |
| 15 | 未处理 | 陈森亮（2026-09-23） | **版本管理两份新用例在 Node 26 本机跑红（CI Node 22 绿），11 条失败。** 现状：`frontend/src/utils/__tests__/demoIdentity.test.js`（4 条）与 `frontend/src/api/__tests__/versionMock.test.js`（7 条）在本机 `npm test` 报 `TypeError: Cannot read properties of undefined (reading 'clear')`，位置是 `beforeEach(() => localStorage.clear())`；两文件头注都有 `// @vitest-environment jsdom`，jsdom 29.1.1 也装着。实测探针（jsdom 环境、Node v26.0.0）：`typeof window === 'object'` 但 `window.localStorage`、`globalThis.localStorage` 全是 `undefined`——jsdom 29 把 localStorage 交给 Node 原生实现，而 Node 不带 `--localstorage-file` 时该能力关闭（跑测试时能看到 Node 的 `ExperimentalWarning: localStorage is not available because --localstorage-file was not provided`）。CI 用 Node 22（`.github/workflows/ci.yml:58`）没这问题，所以 `9f4c5fe` 在 CI 是绿的，两份用例本身逻辑没错。依据：本仓既有约定是「jsdom 下 localStorage 不可用，要用就自己装桩」——`positionAssignmentMock.test.js:61-80` 已经写明这点并用 `Object.defineProperty(globalThis, 'localStorage', { value: makeStorage(), … })` 装桩，其余用到落盘的 mock 用例都照这个模式走，这两份新文件漏了。期望：两份用例照 `positionAssignmentMock.test.js` 的写法自建 storage 桩（或抽一个公共 test helper 给全仓复用，更推荐——现在装桩代码在多份用例里重复），使本机 Node 26 与 CI Node 22 都能跑绿；**不要改成删用例或加 skip**。注：本条只是测试基础设施差异，不影响功能与演示。 |




| 7 | 未处理 | 陈森亮（2026-09-18） | **R3 · 状态机漏边 / 数据层无守卫（能力系 + 岗位连接器 + 知识库）。** ①技能 `unifiedSkillMock.js:454-464 / 569-581 / 533-547`：删除 / 停用 / 发布无状态守卫——已发布可删（快照一起没）、草稿可提停用（造出 PUBLISHED+DELIST 假态）、空 SKILL.md 可发布；②MCP / API / 业务系统 `mcpConnectorMock.js:445-464`、`apiConnectorMock.js:685-709`、`bizSystemMock.js:217-234`：审核中可 update、已发布可 delete，只靠 UI 藏按钮；③模型 `adminModelMock.js:329-352`：无审核中拒写，默认模型改连接字段回 DRAFT 但 `isDefault` 未摘（未发布模型带【默认】排最前且可删）；④岗位连接器页签绑 MCP/API 只存内存：`PositionBusinessSystemTab.vue:97-112` 写 `store.basic.connectorMcpIds/ApiIds`，`PositionDetailTabs.vue:225-245` 不上送、mock 无字段，toast「绑定成功」→ 保存 → 刷新即丢（业务系统 `businessSystemIds` 走通了，三者不对称；md 岗位 §8.1 L498 / §8.2 L516 点名了这两个字段）；⑤企业类型知识库无法新建 / 编辑：`KnowledgeBaseEditor.vue:409` `prop="scopeRefId" required` 触发 Element 内置必填，报 `scopeRefId is required`；⑥API_KEY 已配置行只改「位置」或参数名不重填值 → 密钥静默清空：`apiConnectorMock.js:655-661` prev 按 in+name 匹配、`ApiEditor.vue:391-403` 不回传 valueMasked（KB 数据源侧回传了所以正确，照抄即可）；⑦数据源【测试连接】对已存在源立即落库、【取消】不回滚：`knowledgeBaseMock.js:699-703`，改坏地址测试后取消 → 库内旧地址被标「已连通」、发布校验放行；⑧MCP【拉取工具】取消不回滚 `mcpConnectorMock.js:518-522`（注释称沿用旧后端契约，**请你裁**：改成保存才落，还是 md 记为「拉取即落库」）。依据：技能 §二.4 L122 / §二.3.3 L79、MCP §二.4「审核中不允许编辑删除」、模型 §二.4 L192、KB §三.3.1、API §三.3。期望：数据层加守卫（照 `unifiedSkillMock.assertNotReviewing` / `domainExpertMock.deleteExpert` 的写法）；岗位 mock 补 `connectorMcpIds/connectorApiIds` 并在 payload 透传；KB 去掉 `required`；API_KEY prev 按 rowId 匹配或回传 valueMasked；数据源测试结果先放内存、保存时再落。 **【2026-09-23 复核更新，陈森亮】**：①有实质进展但**删除路径漏了**——`unifiedSkillMock.js:227-229` 已新增 `assertNotReviewing()` 并覆盖 `:420/:456/:813/:825/:838` 五个写点，发布/下架另有 `:553`/`:593` 守卫，唯独 `removeSkill`（`:471-481`）只拦引用不看状态、没调 `assertNotReviewing`，同文件其余写点都调了，像遗漏不像有意豁免；「空 SKILL.md 可发布」仍未拦。④**前提已不成立，可销**：连接器架构已迁移（`mcpConnectorMock.js:288-289` v8「岗位私有连接器不再绑定所属岗位，改 referencedByPositions」），岗位侧不再存 mcp 字段，页面 `PositionConnectorTab.vue:13-15` 直接查三个连接器 mock，两侧都落盘。②③⑥⑦⑧ 与 09-18 登记时一致，未被这批 40+ commit 触及。另**新发现两点，随本条一起改**：(a) `adminModelMock.js:341-343` 比「审核中未拒写」更严重——审核中改连接字段会静默 `m.status='DRAFT'; m.pendingAction=null`，等于开了一条绕过审核撤回在审提交的暗道；(b) ⑦⑧ 这类「测试/拉取结果立不立即落库」三处口径不一：`mcpConnectorMock.js:509-518 testMcpConn` 按 PRD「仅当前抽屉内展示」不落库，而同文件 `fetchToolsResult`（⑧）与 `knowledgeBaseMock.js:720-724`（⑦）都立即落库——**请一并定调再改，不要逐个修**。 |
| 9 | 未处理 | 陈森亮（2026-09-18） | **R5 · 岗位系派生值不同源 / 悬空引用。** ①岗位 `claimedUserCount` 是静态种子，分配表增减不回写（`positionMock` 种子 vs `positionAssignmentMock.setUserPosition`）：解绑全部用户后【停用】仍「已被 18 个用户领用」，401/402/403 永远停不了删不了；反之未发布 404 可带着绑定关系被删（删后分配行 `positionId=404、positionName=null`）；②批量绑定不调 `markApplicationAssigned`（`AdminPositionAssignments.vue:169-199`）：待分配标签不消、徽标不减，单个分配路径是对的；③`positionAssignmentMock.js:87-99` `setUserPosition` 不拦未发布 / 停用岗位（md 岗位管理 §六 L89 只在 UI）；④知识库按**岗位名字符串**联查 + 岗位名单硬编码（`PositionKnowledgeTab.vue:55-56`、`knowledgeBaseMock.js:61-64`）：改岗位名（甚至未保存）知识页签立即变空、新岗位永远关联不到知识库；⑤种子 404 的 Agent 引用通用技能 sk_303（`positionMock.js:195`，违反 md §6.4「只能引用岗位私有」），删后显示「（技能已删除）」但 `skillCount` 仍 1、发布门放行（`positionModel.js:483-486`）；⑥删岗位不清 sampleTask / dataTable / runtimeSpec.positionIds，且 `posSeq` 回种子后新建的第一个岗位 id=405 会「继承」上一轮 405 的自动化任务 / 档案（`positionMock.js:321-337,34`）；⑦Agent 删除后自动化任务 `execAgentId` 悬空显示裸 id（`positionMock.js:790-802`）。期望：领用数由分配表实时派生（或分配写点回写）；批量绑定走与单个相同的标记；`setUserPosition` 数据层拦；知识库改按 positionId 联查、岗位候选从 positionMock 实时读；种子 404 改引岗位私有技能且发布门识别悬空引用；删岗级联清三处；任务侧对已删 Agent 清空或标注。 |
| 10 | 未处理 | 陈森亮（2026-09-18） | **R5 · 能力系三对引用全是硬编码、零联动。** ①技能 `toolCount` / 类别永远是种子（`unifiedSkillMock.js:259,313-320,402-434`）：正文插入或删光 `@tool[...]` 后工具数不变，类别标签永不出现（`getSkillDetail` 恒 `category:null`）；②技能 ↔ 连接器两套真相：工具坞候选 `unifiedSkillMock.js:305 TOOL_DIRECTORY` 5 条硬编码，新建的 MCP 进不了工具坞，连接器删除 / 停用后技能侧仍「连接正常」；MCP 侧 `refs` 列的技能名（如「报销单查询」）在技能模块根本不存在；③专家 ↔ 技能：`domainExpertMock.js:54-58 SKILL_CANDIDATES` 硬编码，删专家 203 后 sk_304 引用清单仍列它、改名不更新；新市场技能（含种子 sk_309 已发布）永远进不了专家候选，草稿 sk_307 却可被引用，候选分类与技能模块不一致。依据：技能 md §二.1 L45「工具数=当前引用的工具数量」、§三.3 L173「类别由工具引用自动派生」、§二.3.5/3.6 引用保护；专家 md §三.4「仅引用市场技能」；一览表 API 行「停用后技能不再可引用」。期望：`toolCount` 保存正文时重算；工具坞候选从三连接器 mock 实时读（含健康度）并在连接器删除 / 停用时回写技能 `checkStatus`；专家候选从 unifiedSkillMock 读已发布市场技能，专家增删改引用时回写技能 `refNames`（positionMock 对岗位私有技能已经这么做，照抄 `_getRaw/_reset` 那套）。 |
| 11 | 未处理 | 陈森亮（2026-09-18） | **R5 · 组织域派生值与级联（06 组织）。** ①角色 `userCount` 是种子快照不由 users 派生（`adminUserMock.js:54-58,305`、`AdminRoles.vue:136`）：「审计观察员」实绑 3 人、userCount=0 → 可删，删后三人角色标签变裸标识、角色筛选下拉已无该项但 roleCode 筛选仍查出 3 人；「系统管理员」userCount=2 实绑 1；②角色改名后 code 与 name 脱钩，新建只查 name 唯一（`:263,274-285`）：改名再新建同名 → 两角色同 code，用户页 `roleLabel` 按 code 取首个匹配串到另一角色；③用户删除 / 停用 / 改名不同步岗位管理分配表（`positionAssignmentMock.js:21-26` 自带 6 人名册）：用户页删 li.na，岗位管理页仍在；④唯一系统管理员可被「设置角色」去掉管理员（`adminUserMock.js:214-224` setUserRoles 无护栏，deleteUser 有）。依据：角色 md §二.1「用户数量=当前绑定该角色的用户数」、§二.3.3「仍绑定用户时不执行删除」；用户 md §二.2.6「平台至少保留一个系统管理员」「删除后其角色、任务…永久删除」。期望：userCount 实时派生；code 稳定不随改名、新建按 code 唯一；分配表与用户表同源（或用户写点级联）；setUserRoles 加最后管理员护栏。 |

| 13 | 未处理 | 陈森亮（2026-09-18） | **R7 · 警告合集（逐条小改，按模块）。** 专家：非首发再发布侧栏按「首次发布 v1.0.0」显示、mock 实际提交 v2.3.1（`publishState.js:57` draft 一律 INITIAL + `VersionDrawer.vue:98/132/155`）；启用旧版改写「最新版本」（`domainExpertMock.js:601`，md §二.1 L49 应只记最近审核通过版本）；市场专家「N 个应用引用」取 `row.skillCount`（`AdminExperts.vue:384`）；`publishExpert` 不校验 `releaseNotes` 必填。技能：AI 生成 500ms 定时器切换对象后写进新对象（`aiLiveGenerate.js:157-164`，疑似）；zip 导入不校验含 SKILL.md（`SkillCreateDialog.vue:60-67`）。岗位：自动化任务种子无 `periodicPreset`，周度 / 月度任务高亮「每天」且点「每天」无反应（`SchedulePicker.vue:97-98,184`）；起 > 止日期可保存、单次无时间 mock 放行（`SampleTaskEditor.vue:406-433`、`sampleTaskMock.js:353-359`）；采集字段 key 重复无行级提示、`intakeErrors` 从不消费，发布前 `doSaveBasic(true)` 静默失败仍弹发布窗（`PositionIntakeTab.vue:50-67`、`PositionDetailTabs.vue:100,247-255,418-422`）；Agent 新建重名静默改「 2」而编辑重名报错（`positionMock.js:749-755` vs `:779`）；档案编辑三段串行中途取消半提交（`PositionDataTableStage.vue:409-415`）；`?tab=` 非法值正文空白（`PositionDetailTabs.vue:87`）；岗位管理下拉默认值 403 显示裸 id（`UserPositionEditDialog.vue:37-41`）。连接器：stdio command 粘贴导入不校枚举（`defValidate.js:221-223`，md MCP §三.4.2 L282）；新建 MCP 工具 `title` 丢失（`McpEditor.vue:598-605`）；新登记 MCP 抽屉测试成功后列表验证列仍「未探测」要再刷新一次；KB mock 允许改专家可见范围（`knowledgeBaseMock.js:326` 仅 UI 锁）。治理：审核快照按 refId 单槽位而非按申请；用户技能审核「审核尺度」三档对结果零影响（`utils/userSkillAuditMeta.js:114 needsManualAudit` 零调用，7 条种子 4 条按尺度不该进人工审核）；反馈附图弹窗只比 seq 不比反馈 id（`AdminFeedback.vue:81-102`）；关键字不 trim（`reviewsMock.js:181`、`myApplicationsMock.js:201`、`feedbackMock.js:53`）。组织：`utils/datetime.js:32-39 nowIsoLocal` 本地墙钟硬拼 `+08:00`，非 +08 时区错 12h；用户名唯一区分大小写（`adminUserMock.js:162`，**请裁**）；RoleEditor 改名 + 权限两段提交无回滚（`RoleEditor.vue:143-146`）。驾驶舱：纯静态 mock（md §一.2 已声明）——非缺陷，演示时会被问「通过审核后数字怎么不变」。 |
| 14 | 未处理 | 陈森亮（2026-09-18） | **PRD「变更记录.md」自动生成机制负责人已拍板保留，由你持续完善。** 现状：`scripts/prd-changelog.mjs` + post-commit 钩子已合入 main（PR #16），已登记进 `docs/README.md` 目录表。期望：①在 `docs/README.md` 或脚本头注写清生成规则（哪些文件夹、什么算一条变更、`--check` 怎么用）；②CI 是否要加 `--check` 步防止落后，你定并实施；③`.obsidian/` 已加入 .gitignore 并移出仓库（PR 待合），你本地不受影响，以后不会再被提交。 |

## 已处理

| 序号 | 状态 | 发起人 | 问题详述 |
|---|---|---|---|
| 3 | 已处理（2026-09-17） | 陈森亮（2026-09-17） | ~~请确认你 09-14 出的全量审查结果是否已全部落地并打闭环戳。~~ **09-17 由陈森亮作第三方核完**：30 条中 23 条已落地、7 条仍存在（C2/C7 已改代码，E1 已删代码，其余 4 条转本表 #4），文件已闭环归档。原文： 文件：`docs/02-PRD与代码一致性审查/PRD与代码现状一致性审查结果-20260914.md`（你在 `7d9e0c2` 新建、`ea5ce2c` 改过）。现状：负责人没有批注过，文内各差异点的落地状态没人核；且这份是你自审自改，按 09-17 新规则（方案稿 7.2）一致性审查要由改动者以外的人核。期望：逐条标「已落地（commit）/ 不做（理由）」，全部有结论后把文件头「状态」改「已闭环（日期）」，负责人归档。 |
| 2 | 已处理（2026-09-17） | 陈森亮（2026-09-17） | **驾驶舱（01 总览）没有 PRD 定义，请补写 md。** 现状：页面已上线（`58c741d`，全量 mock），但 `docs/PRD/数字员工管理端PRD/01总览/` 是空目录。依据：CLAUDE.md「md 为唯一口径」——代码有、md 没有就无法做一致性核对。期望：补一份 `01总览/驾驶舱/prd.驾驶舱.md`（页面结构、字段、交互、状态），按现有页面反写即可。来源：`docs/04-待补需求定义/补定义需求清单-20260907.md` 第 1 项。（配额与限流属 04 运行，已分给曹春林，见 clcao.md #1） |
| 1 | 已处理（2026-09-18） | 陈森亮（2026-09-17） | **连接器三件套的「连接器类型 / 所属岗位」在 mock 层没有落地。** 现状：`mcpConnectorMock.js` / `apiConnectorMock.js` / `bizSystemMock.js` 三个 mock 都没有 `type`、`positionId` 字段；MCP / API 编辑器表单里有「连接器类型」「所属岗位」，但提交后被 mock 丢弃；三个列表页的「连接器类型」列恒为空、类型筛选无效；业务系统编辑器（`BizSystemEditor.vue`）连「连接器类型」表单项都没有。依据：`prd-连接器-MCP.md` L244、`prd-API.md` L122、`prd-业务系统.md` L101 三份均写「连接器类型必选、创建后不可改；所属岗位仅岗位私有时展示、可绑已发布岗位」。期望：三个 mock 补 `type` / `positionId`（种子补齐 + bump version）、创建/编辑透传并校验（岗位私有必绑岗位）、列表返回 `type` 使筛选生效、`BizSystemEditor` 补类型与所属岗位表单、对应用例。注：09-15 记录里「MCP 侧删所属岗位」的做法作废，以 md 为准。来源：`docs/03-…/待决策清单-20260917.md` #9（已归档）。 |
| 4 | 已处理（2026-09-18） | 陈森亮（2026-09-17） | **03 能力四处 PRD 补写（0914 审查第三方核出的「代码有、md 没写」项）。** ① 引用清单弹窗：MCP / API / 业务系统三份 md 只写"点击弹出引用清单"，代码三页弹窗标题固定「被技能引用」+ 底部【关闭】——请各补一句标题与按钮（`prd-连接器-MCP.md` L51、`prd-API.md` L39、`prd-业务系统.md` L33）；② MCP 鉴权方式：md §四.4.1 隐含但未明写「必选（默认无鉴权）、stdio 不适用」，一览表 §5.2 也没有「鉴权方式」行——请补；③ 一览表 §6.2 API 缺「请求方式 ✅ GET/POST/PUT/DELETE/PATCH，默认 GET」一行（`prd-API.md` L156 已写、代码 `ApiEditor.vue:353` 必选校验）；④ 技能「效果测试」：代码保留整条链路但 featureFlag 关闭、入口隐藏（`featureFlags.js:13`），负责人 09-17 裁决「保留并在 md 记为预留」——请在 `prd.技能.md` 加一句「效果测试：预留能力，本版默认隐藏」。四处都是纯 md，改完提交即可。 |
| 5 | 已处理（2026-09-18） | 陈森亮（2026-09-17） | **输入框字数上限全站统一——8 条待销项归你定并执行。** 来源 `docs/调研讨论/已闭环/2026-09-15-输入框字数限制规范盘点.md` §五不一致清单、§七待销项：①知识库名称 100 还是 64（一览表名称类例外未列它，`prd.知识库.md:84` 写 100、代码 100）；②URL 两个标准：业务页 URL 1024 vs 知识库检索地址/base_url 500；③把 200（行内备注类）、128（标识符类）补进一览表统一规则表；④档案名称 128→64、专属技能名 128→64（B2/B3，随①③定）；⑤补 md：模型 app_id、档案编目三字段、档案说明的字符数（A2/B4/B5）；⑥代码收敛：示例问题 60 的 5 个真值源、AGENT_MAX/SKILL_MAX 两份定义（C2/C3）；⑦错误文案统一「最多 N 个字符」；⑧计数器统一 `show-word-limit` 或共用常量。期望：①②③先在一览表 §规则表定口径并提交，④–⑧按定下的口径改 md + 代码 + 用例（一件事一条 commit）；全部做完在 commit 说明写 `关闭待办 yuepu#5`。 |
| 6 | 已处理（2026-09-20） | 陈森亮（2026-09-18） | **R2 · 连接器类型 / 所属岗位 数据层缺失（与 #1 合并做，范围比 #1 大）。** 现状：①存量 API 全部无法编辑保存——`apiConnectorMock.js:495` toRow 不出 `type`，`ApiEditor.vue:505` 编辑态类型下拉禁用、`:340` 又必填，三者夹死；②存量专家同型——`domainExpertMock.js:273-325` create/update 不写 `type/positionId`、toRow 不出参，`ExpertEditor.vue:380-383,613`，列表类型列恒「—」，`listExperts`（`:242-263`）忽略 `type` 筛选；③岗位私有 专家 / MCP / API 一个都建不出——`ExpertEditor.vue:109`、`McpEditor.vue:111`、`ApiEditor.vue:144` 写 `const { default: { listPositions } } = await import('@/api/position')`，该模块无 default 导出 → TypeError，且传 `status:'PUBLISHED'` 而 positionMock 用小写 `published`。依据：`prd-API.md` L122、`prd-连接器-MCP.md` L244、`prd-业务系统.md` L101、`prd.专家.md` L168（类型必选、岗位私有绑已发布岗位）。期望：三连接器 mock + 专家 mock 补 `type/positionId`（种子补齐、bump version、create/update 透传并校验「岗位私有必绑岗位」、列表返回并支持 `type` 筛选）；三处 import 改具名 `import { listPositions }` + `status:'published'`；编辑态 type 为空时允许补选或 mock 回填默认。回归：`reviewFlowIntegration.test.js`、`govSeedRefIntegrity.test.js` 必须仍绿。 |
| 8 | 已处理（2026-09-20） | 陈森亮（2026-09-18） | **R4 · 字数上限 mock 没跟 UI（与 #5 合并做，#5 清单缺知识库描述）。** 09-16 `217ce1f` 只改了页面/md：岗位描述 UI 2000 / mock 500（`positionMock.js:296,703`）、岗位领用页文案 300 / 100（`:715`）、岗位示例问题 300 / 60（`:720`）、技能示例问题 300 / 60（`unifiedSkillMock.js:419`）、知识库描述 md 2000（`prd.知识库.md` L88 + 一览表 L374）/ mock 500（`knowledgeBaseMock.js:292`）且 `KnowledgeBaseEditor.vue:419` maxlength 也是 500；用户名 `UserEditor.vue:113` 页面不 trim 而 `adminUserMock.js:159` trim 后校验。实走：填 600 字描述计数显示 600/2000，保存 toast「岗位描述不超过 500 字」。期望：mock 与 UI 同一常量源（如 `positionModel.js` 的 DESCRIPTION_MAX），改完把对应用例（`positionMock.test` / `unifiedSkillMock.test` / `knowledgeBaseMock.test` 头注仍写 ≤500）一并改。 |
| 12 | 已处理（2026-09-20） | 陈森亮（2026-09-18） | **R6 · 运行时错误（岗位 / 治理 / 访问审计）。** ①岗位详情页加载完成前（冷加载约 300ms 内）点【保存】/【发布岗位】→ pageerror `TypeError: Cannot read properties of null (reading 'name')`（`PositionDetailTabs.vue:275 ensurePersisted` 读 `store.basic.name`，`store.load` 完成前顶栏按钮已可点；Playwright 2/2 稳定复现）→ 顶栏按钮在 `store.loaded` 前禁用；②`md-editor-v3` 默认从 unpkg 拉 prettier / echarts，无外网每次打开岗位详情 3 条 `ERR_CONNECTION_CLOSED` → 关 CDN（`noPrettier` / 本地）；③登录访问页时间过滤只作用于当前页（`AdminLoginLogs.vue:104` `rows.filter(inRange)`，total/page 来自 mock 全量）：第 1 页空表、分页仍显 total=11、翻到第 2 页才出现 → 过滤下沉 mock 或全量过滤后再分页；④访问审计【查看】跳知识库 / 审核中心 / 用户技能审核不注入 keyword（`KnowledgeBaseList.vue:162,167` 读 `kw`、`UnifiedReview.vue` 不读 route、`UserSkillReviews.vue:139` 只读 `view`；其余 7 页读 `route.query.keyword` 正常；md 访问审计 §6.3 L128）。（三处 `import('@/api/position')` 的 TypeError 已在 #6） |
| 16 | 已处理（2026-09-23） | 陈森亮（2026-09-23） | **技能分类 8→11 类改了种子但没 bump 持久化版本号，老浏览器里旧分类会活下来。** 现状：`3318cbb` 把 `fieldDictMock.js:22` 的 skillCategory 换成 11 类，并把 `unifiedSkillMock.js` 两条示例数据的孤儿分类值改掉（智能创作→内容创作、知识与学习→知识管理），但两个 mock 的 `attachPersist` 版本号都没动（`fieldDictMock.js:33` 仍 `version: 3`、`unifiedSkillMock.js` 仍 `version: 5`）。依据：`mockPersist.js:137` 恢复逻辑是「`parsed.v === version` 才用存量，不符才丢弃降级到种子」——版本号不变 + 种子变了 = 之前打开过 demo 的浏览器**继续用旧快照**，分类下拉还是老 8 类、两条示例技能还挂着「智能创作 / 知识与学习」这种现在选不出来的值；只有没打开过或手动 `?resetMock=1` 的浏览器才看得到新分类。commit 说明里只核对了 `mockSeedOverrides.json` 出厂数据（那个确实已失效跳过），漏了 localStorage 快照这条路径。期望：`fieldDictMock` 与 `unifiedSkillMock` 的 persist version 各 +1（3→4、5→6），让旧快照失效重播种子；顺带确认这批改动里其他改了种子的 mock 有没有同样漏 bump。注：改种子必 bump 是既有约定，其余 mock 都照做了（如 domainExpertMock 09-20 改种子时 3→4、本批又到 5）。 |
| 17 | 已处理（2026-09-23） | 陈森亮（2026-09-23） | **演示阻断两条（连接器引用弹窗文案错配 + 知识库 kb_7 图标为空）。** ①**「N 个岗位引用」点开弹的是「被技能引用」**：`27a5aaf` 把 `positionCount` 的语义从「绑没绑岗位（0/1）」改成「被 N 个岗位引用」并新造了 `referencedByPositions` 清单，但点击行为没跟着改——`AdminMcp.vue:566` 按钮写 `{{ row.positionCount }}个岗位引用`，`:156-159` `openRefs` 却读 `row.referencedBySkills`，`:745` 弹窗标题写死「被技能引用」，三方对不上（`AdminBizSystems.vue:354`+`:174`、`AdminApis.vue:601`+`:300` 同构）。旧口径下 positionCount 恒 0/1 属「凑合」，语义一改就是明确的错，演示时点下去当场露馅。commit 说明里自认「既有行为，本次不动」。期望：`openRefs` 按 `row.type` 分流到 `referencedByPositions`（数据现成）并让弹窗标题跟着变，顺带解掉②。②**`referencedByPositions` 是死字段**：三个 mock 都往外吐（`mcpConnectorMock.js:330`、`apiConnectorMock.js:525`、`bizSystemMock.js:159`），且带着 positionName，但全仓 `.vue` 零消费——显然是为①准备的，只写了一半。③**知识库种子 kb_7 图标为空、打开就存不下去**：`knowledgeBaseMock.js:173` `{ id: 'kb_7', name: '薪酬与绩效制度', icon: '', …, description: '' }`，而 `61dbd19` 已把图标改必填（`KnowledgeBaseEditor.vue:100` `icon: [{ required: true … }]`）、描述早就必填（`knowledgeBaseMock.js:303-304`）——这条种子一点编辑就被两条必填卡死。本批唯一被新必填卡住的种子（岗位图标/采集字段/领用页文案/任务提示词/技能图标的既有种子都已逐条核过，不卡）；`a4d7540` 做「采集字段至少 1 个」时是主动补了种子的，知识库这条漏了。期望：补 kb_7 的 icon 与 description；若 kb_7 是刻意留的「空白库演示样本」请说明，那它与 a4d7540 的处理方式冲突，需定口径。 |
