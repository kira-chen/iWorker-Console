# 浦月（yuepu · GitHub yue0403）的待办

> 用法：任何人在执行任务时发现**需要 浦月 处理**的事项，往下表追加一行（序号递增，状态填「未处理」）。
> 浦月 处理完后把状态改为「已处理」并在同一次 commit 里提交；不删行，留作记录。
> 问题详述要写清：现状 / 依据（md 章节或 commit）/ 期望结果，让人不用回头找上下文就能动手。

| 序号 | 状态 | 发起人 | 问题详述 |
|---|---|---|---|
| 1 | 未处理 | 陈森亮（2026-09-17） | **连接器三件套的「连接器类型 / 所属岗位」在 mock 层没有落地。** 现状：`mcpConnectorMock.js` / `apiConnectorMock.js` / `bizSystemMock.js` 三个 mock 都没有 `type`、`positionId` 字段；MCP / API 编辑器表单里有「连接器类型」「所属岗位」，但提交后被 mock 丢弃；三个列表页的「连接器类型」列恒为空、类型筛选无效；业务系统编辑器（`BizSystemEditor.vue`）连「连接器类型」表单项都没有。依据：`prd-连接器-MCP.md` L244、`prd-API.md` L122、`prd-业务系统.md` L101 三份均写「连接器类型必选、创建后不可改；所属岗位仅岗位私有时展示、可绑已发布岗位」。期望：三个 mock 补 `type` / `positionId`（种子补齐 + bump version）、创建/编辑透传并校验（岗位私有必绑岗位）、列表返回 `type` 使筛选生效、`BizSystemEditor` 补类型与所属岗位表单、对应用例。注：09-15 记录里「MCP 侧删所属岗位」的做法作废，以 md 为准。来源：`docs/03-…/待决策清单-20260917.md` #9（已归档）。 |
| 2 | 未处理 | 陈森亮（2026-09-17） | **驾驶舱（01 总览）没有 PRD 定义，请补写 md。** 现状：页面已上线（`58c741d`，全量 mock），但 `docs/PRD/数字员工管理端PRD/01总览/` 是空目录。依据：CLAUDE.md「md 为唯一口径」——代码有、md 没有就无法做一致性核对。期望：补一份 `01总览/驾驶舱/prd.驾驶舱.md`（页面结构、字段、交互、状态），按现有页面反写即可。来源：`docs/04-待补需求定义/补定义需求清单-20260907.md` 第 1 项。（配额与限流属 04 运行，已分给曹春林，见 clcao.md #1） |
| 3 | 未处理 | 陈森亮（2026-09-17） | **请确认你 09-14 出的全量审查结果是否已全部落地并打闭环戳。** 文件：`docs/02-PRD与代码一致性审查/PRD与代码现状一致性审查结果-20260914.md`（你在 `7d9e0c2` 新建、`ea5ce2c` 改过）。现状：负责人没有批注过，文内各差异点的落地状态没人核；且这份是你自审自改，按 09-17 新规则（方案稿 7.2）一致性审查要由改动者以外的人核。期望：逐条标「已落地（commit）/ 不做（理由）」，全部有结论后把文件头「状态」改「已闭环（日期）」，负责人归档。 |
