# 待决策清单 · 2026-09-12

这份清单是《测试用例审计-20260912.md》第十节（J1–J22）经团队讨论后的**剩余项**。

**团队已按规则闭环 16 项**（依据：md 是唯一口径 / 您此前已拍板 / 02 审查已裁「按 md 实现」），成果见 commit `83b8331`…`b6b743f` 六条。**剩下 6 项**规则给不出答案，需要您拍板——每项都写成「背景 → 选 A 还是 B → 团队建议」，可以直接在本文件上批注。

已闭环的 16 项不必再看，附录一列了清单备查。

---

## 执行状态（2026-09-13 更新）

**六项均已获负责人批复「认可团队建议 A」，并于 2026-09-13 全部执行完毕**（commit `fb2f0cc`…`21b75ad` 九条，详见 `docs/PRD-review/2026-09-13.md`）。两个附带项批复：决策 3 的登录页/占位页「一起删」、决策 4 的报表两页「一并删除」。

| 决策 | 批复 | 落地 |
| --- | --- | --- |
| 1 名称 / 说明字数 | A（64 / 500） | 页面 + mock + md + 一览表补子字段行 |
| 2 提交发布提示 | A（带版本号） | md 回写，代码本就如此，补断言 |
| 3 员工端去留 | A（删，附带项一起删） | 删 53 个产品文件、191 条用例；可达性实证 0 孤儿 |
| 4 契约测试 / 报表 | A（留代码删测试；报表一并删） | 删 45 条契约用例；报表两页 + EChart + echarts 依赖 |
| 5 审核联动 | A（排期做） | 八模块联动 + 我的申请回写 + 审核人；+16 条用例 |
| 6 三个废弃字段 | A（清，已确认废弃） | 推荐问题 / 档案三字段 / AI 生成函数，三条 commit |

结果：测试 160 文件 2247 条全绿、browser 29 条绿、build 通过、skip 为 0；产品文件 252 → 199。

**附录二**：第 1 项（两笔 §7 PRD 修订）已于 2026-09-13 获负责人确认为正式口径，销项；其余 4 项与新增的「PRD 正本修改归口」问题仍需您示下。

---

## 需要您决策的 6 项

### 决策 1 · 自动化任务的「任务名称」最多能填多少字

**背景**：岗位详情页的自动化任务里，「任务名称」这个输入框，现在三个地方说了三个数：页面上限制 60 字，今天 PRD 新写的是 128 字，而《各模块必填选填字段一览表》里全站通用的规矩是「名称类一律 64 字」。「说明」这一栏同样分裂：页面 200 字、PRD 500 字。

PRD 那段是今天上午补写的，说明写着「根据前端实现补全」，但数字和页面对不上——所以我们判断不出是笔误还是新要求，不敢自己改。

| 选项 | 具体做法 | 后果 |
| --- | --- | --- |
| **A（团队建议）** | 名称 **64** 字、说明 **500** 字 | 名称跟全站通用规矩走，说明用 PRD 今天写的数。改 PRD 一个数字、一览表补两行、页面改两个限制 |
| B | 照 PRD 现在写的 **128 / 500** | 页面改两个限制；但「名称一律 64 字」这条全站规矩要为它破一次例，一览表得加一条特例 |

**团队建议 A**。理由：09-08 您定过「md 里的数值 = 一览表通用规则」，选 A 不用开特例。

---

### 决策 2 · 技能提交发布后，提示语用哪一句

**背景**：技能点【提交发布】之后弹的那句提示，PRD 前后两节写了两个版本，页面现在用的是带版本号的那句。

| 选项 | 提示语 | 后果 |
| --- | --- | --- |
| **A（团队建议）** | 「已提交发布 v1.2.0，进入审核」 | 就是页面现状，只改 PRD 前面那一句，**代码不用动** |
| B | 「已提交发布审核」 | 改 PRD 后面那句 + 改代码 + 改用例 |

**团队建议 A**。理由：带版本号信息更全，且零代码改动。

---

### 决策 3 · 员工端那套代码（约 1.2 万行）删还是留

**背景**：仓库里还留着一整套「员工端」——聊天、定时任务、个人空间、领岗位、绑定岗位等 **47 个文件、约 11,600 行**，外加 20 个测试文件 191 条用例。

管理后台完全用不到它们：菜单里没有入口，只有手敲网址才能进去；进去后大部分是「功能封存」的占位页，少数能打开的页面因为没有 mock 数据、直接连不存在的后端，操作必然报错。

您 09-11 说过「不用管员工使用端」，但那句话定的是**不验收**，没说代码删不删——删这么大一块，团队不敢自作主张。

| 选项 | 具体做法 | 后果 |
| --- | --- | --- |
| **A（团队建议）** | 整体删除（代码 + 191 条用例） | 管理后台演示零影响（已实测这些文件零引用）；仓库少掉约 1/5 的无关代码，研发读代码时不再被干扰。要顺带改 4 个后台共用文件的引用。以后想做员工端，从 git 历史找得回 |
| B | 留着不删，只在文件开头注明「员工端封存」（现状） | 不用动代码；但研发打开项目仍会看到十几个死页面，每轮审计还要再解释一遍 |

**团队建议 A**。理由：工程大原则第②条是「代码要让研发读得懂」，这批是全仓最大的一块与 demo 无关的代码；而且 09-09 您已经按同样标准删过 17 个死文件。

**顺带请定**：`Login.vue`（登录页，现在进去会被立刻弹走）、`ChangePassword.vue`、几个占位页，要不要一起删？

---

### 决策 4 · 「以后接后端用」的那半套代码要不要留

**背景**：每个数据接口文件都写成两条路——「演示走 mock 假数据」和「以后接了后端走真接口」。现在永远只走前一条。共 23 个接口文件、326 处真接口调用。

测试里有 6 个文件约 45 条用例，专门验证那条走不到的路「网址拼得对不对」。

| 选项 | 具体做法 | 后果 |
| --- | --- | --- |
| **A（团队建议）** | 真接口代码**保留**（当作给研发看的示例），只删那 45 条测试 | 改动小，代码不动；测试台账里不再有「测一条走不到的路」 |
| B | 真接口代码全站一起删，只留 mock | 研发少看一半接口代码；但要动 23 个文件、过一遍全站回归，工作量大 |

**团队建议 A**，并且：如果要做 B，应该全站一次做完，不能只删其中 5 个文件——否则「两条路」的写法只剩一半，比现在更难读。

**顺带请定**：报表页两个（`FdeReports` / `SysConfigReports`，菜单里已隐藏、没有 PRD、没有 mock、打开就是失败态）删还是留？它们是 `echarts` 这个依赖的唯一使用者。

---

### 决策 5 · 「审核中心点通过之后，业务对象状态跟着变」要不要做

**背景**：PRD 写得很清楚——审核中心点【通过】，对应的岗位/专家/技能/知识库就应该变成「已发布」，「我的申请」里那条记录也应该变成「已通过」，同时记下审核人和时间。

现在页面上点【通过】只把审核列表那一行划掉了，业务对象状态没变、我的申请没变、也没记审核人。结果是 demo 演示时，「提交审核 → 审核 → 生效」这条链路走不通。

这件事**不需要您做产品取舍**（PRD 已经写明怎么做），只需要您定**要不要现在排期**。

| 选项 | 后果 |
| --- | --- |
| **A（团队建议）** | 排进下一批做，约一天工作量（涉及 8 个数据层文件 + 治理两页用例）；做完 demo 的审核链路才完整 |
| B | 暂不做，继续挂账 |

**团队建议 A**。理由：审核是管理后台的主线功能之一，链路断着会影响演示完整性（工程大原则第①条）。

---

### 决策 6 · 十几个「没人用的函数」连同它们的测试一起清掉

**背景**：代码里有十几个函数和常量，已经没有任何页面在调用了，但还各自带着测试在跑绿——等于在测「没人用的东西」。

团队已经按 09-09 您授权过的同类标准清掉了大部分（见 commit `9c29ff9` / `264669d`）。**剩下这几个不敢直接动**，因为它们不是单纯的死代码：

| 项目 | 为什么单拎出来 |
| --- | --- |
| 岗位的「推荐问题（固定 4 格）」 | 界面早就删了，但数据里还在存这个字段、种子数据里还有 4 条；**PRD 里根本没有这个功能**。删要动数据结构 |
| 工作档案配置的三个字段（checklist / askTier / pendingTtlDays） | 界面上没有、PRD 里也没有，但它们是一个还在用的校验函数里的分支 |
| 业务系统的「AI 生成示例问题」 | 09-04 有过一条注释明确写「保留在接口层不删」，团队不清楚当时的理由 |

| 选项 | 后果 |
| --- | --- |
| **A（团队建议）** | 一并清掉（含数据结构与种子数据调整） | 代码更好读；对现有页面零影响 |
| B | 保留 | 下一轮审计还会再列一次 |

**团队建议 A**，但这三项都涉及「PRD 里没有的功能」，请您确认它们确实是废弃的、不是待做的需求。

---

## 附录一 · 团队已闭环的 16 项（备查，不需要决策）

| 项 | 事情 | 依据 |
| --- | --- | --- |
| J1 | 7 份 PRD 补上「状态是独立一列」 | 09-11 您按设计图拍过板，代码已改、PRD 漏回写 |
| J4 | 技能列表页测试不再验「走不到的路」 | 审计自身原则：不写永远绿的用例 |
| J5 | 自动化任务两个开关退役，三模式成唯一形态 | 另一侧已无人使用；顺带修好「保存丢字段」的主链路缺陷 |
| J7 | 「提示词」改为非必填 | PRD 明确只列了两个必填项 |
| J8① | 知识页签【查看】说法补全 | 与知识库 PRD 一致 |
| J8③ | 页签标识改名对齐 PRD | PRD 唯一口径 |
| J9 | 角色编辑器去掉 PRD 没有的提示语 | 02 审查 Q325 已裁「按 PRD 措辞」 |
| J10 | 专家 PRD 补「停用不刷新更新时间」 | 模型 PRD 已写明同一规则 |
| J11 | 头像菜单里两个必然失败的入口先隐藏 | Q183「登录方案暂不考虑」 |
| J13 | 清掉 11 处没人用的函数及其测试 | 09-09 您授权过同类清理 |
| J14 | 技能编辑器 showClose 开关退役 | 另一侧无人使用 |
| J15 | 代码里比 PRD 多的 6 处限制：3 处补进 PRD、3 处按 PRD 收敛 | PRD 唯一口径 |
| J16 | 检活失败的错误码不再显示「UNKNOWN」 | PRD 要求显示错误码 |
| J17 | MCP 测试连接失败改成 PRD 的两段式 | 02 审查 Q135 已裁「按 PRD 实现」 |
| J18 | 领用文案满 6 条改为可点后提示 | PRD 写的是「提示」不是「藏起来」 |
| J19 | 字段字典删草稿行不再多弹一次确认 | PRD：仅从草稿移除 |
| J20 | 新加 4 条代码卫生检查 | 按您 08-08 定的「零误报才收」标准，逐条实测通过 |
| J21 | 分页规则（四档 + 跳页）补进 PRD | 09-11 您拍过板，PRD 漏写 |
| J22 | 修好测试框架吞掉表单校验异常的问题 | 此前空表单也能提交成功却测不出来 |

附带修掉 **33 个代码缺陷**（K 编号），其中较重要的三个：
1. **自动化任务保存丢字段**——设成「每 3 小时」存进去变「每 1 小时」，设成「单次」重开变「按周期」；
2. **MCP 停用没走审核**——PRD 要求进「审核中」，实际直接变成「未发布」；
3. **模型编辑器表单校验形同虚设**——空表单点【接入】也能提交（测试框架把校验异常吞了，所以一直没被发现）。

## 附录二 · 团队发现但未处理的事项

1. ~~**两笔 PRD 修订的确认**~~ → **已确认（2026-09-13）**：负责人确认 `df9b90e`、`ed839c3`（09-12 §7 自动化任务）**即正式口径**。基于其落地的 K1–K9、K3/K4/K5、J5 与决策 1 依据成立，本项销项。
2. **API 页分页文案**：`prd-API.md` 的分页写「共 N 个 · 每页 X 个」，与 09-11 统一的「共 N 条数据」不一致——因为 API 页是按「系统」分页，单位可能要保留「个」，请您定一句。
3. **09-09 遗留两项未回音**：Agent「存在某个」的口径、用户页双空态。
4. **运行规格 PRD 残留后端表述**：`prd.运行规格.md` 两处「由后端返回」，纯前端 demo 里属文档卫生问题。
5. **PRD 里没写、团队按最贴近写法落地的文案**（运行规格保存确认相关，共 4 处，已在代码注释标注）：放弃修改确认、待审批阻断提示、两个确认按钮文案——如与您预期不符请指出。
