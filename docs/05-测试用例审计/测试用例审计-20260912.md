# 测试用例审计 · 2026-09-12（首轮全量）

**基线**：起点 commit `6483bba`（含 yuepu 今日 PRD §7 更新）；`npx vitest run` 149 文件 / 1840 条全绿；`*.browser.test.js` 5 个（未跑，仅判可运行性：可运行，见 T21）；`it.skip/.only/.todo` = 0。
**范围**：全量（154 个测试文件，按 7 组并行只读审查：岗位系 / 知识库+连接器三件套 / 技能系 / 专家·模型·运行规格 / 治理域 / 组织域 / 共享层·卫生守卫·前台残留）。
**口径**：`docs/PRD/数字员工管理端PRD/` 各模块 md 为唯一口径；原型措辞只作历史出处。三方（md / 代码 / 用例）不一致的归因：用例≠md 且代码=md → 改用例；用例=代码≠md → 代码缺陷（本轮不改代码、不改用例）；三方各异或涉近期拍板 → 待裁决。
**终点 commit**：见文末「处置结果」（下一次 `/test-audit changed` 以此为起点）。

## 发现汇总

| 视角 | 岗位系 | 知识库+连接器 | 技能系 | 专家·模型·运行 | 治理域 | 组织域 | 共享层 | 合计 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A 产品口径 | 20 | 34 | 12 | 20 | 3 | 4 | 1 | 94 |
| B 代码现状 | 7 | 11 | 9 | 2 | 3 | 3 | 4 | 39 |
| C 可信度 | 2 | 11 | 3 | 6 | 2 | 3 | 5 | 32 |
| D 可读性 | 8 | 14 | 4 | 6 | 2 | 3 | 1 | 38 |
| E 变更增量 | 1 | 7 | 2 | 2 | 2 | 5 | 2 | 21 |
| F 结构覆盖 | 12 | 13 | 4 | 4 | 9 | 3 | 8 | 53 |

按处置类型聚合后共 **58 个 T 条目**（一个 T 条目可含同类多处），另有 **代码缺陷 41 项**、**待裁决 27 项**、**明确不测 14 项**。各组原始报告（含每条的 file:line 与 md 条款原文）存审计过程记录，本文件按处置类型只列执行单元。

---

## 一、删除失效（单条 / 幽灵 mock / 假断言）

- **T1** 组织 · `adminUsers.test.js:13` · `vi.mock('@element-plus/icons-vue')` 幽灵：`AdminUsers.vue` 不 import（图标由 `main.js` 全局注册）。删。
- **T2** 连接器 · `adminMcp.test.js` · `:29 relistMcpService` 桩（组件已不 import）、`:212/:260` 对该函数的 not 断言、`:389-403`「页头不再有角标」整条（md 无角标条款，「没有=没有」）、`:51/52/62 el-empty/el-card/el-pagination` 桩（模板已无）、`:48-49` StatusTag/HealthTag 全局桩（对局部 import 无效）。删；`:208` 用例改名「已停用（DELISTED）归未发布，操作位出【发布】（md §二.3.4 L108）」只留正向断言。
- **T3** 连接器 · `ApiEditorReadWriteError.test.js:40,80` · SchemaFieldEditor 全局桩对局部 import 无效、`aiGenerateExampleQuestion` 组件未 import。删两行。
- **T4** 连接器 · `ToolDockBizSystem.test.js:23,28-30,76,112` · ToolDock 已不 import `@/api/admin`，「不调 listBizSystems」是空断言；`BizSystemEditorSkills.test.js:34-38` `aiGenerateBizExampleQuestions` 幽灵桩（UI 改走 useAiLiveGenerate）。删。
- **T5** 连接器 · `bizSystemMeta.test.js:71-76` ·「不再校验 code」：validateBizSystemForm 无任何 code 字样，永不红。删。
- **T6** 连接器 · `connectorEditorsPrototypeLayout.test.js:330-335,489-495` ·「同行」只查包装类，jsdom 不验布局且 md 无条款。删（要守布局走 browser 用例）。
- **T7** 连接器 · `connectorEditorsPrototypeLayout.test.js:239-252,274,497-507` · `is-card`/caret「▶」/`.reference-section` 非卡片/`.pr-add` null 纯原型视觉断言，md 无出处。删；B3 用例改断「收起业务页」（md 业务系统 §三.3 L105）。
- **T8** 连接器 · `bizSystemMeta.test.js:45-47` · `expect(常量).toEqual(常量)`。删；`:63-65` 改名「连接方式仅登录态托管一种（md §三.2）」保留。
- **T9** 连接器 · `toolKey.test.js:20-22` · `TOOL_TYPES` 常量同义反复。并入 L13 normType 用例。
- **T10** 专家 · `adminModels.test.js` fixture 幽灵字段 `publishedVersion`/`reviewComment`（`adminModelMock.mkModel` 无此字段）。删字段。

## 二、更新冲突用例（用例≠md 或 ≠代码现状，代码=md）

- **T11** 组织 · `adminUsers.test.js:41-50,144-152` · 排序用例走 bdfed58 为测试保留的兼容壳 `onSortChange`，真实入口是 0de8fe9 的表头按钮 `toggleSort`。改：tableColStub 渲染 header 插槽，点 `.time-sort`，断言 `listUsers` sort desc→asc + page:1、箭头 ↓→↑（md 用户 §二.1 L49）。
- **T12** 组织 · `adminRoles.test.js:86` · listRoles 桩返回 `{list,total}`，真实 mock 返回裸数组。改 `mockResolvedValue(ROWS)`。
- **T13** 技能 · `skillEditorReadonly.test.js:6-8,66,84` · 头注引「平台技能 Tab 只读详情 设计 §4.2」，source 用无人调用的 `'platform-candidate'`。改 `'platform'`，头注改「md 技能 §三.2/§三.5/§三.7 只读态；入口=【查看】→ 编辑路由 ?view=1」。
- **T14** 连接器 · `BizSystemEditorSkills.test.js:43-49` · defValidate 桩缺 `isBlankBizPage`（`BizSystemEditor.vue:47-54` 已 import），触碰 save 即炸。改 `importOriginal` 展开。
- **T15** 连接器 · `BizSystemEditorSkills.test.js:160` · fixture `status:'active'` 是已删模型，改 `'NOT_PUBLISHED'`；`:52-58` IconPickerPopover 桩补 `readonly/headless` props。
- **T16** 连接器 · `knowledgeBaseMock.test.js:427` · UPLOAD 夹具缺 md §五.1 必填项，改 `{ ...UPLOAD_DEFAULTS, embeddingModelId:'md_emb_1' }`。
- **T17** 连接器 · `knowledgeBaseMock.test.js:365-370` · 模块级 `lastTest` 单例跨用例泄漏（shuffle 下随机假绿），L366 改独占 endpoint。
- **T18** 专家 · `adminModels.test.js:158-165` · fixture `md_pending` 无 `pendingAction`（`AdminModels.vue:152 isPending=!!row.pendingAction`），审核中行从未真正触发。补 `pendingAction:'PUBLISH'`；`:341` 用例名「编辑任何状态下都可点（无审核锁定态）」与 md 相反，改「非审核中行 → 编辑可点」。
- **T19** 专家 · `adminModels.test.js:17-24` · api 桩缺 `withdrawModel` 键（页面 import 了）。补。
- **T20** 岗位 · `positionWorkbenchMock.test.js:111-112` · 注释「描述 2000」、断言 2001 被拒，测不出 md §2.1 L175 的 500 线。改 500 放行 / 501 拒。
- **T21** 共享 · `themeTokens.browser.test.js:24-32` · `referencedTokens()` 不剥注释，`theme.css:321` 注释里的 `var(--c-primary)`（94c8ecf 09-11）使真浏览器必红。改先 `replace(/\/\*[\s\S]*?\*\//g,'')`，扫描范围扩到 `src/assets/*.css` 全部 8 个（剥注释后 0 缺，不引入新红）。
- **T22** 治理 · `skillReviewMock.test.js:30` ·「≥7」与 `:64`「=7」不一致，种子恒 7。改 `toBe(7)`。
- **T23** 专家 · `domainExpertMock.js:549-554 __resetExpertMock` 不重置 `reviewSnapshots`（测试基建，非产品逻辑）。补 `reviewSnapshots = {}; seedReviewSnapshots()`。
- **T24** 岗位 · `positionAgentTab4C.test.js:250,263,308-311` · 只对 `LIMITS.SKILL_MAX` 常量断言（改回 20 也不红）。加 `expect(LIMITS.SKILL_MAX).toBe(100)` + 文案「每个 Agent 最多引用 100 个技能」字面（md 岗位 §6.4 L354；09-08 裁决）。

## 三、修正头注 / 改名（不改断言）

- **T25** 组织 · 7 文件「原型复刻批次 2A / 照原型」→「对齐 md §节（历史出处：原型）」：`adminUsers.test.js:6,8,119,144,156`、`adminRoles.test.js:6-7,95`、`RoleEditor.test.js:9,131,174,276`、`roleCheckCards.test.js:6`、`userEditor.test.js:6`、`userRoleDialog.test.js:6`、`userPositionEditDialog.test.js:6`。
- **T26** 岗位 · `SampleTaskPrototype4B.test.js:2-13,202,244,421`（「#20 详细说明」「执行星期按钮条」已删；→「对齐 md §7（2026-09-12 ed839c3）三模式/提示词/空闲时段」）；`sampleTaskMock.test.js:29`（加「c40c606 三模式+preKick，persist v4」）；`adminPositionsOps.test.js:8`（「照交互原型 v2 positionActions L1170」）；`positionMock.test.js:91-95` 注释「§6.5 不参与发布阻断」过期（09-09 Q11 推翻）→「mock 层不做完整性门，门在页面层 computeCompletenessMissing；404 种子六项齐备故可发」；`positionDetailTabs.test.js:8-9,97-104`（「六页签」→「md §1.3 七页签 + 运行/效果测试两占位 = 9，09-10 选 C」）；`positionCompletenessG1.test.js:281` 用例名「§5.3」→「§5.2/§11」；`positionModel.test.js:400` 注释「§7.6」→「§7.9」；`PublishCheckDialog.test.js:143`「照原型 L1224」→「md §3.7」；`positionAssignmentMock.test.js:18`「种子 6 用户照原型」；`SkillFileTree.test.js:432-556`「后端端点 N」→「xxx（mock 层）」。
- **T27** 技能 · `adminSkillEditBackPlatform.test.js:6`「SysConfigSkills」、`adminSkillEditDescription.test.js:257,266`「AdminSkills」→ 实断 AdminSkillsUnified（随 T32 合并一起做）；`unifiedSkillApiFor.test.js:79`、`unifiedSkillMock.test.js:172`、`aiLiveGenerate.test.js:5-8,34,47`「交互原型 v2 / 照原型逐字」→ md 技能 L12 / L252-255、一览表 L328/L330 + AI生成按钮Prompt规范 L16；`skillCategory.test.js:10-28` 英文用例名 → 中文「categoryLabel：OPERATION→操作类、QUERY→查询类（md L172）」；`skillPublication.test.js:36`「七态」→「八态」（随 T54 补第 8 态）。
- **T28** 专家 · `adminModels.test.js:5-15,247` 头注/describe 写已废 V95/V96 → 「对齐 prd-模型.md §二.3.1 三态按钮 + §二.3.4 验证列」「三态 + 发布/停用双向过审」；`expertEditor.test.js:490`（名「查看/检索测试→跳」但只点查看且与 L505 矛盾 → 「「查看」→ 收抽屉并跳知识库路由带参」）、`:345,353,380-414,644`「照原型」→ md §三.1/§三.2/§三.5，`:411` 282px 标「无 md 条款，视觉守卫」；`adminModelMock.test.js:33`「种子照原型」→「种子 5 行：默认模型恒首位，出参只带掩码」、`:54`「（M10）」标注 md §三.2 名称≤64 唯一；`adminExperts.test.js:336`「确认文案照原型」→「对齐 md §二.3.4」；`adminModel.test.js:69`「V96：模型只有上架/下架」→「提交发布 / 提交停用审核」。
- **T29** 治理 · `skillReviewMock.test.js:127`「即时生效并持久化」（09-09 已改草稿制）→「写入后 getRiskConfig 读回新尺度（由【保存设置】调用，md §7.1）；非法值报「审核尺度不合法」」；「照原型/原型 v2」8 处：`feedbackMock.test.js:5-6,29`、`loginLogMock.test.js:4`、`reviewsMock.test.js:12,79`（→「通过停用申请 → DELISTED（md §5.2 L80）」）、`myApplicationsMock.test.js:12`、`reviewMeta.test.js:18-20`、`market.test.js:3-4`、`defValidate.test.js:116`。
- **T30** 共享 · `IconField.test.js:6`「原型复刻批次 1 S3/S4」→ md 专家 L166-170 / 技能 L185-188 / 模型 L233-236 / 一览表 L24；`useAdminList.test.js:16-17`「原型 L1546」→ 岗位管理 L48 / MCP L167；`drawerEditor.test.js:175`「原型基座 780px」→ PRD-review 09-08 拍板；`iconCrop.test.js:15` 补 md 出处。
- **T31** 连接器 · `adminMcp.test.js:5-16` 头注停在 08-20 → 「对齐 MCP md §一/§二.1（状态列 09-11 拍板待裁见 J1）/§二.2/§二.3；StatusTag/HealthTag/ListPagination/ListStates 为真组件」，`:438` 真实 400ms 等待改 fake timers；`adminKnowledgeBase.test.js` **改名 `knowledgeBaseList.test.js`**（被测 KnowledgeBaseList.vue）+ 头注「PRD-20260903」→ md §三/§四，`knowledgeSourceList.test.js:7` 同；`knowledgeBaseMock.test.js:23-32,59,150,165,241,304`「PRD-2026090x」→ md §，`:165` 夹具名 '企业制度库文档' → '产品资料'；`dataTableTypes.test.js:37`「7 类」→「md §4.2.2 六项 + ENUM 存量兼容（仅回显）」；`docMeta.test.js:135`「契约 §0.1」；`dataTableMock.test.js:21` 用例名「401/402/403」补 402/403 `total===1` 断言；`mcpConnectorMock.test.js:3-4,36,52`「PRD-20260903」「照新原型兜底」；`mcpMeta.test.js:14,31,57,150`、`mcpVerify.test.js:32-52`、`ToolDockPlatform.test.js:5-19` 引已废契约/ToolHealthService；`connectorEditorsPrototypeLayout.test.js` **改名 `connectorEditorsLayout.test.js`**，头注 L17 与 L340 矛盾、L25「Q114 待裁」过期，用例名换 md §节，A4「六列」实断 5 列；`sourceMappingEditor.test.js:9,103,119-120`（删两行位置断言）、`schema.test.js:228,423`、`SchemaFieldEditor.test.js:29,50,93`；`bizSystemMock.test.js:34-124` B4/B6/BQ 代号 → md §；`BizSystemEditorSkills.test.js:6,42,51,119`（「watch 非 immediate」与代码相反 → 「watcher 为 immediate（5303c7c），此处走 false→true 覆盖常驻路径」）；`ToolDockBizSystem.test.js:6-19`「契约 §4.5.3」；`paramRowsEditor.test.js:95,178`「默认（MCP Env 形态）」→「默认形态（知识库数据源用）」，L95「表头四列」实断一列；`connectorEditorsPrototypeLayout.test.js:407-425` 用例名去「原型 L3892」。

## 四、合并 / 拆分

- **T32** 技能 · `adminSkillEdit{BackPlatform,BackPosition,Description,LeaveGuard,Q2Diff,SourceMatrix}.test.js` 6 文件 22 条（文件名带审查编号，8/11 个 vi.mock 逐字相同）→ 合并为 `adminSkillEditPage.test.js`：公共桩（vue-router hoisted routeState + leaveGuard 捕获；stores/position spy；SkillFocusEditor 桩 props/emits 并集）；describe 按 md 分：数据源分流 / 返回 / 保存 / 离开拦截 / 工具引用 diff / 只读态（新增）。用例数不减；旧 6 文件删除（属合并非删覆盖）。
- **T33** 技能 · `components/__tests__/skillCreateDialog.test.js`(9) + `components/skill/__tests__/SkillCreateDialog.test.js`(13) 同组件两处 → 合到后者；前者 `TYPE_OPTIONS` 旧词表「系统内置/平台共享」改「通用技能/岗位私有/市场技能」；删前者。
- **T34** 技能 · `skillFileTree.test.js` 104 条 20 describe 4 主题 → `skillFileTree.test.js`（树/路径/移动/上限）+ `skillFileTree.frontmatter.test.js` + `skillFileTree.export.test.js`；条数不减。
- **T35** 岗位 · `positionModel.test.js` 636 行 71 条 12 describe 8 主题 → 拆出 `positionModel.publish.test.js`、`positionModel.intake.test.js`、`positionModel.toolRef.test.js`，其余留原文件；条数不减。
- **T36** 连接器 · `bizSystemMeta.test.js:50-210` 19 条全测 `defValidate.validateBizSystemForm` → 搬入 `defValidate.test.js`；`:141-144` 与 `:154-157` 输入完全相同，合并（19→18）。
- **T37** 连接器 · `mcpMeta.test.js:227-251` 读 McpEditor 源码做正则断言 → 迁入新建的 `mcpEditor.test.js`（见 T58）。
- **T38** 连接器 · `connectorEditorsPrototypeLayout.test.js:427-459` 与 `SchemaFieldEditor.test.js:109-143` A5 重复且断言实现结构 → 前者只留 md 可溯部分（反解析出 2 行、对象行有【＋子字段】）。
- **T39** 组织 · 6 个页面级测试各复制一份 el-table 桩（`adminUsers/adminRoles/adminExperts/adminPositionsOps/adminPositionAssignments/userSkillReviews`）→ 抽 `views/admin/__tests__/helpers/elTableStub.js`（已建），各文件改 import。
- **T40** 治理+共享 · `govDialogs.js` 用例两组都提出，合为一个 `utils/__tests__/govDialogs.test.js`（治理组落地）。

## 五、提升断言（假绿 → 断言用户可见结果）

- **T41** 组织 · `adminUsers.test.js:187-194` 重置密码补确认正文「确认将「周明」的密码重置为默认密码？」+ `wemate123`（md 用户 §二.2.5 L95）；`userPositionEditDialog.test.js:71-95` 三条补 `success('岗位绑定已更新')`（md 岗位管理 §3.3 L70）、`update:visible false`、无变化不 success。
- **T42** 技能 · `VersionHistoryList.test.js:65-68,70-82` 空态文案「暂无版本 · 发布后将在此生成不可变版本快照」逐字 + 发布人/下线于/升级说明（md L249-250）；`skillCreateDialog` 重开清空追加 `zipItems=[]`、`createCategory=''`、`createMode='zip'`（md L152/L165）；`versionDrawer.test.js:171-180` 撤回补 `success('已撤回提交')`（md L243）+ 新增「提交失败 → error(原因)、抽屉不关、升级说明保留」（md L234）；`adminSkillEditDescription`（并入 T32）保存后补 `success('技能配置已保存')`（md L176）。
- **T43** 专家 · `adminModels.test.js:299-323` 发布/停用补 `confirm('发布模型',{confirmButtonText:'提交审核'})` + `success('已提交发布审核')` / `success('已提交停用审核')` + 停用文案 `not.toContain('**')`（aa7d251 防回归）+ 停用默认模型含「停用生效后将同时取消其默认标记」（md §二.3.5/§二.3.7）；`modelConfigEditDialog.test.js:395` confirm 含「客户端将无法获取」+'连接信息变更'、`:425` '类别变更'、`:456` 加【关闭】、`:462` 加「连通性验证成功（120 ms）」、`:341` 加 `success('已保存，正在验证连通性…')`（md §三.5/§三.6/§三.4.2）；`expertEditor.test.js:272` 加 `warning('请先补齐必填项')`（md 专家 §三.7）；`modelPresets.test.js` 补上下文档位 [8K…1M] 与提供商 8 项顺序与 md §三.2 逐一相等（label 空格差异记代码缺陷 K-24）；`runtimeSpecMock.test.js:103-114` 持久化改表驱动覆盖 update/delete/assign/apply/unassign 五写点。
- **T44** 治理 · `govSeedRefIntegrity.test.js:27-64` 只钉 5 行 → 遍历审核中心全部行 + 我的申请全部行 name↔实体名（kind→getter 表，TOOL 按 subType 分 MCP/API）。
- **T45** 连接器 · `adminMcp.test.js:181-206` 三态 `toContain` → `toEqual` 精确集合 + 审核中编辑 tooltip（md §二.3.1 L82-86/§二.3.3 L103）；`:216-261` 发布/撤回/停用补 confirm 三参逐字 + toast + 发布后状态即时替换（md §二.3.4-§二.3.6；能红验证：删 `loadPubSummary()`）；`:330-339` 验证补三种 toast + 行内 HealthTag + 时间（md §二.2 L64-68）；`HealthTag.test.js:36-42` it.each 钉「连接正常/连接异常/未探测」字面；`ApiEditorReadWriteError.test.js:112-122` 保存失败追加抽屉未关、输入保留、`msg.error`（md API §三.7 L189）；`bizSystemMock.test.js:48-53` `some` → `every` + `not.toContain('biz_2103')`；`BizSystemEditorSkills.test.js:243-252,227,183-206` 删除断「客户记录」消失 + 空态 + `success('已删除')`、`warning('请输入技能名')`、「已创建，已在新标签打开编辑器」；`connectorEditorsPrototypeLayout.test.js:407-425` Bearer 加「Authorization: Bearer」前缀断言（`ApiEditor.vue:635`）。
- **T46** 共享 · `AdminRail.test.js:269-277` `commands` 改 `toEqual(['changePassword','logout'])`。

## 六、补真实挂载冒烟（页面级全桩化 → 至少一条真 Element Plus 挂载）

背景：2026-09-11 分页条 TDZ 让 15 页白屏而 1838 条全绿。本轮实证（连接器组）：`adminMcp/adminKnowledgeBase/knowledgeSourceList` 局部 import 真 ListPagination（局部绑定优先于全局桩）**能拦**；`adminPositionsOps/adminRoles/adminUsers` 把 ListPagination 桩成空 div、`adminApisPaging` 根本不挂载、`AdminBizSystems` 无测试（62545c6 白屏即实例）**拦不住**。

- **T47** 组织 · 新建 `adminUsersSmoke.test.js` / `adminRolesSmoke.test.js`：`createApp(页面).use(ElementPlus)`，只 mock api，不桩任何组件；断言 mount 不抛、页头文案在、`.el-pagination` 在、行文案含种子用户名/角色名、`console.error` 零调用。
- **T48** 岗位 · `adminPositionsOps.test.js` 补一条真实 ListPagination/ListStates 挂载（另起 describe 不注册桩）：不抛、「共 N 条数据」、跳页框回填当前页。
- **T49** 技能 · `adminSkillsUnified.test.js` ①桩换 `adminMcp.test.js:74-106` 的行渲染桩（或 helper），补「操作列按三态出按钮 + 编辑禁用 title「审核中不可编辑」+ 发布置灰缺项 title + 删除 title「删除前需二次确认」」（md 技能 L61-65）与「右钉列只有操作一列」（fe11191「三列看不见」防回归，el-table-column 桩透出 fixed）；②真 EP 挂载冒烟（不桩 el-*，`console.error` 零，探针「技能」「平台全部技能 —— 通用技能 / 岗位私有 / 市场技能 统一管理」「搜索技能名称或描述」「最新版本」）。
- **T50** 专家 · `adminExperts.test.js` 真 EP + 真 ListPagination 冒烟（断言「经营分析专家」+ `console.error` 零 + textContent 不含孤立 `>`，守 K-11）；`adminModels.test.js` 同（「DeepSeek」+「连接正常」）；`expertEditor.test.js` / `modelConfigEditDialog.test.js` 各一条真 el-drawer/el-form 挂载（标题「新建专家」/「接入模型」+ footer 按钮）。
- **T51** 连接器 · 新建 `adminBizSystems.test.js` 首条冒烟（mock admin 回 3 行，不桩局部组件，mount 不抛且 3 `.t-row` + `.list-pager`）；`adminApisPaging.test.js` 改造为真挂载 `AdminApis.vue`（原用例测的是自己复刻的 `useGroupPaging`，从不 import 页面——「改动那边时这里应同步失败」是假的）。

## 七、补齐缺口用例（md 规则 / 踩坑防回归 / 新增写点 / 无测试文件）

每条用例守一条 md 规则，用例名写「什么情况 → 期望什么（md §节）」。「待代码修后落地」的用例（现状必红）不写、不 skip，列入代码缺陷清单。

- **T52** 组织 · `adminUsers.test.js`：【更多】菜单顺序/分隔线/危险样式/title「删除前需二次确认」+ 点项触发确认框 + ▾/▴ 翻转（md 用户 §二.2.1 L61-62、§二.2.4 L87-88；3cc4591 改码未改测）；进行期间不可重复点击（§二.2.1 L63-64）；搜索 300ms 防抖回第 1 页 + 筛选切换立即重查（§一.2 L20-21）；失败文案「删除失败」「重置失败」+ 显示名为空用用户名（§二.2.5 L99、§二.2.6 L111、L95/L106）。`adminRoles.test.js`：页面权限列聚合「√ 管理端（岗位）」「√ 用户端」「未开通任何页面」、「N 个用户」、搜索仅【查询】/回车生效、权限树加载失败（md 角色 §二.1 L40-41、§一.2 L20、§一.3 L31）、删除失败文案（§二.4 L87）。`userPositionEditDialog.test.js`：forceSave=true 未变化仍保存并 emit saved / false 直接关窗（md 岗位管理 §4.3.3）。`userEditor.test.js`：rules（用户名必填 3–32「请输入 3–32 个字符」、显示名「请输入显示名」、邮箱「请输入有效邮箱」；编辑态无 username 规则）、重开清空、保存失败「保存失败，请重试」（md 用户 §三.1/§三.3/§三.5/§三.6）。`RoleEditor.test.js`：角色名「请填写角色名称」maxlength=64、分组联动/部分选中 data-ind、重开清空、保存失败（md 角色 §三.1-§三.4）。`adminUserMock.test.js`：写点落盘（createUser/deleteRole 后 localStorage 快照 v=2）、坏形状回种子（13 用户/5 角色）。
- **T53** 岗位 · `SampleTaskPrototype4B.test.js` Stage 组：缺指令红标、软上限 20「已达 20 条任务上限」、默认选中第一条（09-10 S2）、脏检查 confirm「有未保存的修改，切换将丢弃。继续？」、启停 toast「任务已停用」（md 岗位 §7.1 L371-388，代码已实现）。`adminPositionsOps.test.js`：列头「最近更新时间」toggleSort → sort asc/desc、listPositions 拒绝 →「加载失败」+【重试】优先于空态、草稿【删除】title「删除前需二次确认」（md §一.3 L29、§二.2 L53、§二.3.1 L62）。`positionAgentTab4C.test.js`：AGENT_MAX 20「已达 20 个上限」、删除确认「删除该 Agent 后会解除其技能关联，技能本身不会被删除。确认删除？」+「Agent 已删除」、「Agent 已保存」、移除确认「仅解除技能与当前 Agent 的关联，不删除技能本身。确认移除？」（md §6.1-6.4）。`positionDataTableStage.test.js`：唯一 ID 单选互斥（DossierCatalogGrid，md §4.2.2 L282）、【取消】toast「已取消未保存修改」（§4.2.1 L275）。`positionDetailTabs.test.js`：dirty 文案「有未保存的修改」、已发布顶栏显版本号/草稿不显（md §1.2 L144-145）。`adminPositionAssignments.test.js`：审批页签点「提交时间」→ sortDir=asc 且 page=1，再点回 desc（md 岗位管理 §4.1 L78；90ee54e）。mock 持久化：`sampleTaskMock.test.js` persist v4 读回 + v3 快照丢弃回种子，`positionMock`(v4)/`positionApplicationsMock`(v2)/`positionAssignmentMock`(v1) 各一条读回。无测试文件：`PositionPersonaTab`（描述空→【AI 生成】disabled+title「请先填写岗位描述」、500ms 后 toast「已生成示例问题」「已生成岗位 SOP」、示例问题占位「如：帮我分析本周经营数据」；md §2.4/§2.5）、`PositionIntakeTab`（10 个置灰+副题「最多 10 个」、抽屉标题、空选项 toast「请至少填写一个选项」、「采集字段已保存/已删除」、删除确认「删除该采集字段？删除后员工领用时不再采集该项。」；md §3）、`PositionBusinessSystemTab`（空态「暂无引用的业务系统」、弹窗只列已发布未引用+搜索、【确认引用】写 businessSystemIds+「引用成功」、只读隐藏【＋ 新业务系统】、行内仅【查看】开抽屉；md §8.3/§11）、`userPositionMock`（listPositions 只含 published；bindPosition 写点读回）。
- **T54** 技能 · `adminSkillsUnified.test.js`：版本管理适配器（submitGate 市场技能缺分类文案 / title「版本管理」/ delistTerm「禁用」/ activeLabel「已启用」/ lastActiveTip；md §四）、「最近更新时间」toggleSort → sort asc + 箭头 ↑（md L47/L54）。`unifiedSkillMock.test.js`：sort=asc 分支；listSnapshots 归一化 sizeBytes/publishedBy/releaseNotes/delistedAt（09-09 P1 防回归；sk_301 v1.2.0 sizeBytes=round(18.6*1024)…）；persist v3 落盘 + resetModules 读回 + version≠3 回种子。`skillPublication.test.js`：第 8 态 PUBLISHED_DELISTING（pendingAction:'DELIST'）派生 / isLocked / stateActions=['withdraw']（md L96/L120/L131）。`adminSkillEditPage.test.js`（T32 内）：保存门 4 条 warning（名称>64 / 分类空 / 描述>2000 / 示例问题>60；一览表 L58/61/62）+ 只读态 ?view=1。新建 `utils/__tests__/iconDisplay.test.js`（emoji→false；http(s)、/path、data:image/→true；9 页 + IconField 依赖）。
- **T55** 专家 · `adminModels.test.js`：发布审核中行「查看/编辑(禁用,tooltip「审核中不可编辑，如需修改请先撤回提交」)/撤回」（md §二.3.1/§二.3.3）；撤回/设为默认/删除三动作（md §二.3.6「已撤回」/§二.3.8「已设为默认模型」/§二.3.9「配置与密钥将不可恢复」「已删除」）；验证完成 toast「检活完成 · 连接正常」/「连接异常」+ 重拉（§二.3.4）。`modelConfigEditDialog.test.js`：真 ElForm 下 md §三.8 四条校验（base_url「必须以 http:// 或 https:// 开头…」、contextWindow 512、extraBody "[1]"、APP_ID_SECRET 缺 app_secret）；watcher immediate 防回归（5303c7c：以 visible:true 初始化 → name 已回填）。`adminModelMock.test.js`：createModel baseUrl/contextWindow/model 空 rejects field；驳回（PUBLISH→DRAFT / DELIST→仍 PUBLISHED）；默认模型停用过审 → isDefault=false；审核中/已发布 deleteModel rejects「仅未发布状态可删除」；种子 Kimi K2 verify FAILED AUTH_FAILED、改 baseUrl 重验 SUCCESS（md §二.4/§二.3.7/§二.3.9/§二.5）。`expertEditor.test.js`：职责描述 2001 字 →「职责描述不超过 2000 字」不发 createExpert（md §三.2）。`adminExperts.test.js`：列头 toggleSort → sort asc + ↑；分类「法律」+状态「未发布」→ listExperts {category,status}；分类下拉 8 项 = getFieldOptionNames('expertCategory')；加载失败【重试】（md §二.2/§一.1/§一.3）。`domainExpertMock.test.js`：已发布专家(204)撤回新版审核 → 仍 published、pendingVersion 清空、latestVersionLabel v1.1.0（md §二.3.4）；删除用例改用草稿 203。`runtimeSpecMock.test.js`：同一岗位恰属 1 个规格不变量（md §一.4）；停用用户 assign rejects「已停用，不能配置规格」；allowUserApply=false apply rejects「该规格当前不开放用户申请」；keyword「客户成功岗」命中「轻」。新建 `adminRuntimeSpecs.test.js`（09-11 改 4 次零测试）：真挂载冒烟（标题「运行规格」+ 汇总行 + 时间列 th col-nowrap）、默认规格【删除】disabled、有岗位规格删除 → alert「…请先解除岗位配置」不调 delete、可删 → confirm md §三.3.6 文案 + 「规格已删除」+ 重拉、maxLifetimeHours=0 →「不限」、双空态。新建 `runtimeSpecEditor.test.js`：新建默认值 2/4/20/10/20/0 + 上限提示「CPU 32 核、内存 128 Gi、临时存储 500 Gi」、查看默认规格提示条 + allowUserApply 禁用、footer 三态按钮组、「规格已创建」/「规格已保存」+ emit saved、{field:'name'} → 就地红字「规格名称已存在，请换一个」、limits 失败 → 加载失败+重试（md §四.3-§四.10）。新建 `runtimeSpecUserDialog.test.js`：岗位继承行「按岗位生效」无解除 / 个人配置行「解除」/ 待审批行「撤回」+「待审批」；添加 2 人 → 「已为 2 个用户配置规格，立即生效」+ emit saved；撤回 →「撤回规格申请」→「申请已撤回」；解除 → 含「回退到岗位规格或平台默认规格」→「规格配置已解除」（md §三.3.4/§三.3.5）。
- **T56** 治理 · `userSkillReviews.test.js`：尺度/状态下拉 change 即时刷新回第 1 页 + 查询后行内容变化（md §三 L52-53）。`myApplicationsMock.test.js`：撤回非 PENDING → 409 状态不变；重提 PENDING/APPROVED → 409；重提 WITHDRAWN → PENDING 清审核人/时间（md 我的申请 §五 L80-83/§七 L99；aa7d251）；submitApplicationRow 覆盖在审行 / withdrawApplicationRow 行保留置 WITHDRAWN 审核人「—」。`reviewsMock.test.js`：submitReviewRow 同 type+refId 在审覆盖不建新行、新 id 避开种子 1..9、cancelReviewRow 无匹配静默（md 审核中心 §六 L92）。新建 `unifiedReview.test.js`（368 行零测试）：操作列 ['查看','驳回','通过'] 驳回 danger；通过 VERSION_PUBLISH → confirmApproveReview + 「已通过审核」+ 重拉；DELIST → 「已通过停用申请」；驳回弹窗 → rejectReview(row,'x') + 「已驳回审核」；POSITION 行快照缺失 → warning(SNAPSHOT_MISSING_HINT) 接口不调（aa7d251）；SKILL【查看】→ router.push SysConfigSkillView（md §3.1/§5.1/§5.2/§七）；`reviewRejectDialog.test.js`：空值「请输入驳回原因」+ focus、maxlength 500。新建 `myApplications.test.js`（407 行零测试）：四态操作列、objectDeleted【查看】disabled、【撤回】type warning（5585a2b）、撤回 confirm+「申请已撤回」、详情底栏按态、重提 alertResubmitSuccess、REJECTED 审核结果 tooltip（md 我的申请 §3.1/§四/§4.1/§4.3/§七）。新建 `fieldManagement.test.js`（393 行零测试）：分组折叠、草稿行数/maxlength 30、空值/重名「选项值不能为空」「选项值不能重复」不调 save、成功「字段选项已保存」、失败弹窗保持开、取消重开回保存态、「暂无选项」（md 字段字典 §三/§五/§2.1）。新建 `adminFeedback.test.js`（403 行零测试）：终端标签/附图「▧ N」/「—」、详情弹窗仅【关闭】、点附图 fetchFeedbackImageBlob + img alt、blob reject → `.fb-image-error`、重拉后 revokeObjectURL + 弹窗关、空态「暂无用户反馈」（md 用户反馈 §三/§四/§五/§七）。新建 `adminLoginLogs.test.js`（薄）：双列排序默认 ↓/↓，点登出 → {sortField:'logoutAt',sortDir:'desc',page:1}；ONLINE 行登出「—」标签「在线」（md 访问审计 §3.1）。新建 `utils/__tests__/govDialogs.test.js`（T40）：confirmApproveReview 标题/按钮/停用文案/resolve-reject、confirmWithdrawMyApp 文案、alertResubmitSuccess 文案+吞 reject（md 审核中心 §5.2 L77；我的申请 §4.1 L51、§4.3 L66）。新建 `govObjectDetail.test.js`（三页共用）：snapshotGate=true 且快照 null → 吸底栏只剩「关闭」+ 提示；=false 全出；kind 与 visible 同 tick → 子编辑器收到 load（5303c7c）。mock restore 形状守卫各 1 条：fieldDictMock / reviewsMock / myApplicationsMock / skillReviewMock。
- **T57** 共享 · 新建 `components/admin/__tests__/listPagination.test.js`（ListPagination 零单测；09-11 三段式 + TDZ + 跳页框回填全无守卫）9 条：真实挂载不抛 + 「共 23 条数据」+ 跳页框初值 "1"；total=0 不渲染；动态 9 →「9条/页（自动）」+ 四档、选 20 emit、重复不 emit；pageSize 20 无自动项；跳页夹取/非法回填；props.page 变化框跟随；页码窗口 [1,…,9,10,11,…,20] / [1,2,3,4,…,20]；‹ › 边界 disabled、点当前页不 emit；联动 useAdminList 选 20 → fetcher {page:1,size:20}。`useAdminList.test.js`：watch(pageSize) 回第 1 页重拉、首拉前改 pageSize 不取数、服务端分页越界一步钳到末页（fetcher 恰 2 次）；`useDynPageSize` 防抖分支 1 条。`mockPersist.test.js`：`?resetMock=1` 分支。`tableFixedColumnOpacity.browser.test.js`：加 `body.admin-scope` + admin-shell.css 变体（18 个后台页实际走的规则未在闸内）。新建 `stores/__tests__/theme.test.js` 3 条、`composables/__tests__/useConfirm.test.js` 3 条。
- **T58** 连接器 · 新建 `adminApis.test.js`（AdminApis.vue 934 行零挂载；与 T51 合一）：三态按钮集合逐字 + 审核中编辑 disabled tooltip + 非 HEALTHY 发布 disabled tooltip「连通性验证通过后才可提交发布」；发布 confirm('发布 API','提交审核') → 「已提交发布审核」；待审停用撤回含「已发布」；删除系统 apiCount>0 disabled tooltip / =0 confirm 逐字；零系统空态 + openCreateApi warning；「没有匹配的 API」；「该系统下暂无 API · 点「在本系统下新建 API」添加」；刷新 → 「检活完成 · 连接正常」+ HealthTag；分页 9 系统/页 3（md API §一.1/§二.1-§二.5/§四）。新建 `adminBizSystems.test.js`（T51 冒烟 +）：三态按钮、审核中编辑 disabled、发布/停用/删除 confirm 逐字 + toast、待审停用撤回含「已发布」、跨页排序（09-09 P1）、`?view=biz_2` 只读深链、「没有匹配的业务系统」（md 业务系统 §一.1/§二.1-§二.3）。新建 `adminConnector.test.js` / `adminKnowledgeBaseShell.test.js` 容器页：tab 缺省/非法/指定/切换保留 query/keep-alive（MCP md §一.2 L20-24；知识库 §二.2 L42-43）。新建 `api/__tests__/apiConnectorMock.test.js`（768 行 v2 零测试）8 组：连接配置变更验证失效、publish 非 HEALTHY 拒、状态机双向、系统名称/描述校验 + 重名排除自身、含 API 拒删、listApis keyword every + state、出参脱敏 valueMasked/clientFill/留空保留、persist 每写点 +1 + restore 形状 + CHECKING→null（md API §二.3/§二.4/§二.5/§一.1/§三.3）。`adminMcp.test.js`：删除流程 4 条（confirm 逐字/被引用软删/末页删回上一页/失败「删除失败」；md §二.3.7）；状态筛选 change 回第 1 页、空态两种文案、空工具 tip、引用情况「暂无引用」/「N 个技能引用」弹窗、无工具发布 warning 逐字、时间列排序 sort asc（md §一.2/§一.3/§二.1/§二.3.4）。`knowledgeBaseList/knowledgeSourceList`：URL query 状态保持（28ee4b0/547d3e5：kw/st/p ↔ srcKw/srcSt/srcP 互清；数据源文件补 vue-router mock）；图标列（aee4775）+ 点名称 审核中→view/其余→edit（md §三.2 L64）。`knowledgeBaseMock.test.js`：icon 落库/读回/保留/清除；名称>100/描述>500/缺 scopeRefId/同类第 6 个「上传 数据源最多引用 5 个」/kbType 不可改（md §三.3.1-§三.3.2）；kb_7 发布拒「请填写知识库描述」+ 无文档拒（§三.6）；remove 409 + 引用源仍在、search errors/topK/rank（§三.4.3/§三.7）；API/MCP 边界 + API KEY「请选择位置」（§六.1/§七）；persist v7 读回/旧 v 回种子。`utils/knowledgeBaseMeta` 新增用例：API_DEFAULTS/MCP_DEFAULTS 鉴权方向（09-09 P0 防回归）。`dataTableMock.test.js` persist；`dataTableTypes.test.js` slotRole 三条文案；`docMeta.test.js` fmtRelative 边界。`defValidate.test.js`：Header 名 128/字符集、新配置密钥留空/authConfigured 放行（MCP md §三.4.1）。`mcpConnectorMock.test.js` 8 组（测试连接不改列表状态、updatedAt 规则、出参脱敏、authConfig 留空/换类型/none、env 按 KEY merge、listMcp 排序/keyword/NOT_PUBLISHED 含 DELISTED/分页、新建 UNKNOWN、deleteMcp/withdraw/getMcpServicePublishStatus/review(reject)）+ persist v5 读回/回种子/坏形状。新建 `mcpEditor.test.js`：切传输清对侧、粘贴导入六条、保存 toast 三种、拉取「已拉取 N 个工具」+ 展开 + 【收起工具清单】、超时 min/max/step、Command 五项不追加存量（md MCP §三.2/§三.3.1/§三.6/§三.8）+ T37 迁入。三编辑器 watcher immediate 防回归（McpEditor/ApiEditor/BizSystemEditor，5303c7c；mountEditorOpened visible 初值 true）。`ApiEditorReadWriteError.test.js` **改名 `ApiEditor.test.js`** 补保存校验 5 条（六项 data-error + warning「请先修正标红项」/ ftp:// 仅 url 红 / Bearer 空「Token 不能为空」/ 编辑成功「API 已保存」+ saved + 关 / 新建「API 已创建」；md API §三.7/§三.2）+ 新建默认 不鉴权/GET/启用 + AI 生成 3 条/「生成中…」/源空置灰 + ParamRowsEditor readonly。`SchemaFieldEditor.test.js`：空态「暂无字段，可不配置（留空表示不约束）」、删父级联+确认、request 形态列（md §三.5）。新建 `providerSystemEditor.test.js`：名称/描述空标红、65/2001 字、field=name 回显、trim payload + saved、编辑回填 + 标题（md §二.5 L78）。`bizSystemMock.test.js`：名称空/65/同名 rejects + 编辑自身不重复、rejectBizSystem 双向（md §三.2/§三.7/§二.3）、persist 10 写点/坏形状/bizSeq。`BizSystemEditorSkills.test.js`：专属技能区仅编辑态反面、业务页四条（计数/20 上限/删除确认/空态）、toast「业务系统已创建/已保存」+ 失败回显、空白页丢弃、自动化操作占位（md §三.3-§三.5/§三.7）、新建态主按钮「保存」查看态只【关闭】（3cc4591；§三.1 L68）。

## 八、明确不测（写明理由，下次不再审）

| 文件 | 理由 |
| --- | --- |
| `components/admin/ListToolbar.vue` | 两插槽纯壳，已在 adminMcp/userSkillReviews 真实挂载 |
| `components/admin/FieldHelpLabel.vue` | 纯展示（label + tooltip） |
| `components/admin/UserSkillAuditTag.vue` | 纯壳，映射在 `userSkillAuditMeta` 已由 riskSettingsDrawer/skillReviewMock 覆盖 |
| `utils/skillTerms.js` | 纯文案常量；`triggerHint` 死导出提请随死码清理删 |
| `components/position/dossier/DossierStrategySelect.vue` | 纯下拉壳，四选项文案并入 DossierRuleListEditor 断言 |
| `components/position/dossier/DossierRuleListEditor.vue` | 已由 positionDataTableStage 真实挂载覆盖 |
| `components/position/PositionAgentSkillTab.vue` | 已由 positionAgentTab4C 经父页真实挂载覆盖 |
| `components/PositionInfoCard.vue` / `PositionAvatarStack.vue` | 死代码：仅 Chat.vue 引用，前台已封存 |
| `components/biz/BizCredentialPanel.vue` / `BizCredentialHostDialog.vue` | 员工端凭证托管，`api/myPosition.js:45-63` 无 mock，管理端 md 明写「管理端不录入登录凭证」 |
| `components/intake/*`、`PositionPickerDialog`、`RebindConfirmDialog` | 员工端换绑流，规则已在 intakeForm/rebindGate util 测试；去留随 J2 |
| `utils/richText.js` | XSS 白名单净化属安全规则非产品规则（工程大原则：生产级安全默认不做）；仅员工端三页可达 |
| `views/admin/AdminComingSoonPlaceholder.vue`、`views/FrontRuntimePlaceholder.vue` | 占位页 |
| 09-11 列宽/圆形图标/不换行/分页条位置等 CSS 修复 | jsdom 布局断言恒 0，不值得用单测钉像素；要守走 `*.browser.test.js` 或截图 |
| `AdminLoginLogs.vue` 除排序外 | 薄壳 |

---

## 九、代码缺陷清单（用例=代码≠md，或代码自身问题；本轮不改代码，交负责人）

按模块，K 编号。「用例侧」列说明修好后要做的测试动作。

**岗位 · 自动化任务 §7（今日 md 更新 vs c40c606 代码）**
- **K1**（中）`SampleTaskEditor.vue:83,531,505-511` + `sampleTaskMock.js:284/314` · preKick 默认 false，md §7.3 L398「默认开启」；新建态不复位。用例侧：修后补「默认勾选 + 勾选态文案」。
- **K2**（中）`SampleTaskEditor.vue:647-651` · 勾选/关闭两态提示拼成一句常显（md L399-400 分态）。用例侧：修后补「取消勾选 → 关闭态文案 + payload.preKick=false」。
- **K3**（中）`SchedulePicker.vue:250-310` · prototype=true 缺【＋ 添加时间】多时间点、每间隔无起始时刻、三模式无起止日期（md §7.3 L396-397；起止日期只活在 prototype=false 分支）。
- **K4**（**高**）`SampleTaskEditor.vue:331-344 buildSchedule()` 不带 scheduleMode/periodicPreset/intervalCount/intervalUnit → mock 回默认；保存「每 3 小时」落 intervalCount=1，保存「单次」scheduleMode 落 PERIODIC 重开回「按周期」。**demo 主链路，单测全绿**。用例侧：修后补 `INTERVAL create → summary「每 3 小时执行一次」且读回三字段`、`编辑态切每间隔 3 小时保存 → payload.schedule 三字段`。
- **K5**（中）`sampleTaskMock.js:55-80 computeNextRunTimes` 不认 INTERVAL_*，预览按每天推算（md §7.3 L401）。
- **K6**（中）`SampleTaskEditor.vue:469,474` toast「样例已保存/已创建」≠ md §7.7「样例任务已保存/已创建」；`PositionSampleTaskStage.vue:220` 删除 toast「任务已删除」≠「样例任务已删除」；`:204-206` 删除确认文案 ≠ md §7.8「删除后该任务将不可恢复，确认删除？」。
- **K7**（中）`SampleTaskEditor.vue:661-673` 提示词引导文案缺后半句、计数「字数: N」≠ md「已输入 N / 8000 字」、无 8000 上限（md §7.4）。用例侧：4B:244-250 随改 + 8001 字被拦。
- **K8**（低）`SampleTaskEditor.vue:686,713,747` 占位「搜索工具名称 / 类型」「搜索平台技能名 / 描述」≠ md「…或…」；工具空态 ≠「暂无引用工具，点击下方添加」；技能无空态（md §7.5/§7.6）。用例侧：4B:256/293 随改。
- **K9**（低）`SampleTaskEditor.vue:403` validate 错误文案仍叫「请填写详细说明（这件事要怎么办）」（字段已改名提示词）。
- **K10**（中）`PositionDataTableStage.vue:13,263-288` 新建档案走【取消】【下一步】弹窗 + toast「工作档案已创建…」——该口径 e705dfb（09-08）已从 md 删；现行 §4.1 L259 右侧弹表单、§4.2 只有【保存】【取消】。用例侧：`positionDataTableStage.test.js:177-194` 随改。
- **K11**（低）`positionModel.js` 发布前检查清单文案 ≠ md §9.2 L494-497（「岗位名称：已填写"XXX"（✓）」等）。
- **K12**（低）`PositionDetailTabs.vue:553-556` 加载失败态缺「加载失败」字样与【返回】（md §12 L526）。
- **K13**（低）`PublishCheckDialog.vue:9,25`、`VersionDrawer.vue:50,86` 注释「修订版本」过期（md §3.7 已是「修复更新」）。

**组织**
- **K14**（中）`UserEditor.vue:165-168` 新建态「初始角色」缺 md 提示「选择一个或多个角色」（md 用户 §三.2 L149；Q439 已裁按 md）。
- **K15**（低）`adminUserMock.js:160` 文案「用户名 3–32 位」≠ md「请输入 3–32 个字符」。
- **K16**（低）`AdminRoles.vue:205,212,217,231` / `AdminUsers.vue:290` 时间列硬编码 165 无 col-nowrap（全站 COL.TIME=168；09-11「硬编码列扫不到」漏网形态）。

**技能**
- **K17**（中）`SkillCreateDialog.vue:151-152` 两条守卫文案 ≠ md L153「请选择技能类型、技能分类并填写创建内容」（Q207 裁 md）。用例侧：skillCreateDialog:117 随改。
- **K18**（低）`AdminSkillsUnified.vue:399-402` submitGate 多一句「请到技能编辑页…」（md L231；Q216）。
- **K19**（低）`unifiedSkillMock.js:103-104` 种子 sk_302 在审 v1.2.0 < 线上 v1.4.0（md L226-229 自动递增）。修种子须 bump v3→4。
- **K20**（低）`unifiedSkillMock.js:393-424` updateSkill/saveSkillFile/setSkillCategory 对审核中技能无写守卫（md L120/L141；锁只在 UI）。

**专家 · 模型 · 运行规格**
- **K21**（低）`AdminExperts.vue:329` el-table 起始标签后孤立 `>` 字符（0de8fe9 引入）。
- **K22**（中）`AdminModels.vue` 验证中文案「正在连接模型」≠ md §二.3.4「正在验证…」；`:110`「点击验证连通性」≠ md「尚未验证过，点击发起验证」（Q148 同句裁 md）。
- **K23**（中）`ModelConfigEditDialog.vue:569` 能力空态「尚未识别能力，请重新验证」≠ md §三.4.1「未探测到已支持能力」（Q244 已裁 md）；`:596-640` 缺 md §三.4.2 固定【⌁ 验证连通性】按钮。
- **K24**（低）`modelPresets.js` 提供商 label「智谱GLM」「月之暗面Kimi」「阿里Qwen」「小米MiMo」无空格 ≠ md §三.2。
- **K25**（低）`adminModelMock.js:12-13` 头注「发布/撤回不改 updatedAt」与 L401/427 实现相反；`domainExpertMock.js:169-170` persist v3 注释描述的 203 变更不存在。
- **K26**（中，跨模块）`reviewsMock.approveReview` 只改审核行，不记 reviewer，不联动专家/模型/知识库状态与我的申请行（md 审核中心 §5.1 L71/§5.2 L79-82；专家 §二.3.3；模型 §二.3.5）。demo 边界待定，见 J12。
- **K27**（低）`domainExpertMock.deleteExpert` 无状态守卫（已发布可删；md 专家 §二.3.7）。
- **K28**（中）`runtimeSpecMock.js:49-64` + `RuntimeSpecEditor.vue:101-122` 校验文案 9 条 ≠ md 运行规格 §四.10（「请填写能力边界说明」「不超过 200 个字符」「CPU须不小于 0.5 核，并按照 0.5 递增」「须为不小于 1 的整数」「非负整数小时，0 表示不限」「不能超过平台单实例上限 32核」「默认运行规格用于平台兜底，不能删除」）。用例侧：runtimeSpecMock.test.js:89,100 随改 + 补 3 条。
- **K29**（中）`RuntimeSpecEditor.vue:124-146` 缺 md §四.8.2「运行配置变更」确认、§四.3 岗位切换提示、§四.8.3 待审批阻断、§四.1 放弃确认。
- **K30**（低）`AdminRuntimeSpecs.vue` 默认规格删除无 tooltip、无【清空筛选】、列格式「2c / 4Gi」≠ md「2 核 / 4 Gi」；`RuntimeSpecUserDialog.vue` 缺「已驳回/已撤回」态展示（md §三.3.5）。

**治理**
- **K31**（中）`RiskSettingsDrawer.vue:133 起` 未渲染 md §7.2 L156-160 三套尺度适用说明（Q416 09-08 裁决补进 md）。
- **K32**（低）`FieldManagement.vue:115-131` 删除草稿行加了 confirm，md §三 L48「仅从当前编辑草稿中移除」无确认——代码超出 md。
- **K33**（低）`skillReviewMock.js:349` 注释「选择即时生效」过期。

**知识库 · 连接器**
- **K34**（**高**）`mcpConnectorMock.js:570-581` 停用直落 DELISTED（列表显「未发布」），md §二.3.6 L135 要求进「审核中」；`AdminMcp.vue:400` toast「已提交停用审核」但读回即「未发布」。`adminModelMock` 已用 pendingAction 正确实现可照抄。用例侧：修后补状态机三段。
- **K35**（**高**）`ApiEditor.vue:428-434` 抽屉顶部提示「1 个 API 对应 1 个可被技能引用的工具」按 md §三.1 L102 应存在（Q114 已裁按 md），用例 `connectorEditorsPrototypeLayout.test.js:363` 反向断言其不存在。用例侧：修后反转为「三态均含」。
- **K36**（中）`ApiEditor.vue:11,73,442` API ID 未只读展示（Q113 裁 md）；名称上限 128 ≠ md 64 且 validate 不查长度。
- **K37**（中）`sourceMappingEditor` 新建示例组缺 md §六.2 L316 的 `enabled`(boolean) 子字段；`knowledgeBaseMeta.js:145-161` 注释过期。用例侧：`sourceMappingEditor.test.js:172-173` 随改。
- **K38**（低）`SchemaFieldEditor.vue:258-286` 查看态编辑入口是「置灰」非 md L170「隐藏」。
- **K39**（低）`McpEditor.vue:788` stdio Command 占位「选择启动命令」≠ md「npx」；`:888` Bearer 占位 ≠ md「粘贴 Bearer Token（不含 Bearer 前缀）」；切「无鉴权」不清本次填写凭证（md L272）。
- **K40**（低）`knowledgeBaseMock.js:300` 允许引用已停用数据源（md §三.3.2 L94 09-09 拍板，UI 已拦 mock 未拦）；UPLOAD 源 mock 零校验（md §五.1 三必填）。
- **K41**（低）`BizSystemEditor.vue:615,634` 按钮「+」半角 ≠ md「＋」；`bizSystemMock.applyBizPayload:193-197` 不丢弃整行空白业务页（编辑器丢、mock 不丢）；`defValidate.js:8-9`、`mcpConnectorMock.js:254-258`、`AdminMcp.vue:476-478`、`AdminModels.vue:3-15,577` 注释过期。

**共享**
- **K42**（低）`AdminRail.vue:171-176,240-242` 头像菜单「退出登录」（守卫立即转回）与「修改密码」（`api/auth.js` 真实 POST 无 mock → 404 英文错误）在 demo 是死操作，无 md。
- **K43**（中）`.vue <style>` 内 46 处无 fallback 引用未定义令牌（`--c-text-base` ×34 散在 18 页、`--bg-base` ×8、`--fw-normal` ×3、`--c-border` ×2、`--lh-normal`、`--c-bg`）——真实令牌漂移；`connector.css` 15 个死类 / `position-detail.css` 4 个；`AdminPositions.vue:611,625` 仍「时间 + 操作」双右钉（09-11 技能页三列被遮同款隐患）。

## 十、待裁决清单（J 编号，请负责人批注）

- **J1**（**高**，四组同报）**状态独立列**：09-11 拍板「按设计图拆出独立状态列（7 页）」已落地（38c3567），但 8 份 md（岗位 §二.1 L38、技能 L40、专家 §二.1、模型 §二.1、MCP §二.1 L45、API §二.1 L32、业务系统 §二.1 L27…）仍写「不设独立状态列」。用例侧：adminMcp/adminPositionsOps 已按拍板断言，专家/模型/技能用例未守任何一边。**建议**：确认拍板覆盖 md → 回改各 md 一句「状态作为独立列紧跟名称列之后」，然后补专家/模型/技能三页列序用例。本轮用例不动。
- **J2**（**高**）**员工端残留代码与用例整体去留**：`stores/{chat,session,task,memory,companion}`、`api/{chat,session,task,space,myPosition,myUserPosition,rebind,auth,report}`、前台 14 个 view、`FrontLayout`、`AttachmentCard/MemoryReceiptCard/ExecutionCard/DocDetailDrawer/PositionInfoCard/PositionAvatarStack`、`utils/taskStatus|richText`、`stores/space` 从管理端入口完全不可达（`FRONT_RUNTIME_ENABLED=false`，api 无 mock 分流，无后端），仅手敲 URL 可达；09-11 负责人裁「不用管员工使用端」但未裁代码去留。涉及用例 **9 文件 108 条**（共享组）+ 岗位组 `myPosition/myUserPosition/rebind/userPosition store/useRebindFlow/rebindGate/positionBinding/intakeForm` 8 文件 + `request.test.js` 401/1001 六条 + `EChart.test.js`（仅报表页用）。**建议 (a)** 整体退役（删代码 + 用例）；**(b)** 封存（不删，用例头注加「员工端封存，demo 不可达，仅作代码考古」）。本轮采用 (b) 的头注做法作为临时措施，不删任何文件。
- **J3**（**高**）**「历史后端契约」测试去留**：`skillFiles.test.js / skillFilesExport / skillFilesPlatformCandidate / skillFilesSourceMatrix`（4 文件 30 条，`stubEnv VITE_SKILL_MOCK=0` 断 `/fde/…` URL；`'platform-candidate'`/`'review'` source 零调用方）、`market.test.js`（6/7 函数零调用方）、`adminModel.test.js`（`VITE_CONN_MOCK=0` 端点契约）、`reports.test.js`（被测两页菜单隐藏、api 直连 `/fde/reports` 无 mock、无 md）。**建议**：留档则头注加「历史契约，demo 不可达」（本轮已做）；退役则连 `api/skillFiles.js:40-41` 死分支、`market.js` 6 死函数、报表两页+路由+api 一并删。
- **J4**（**高**）`adminSkillsUnified.test.js:109` `stubEnv VITE_SKILL_MOCK=0` 使 7 条用例断言 demo 永远不走的真实端点分支，mock 路径页面层零用例。改造 = 整文件重写（去 stubEnv、对真实 unifiedSkillMock 断言可见结果），与 J3 同一裁决。
- **J5**（**高**）`SchedulePicker.vue:32 prototype` + `SampleTaskEditor.vue:48 embedded` 开关：false 侧唯一消费方 `views/TaskEditor.vue` 已被占位页替换，非 embedded 分支无人走；死代码 `TYPES/toggleWeekday/多时间/起止日期` 只活在 false 分支。**建议**退役开关、把「起止日期 / ＋ 添加时间」按 md §7.3 搬进三模式（恰好修 K3）；`SchedulePicker.test.js` S6「无月末」护栏、4B:173-178/:421-428 零回归用例随之删。
- **J6**（**高**）`SampleTaskEditor.vue:589-623` 任务名称 maxlength 60 / 说明 200 / 三处占位 ≠ md §7.2「128 / 500 / 如：每日经营分析报告…」——md §7.2 由 df9b90e（今日）「根据前端实现补全」却与代码不符，是 md 笔误还是代码要改？
- **J7**（中）「提示词」是否必填：代码必填（错误文案还叫「详细说明」，K9），md §7.7 必填只列名称+一句话指令，§7.4 无「必填」。
- **J8**（中）md 内部矛盾：岗位知识页签【查看】§5.2 L315「抽屉」vs §11 L519「跳转知识库模块」；技能提交发布 toast L79「已提交发布审核」vs L232「已提交发布 vX.Y.Z，进入审核」；页签 name md `tasks` vs 代码 `sampleTasks`（改名影响深链）。
- **J9**（中）`RoleEditor.vue:132` toast「请先补齐必填项」md 无（Q325 裁「md 措辞为准」，toast 由 7557065 09-08 引入）：删 toast + 用例 :300，或补进 md §三.4。
- **J10**（中）`modelMock/expertMock`「提交停用审核不刷新 updatedAt」：md §二.2「提交审核与撤回…重排；重新验证、停用或设置默认…不改」二义（09-09 A20/Q199 采「停用=不刷新」）。
- **J11**（中）`ChangePasswordDialog.vue`：有校验规则但走真实 HTTP 无 mock，无 md；入口在 AdminRail。①补 authMock 后写用例（规则无 md 出处需补定义）；②随登录退役隐藏入口，标记不测。
- **J12**（中）demo 是否要做「审核中心通过/驳回 → 业务对象状态联动 + 我的申请行联动 + 记审核人」（K26）。
- **J13**（中）**死导出清理批**（用例守着零调用方代码；本轮不删用例、不删代码，等一并裁）：`skillFileTree.js` isPublished/didRequeueForReview/isMdPath（8 条）、`tokenEstimate.js:67-93` routeDescHint/routeTriggersHint（4 条）、`skillPublication.js:141-160` TARGETS/targetLabel/targetActions（2 条）、`mcpImport.js:21-32` suggestCodeFromKey（3 条，且与 md §三.2 L225 相悖）、`mcpMeta.js:43-114` isRedDot/countUnhealthy/fmt*（19 条）、`marketMeta.js:36-75,160-177` + `reviewMeta.js:52-57`（≈8 条）、`modelPresets.js MODEL_PRESETS` + FIELD_TIPS.preset（3 条；Q224 已删预设卡）、`positionModel` 推荐问题固定 4 格（4 条；RecommendedQuestionsEditor 76ed7e8 已删，字段静默透传）、`dossierConfig` checklist/askTier/pendingTtlDays（3 条；md §4 与 UI 均无）、`stores/position.addSkill/removeSkill`（2 条）、`ToolDock` 默认分支 platform 两 tab/FDE 四 tab（3 条；恒传 ADMIN_TOOL_TABS）、`ParamRowsEditor.inDisabled`、`skillTerms.triggerHint`、`bizSystemMock.aiGenerateBizExampleQuestions`。**建议**：一次性清理死码 + 对应用例（约 60 条）。
- **J14**（中）`SkillFocusEditor.vue:86 showClose` 默认 true 分支唯一挂载点传 false；`SkillFocusEditorPackage.test.js` 19 条在 true 分支跑。建议退役开关；本轮未动。
- **J15**（低）一览表/md 未写而代码有的限制：dataTableTypes 唯一 ID 限 TEXT/INTEGER；业务页描述 ≤100；`schema.js FIELD_TYPES` 含 integer（md 五类）；SchemaFieldEditor 列头「参数名/描述/变量类型」vs md「字段名/字段类型/字段说明」；SkillFocusEditor 描述上限 1000 只作用业务系统技能（一览表只有 2000）；`adminUserMock`「不能删除最后一个系统管理员」「用户名已存在」护栏 md 无。补 md 或改代码。
- **J16**（低）`mcpConnectorMock.js:463` MOCK_FAIL_REASON 'CONN_REFUSED…' 不在 `mcpVerify` 错误目录 → 悬浮「错误码：UNKNOWN」（md §二.2 L61）；改常量或目录登记。
- **J17**（低）`McpEditor.vue:164-165` 测试连接失败单行文案 vs md §三.5 L295 标题/正文两段（Q135 已裁 md，涉 09-09 复刻批次 3A）。
- **J18**（低）`ClaimNotesEditor` 满 6 条是「藏按钮」，md §2.3 L188 是「提示"领用页文案最多 6 条"」（warning 不可达）。
- **J19**（低）`FieldManagement` 删除草稿行确认弹窗超出 md（K32）保留与否。
- **J20**（须确认）**新卫生守卫规则**（存量 0 命中，可零误报静态查出）：①`theme.css --col-*` 与 `tableLayout.COL` 同源同值（09-11 令牌漂移教训）；②用 `COL.TIME/STATUS/COUNT/TAG` 定宽的列必须带 `col-nowrap`（54 列 0 命中）；③`assets/*.css` 的 `var(--x)` 剥注释后必须有定义；④`moduleGraph` 扩默认导入/相对路径/动态导入路径存在性（312/67/66 处 0 缺）。**不建议**（存量有命中，先整改）：.vue style 未定义令牌（46 处，K43）、共享 css 死类（26）、el-table 双右钉（1）、`el-table-column` 字面量 width（AdminRuntimeSpecs 9 处）。
- **J21**（低）md 补写建议：分页条「每页条数 10/20/30/50 手动档 + 跳至 __ 页」（09-11 拍板未回写 md）；用户/角色 md 无「用户名唯一」「最后一个管理员不可删」。

---

## 处置结果（2026-09-12 当日执行完毕）

**T1–T58 全部 done**，无 skip。执行由 7 个实施 agent 按文件所有权并行完成，主流程复核后按模块各成一条 commit（`ee97329`…`36ff5a3` 共 10 条：技能定义 / 公共桩 / 组织域 / 岗位系 / 技能系 / 专家·模型·运行 / 治理域 / 共享层 / 知识库+MCP / API+业务系统）。**终点 commit（测试改动）：`36ff5a3`**——下一次 `/test-audit changed` 以此为起点。

**前后对比**

| 指标 | 审前（6483bba） | 审后（36ff5a3） |
| --- | --- | --- |
| vitest 文件 / 用例 | 149 / 1840 | **185 / 2444**（+36 文件、+604 条；三个随机 seed 全绿） |
| browser 文件 / 用例 | 5 / 未跑（themeTokens 会红） | **5 / 29 全绿**（首次取基线） |
| `skip/only/todo` | 0 | 0 |
| views/admin 无测试页 | AdminApis、AdminBizSystems、AdminConnector、AdminKnowledgeBase(壳)、AdminRuntimeSpecs、UnifiedReview、MyApplications、FieldManagement、AdminFeedback、AdminLoginLogs | 仅 AdminComingSoonPlaceholder（占位页，明确不测） |
| 零测试组件/工具补上 | — | RuntimeSpecEditor、RuntimeSpecUserDialog、McpEditor、ProviderSystemEditor、GovObjectDetail、ReviewRejectDialog、ListPagination、PositionPersonaTab、PositionIntakeTab、PositionBusinessSystemTab、apiConnectorMock、userPositionMock、knowledgeBaseMeta、govDialogs、iconDisplay、theme、useConfirm、useDynPageSize |
| 页面级真 EP 冒烟 | 0 | 用户/角色/岗位/技能/专家/模型/运行规格/API/业务系统 9 页 + 专家编辑器/模型编辑器 |
| 合并/拆分 | — | adminSkillEdit 6→1、SkillCreateDialog 2→1、skillFileTree 1→3、positionModel 1→4、bizSystemMeta 校验搬 defValidate |
| 改名 | — | adminKnowledgeBase→knowledgeBaseList、ApiEditorReadWriteError→ApiEditor、connectorEditorsPrototypeLayout→connectorEditorsLayout、adminApisPaging→adminApis |
| 产品代码改动 | — | 仅 `domainExpertMock.js __resetExpertMock` +3 行（测试复位函数） |
| `npm run build` | 通过 | 通过 |

**删除反证**（单条，共 11 处）：adminUsers 幽灵 icons mock；adminMcp `relistMcpService` 桩 + 两处 not 断言（组件不 import）、「页头不再有角标」（08-22 已移除，md 无条款）、el-card/el-pagination/StatusTag/HealthTag 无效桩；ApiEditor 两处幽灵桩；ToolDockBizSystem「不调 listBizSystems」空断言；bizSystemMeta「不再校验 code」「CRED_STATE 常量等于自身」；connectorEditors 两条「同行」（jsdom 不验布局、md 无条款）与 `is-card`/`.pr-add` 纯原型视觉；toolKey `TOOL_TYPES` 并入 normType；govSeedRefIntegrity 4 条单行用例由全量遍历覆盖；adminApisPaging 4 条复刻逻辑用例由真挂载 adminApis 前 4 条覆盖；adminRoles 时间列 165 像素断言（K16 缺陷不固化）。

**能红验证**：各组共 25 次「改坏产品代码 → 用例红 → 还原（git diff 零改动）」，其中 `listPagination.test.js` 把 09-11 的 `watch(current)` 提到 `const current` 之前复现 TDZ → **13/13 红**；`apiConnectorMock` 去掉验证失效 → 6 红；`adminMcp` 删 `loadPubSummary()` → 4 红。

**执行中新发现（追加）**
- 代码缺陷 **K44**（低）`DrawerEditor.vue:72-74` 标题拼「新建」+entity → McpEditor 登记态抽屉标题「新建MCP」，md MCP §三.1 L199 写「登记 MCP」（底部按钮已是「登记」）。
- 代码缺陷 **K45**（低）`BizSystemEditor.vue:257` 查看态仍调 `listBizSystemSkills`（区块不展示，多一次无用请求）。
- 代码缺陷 **K46**（低）`AdminUsers.vue:50-55` `onSortChange` 兼容壳已无测试消费方，可退役。
- 待裁决 **J22**（配置）：真 ElForm 校验在 jsdom 下跑不通——`async-validator` 是 CJS，element-plus 被 Node 外置加载后 `new AsyncValidator()` 抛错且被 `ElForm.validate` 吞成 `resolve(true)`（空表单点【接入】也能调 createModel）。在 `vitest.config.js` 加 `test.server.deps.inline: ['element-plus','async-validator']` 即可跑通（已验证后撤销）。本轮 A12 退化为「从 rules 取 validator 直接断言」；确认后可改回 DOM 红字断言。
- 环境事实：本仓 jsdom 下 `globalThis.localStorage` 为 undefined（Node 22+ 实验占位），mockPersist 走内存；持久化「读回」用例统一采用「注入内存版存储 + `vi.resetModules()` + 动态 import」范式；同文件其他用例对 mock 模块须**静态导入**（否则 resetModules 后拿到新实例随机假红——knowledgeBaseMock A6 已修）。
- 过程违规自报：两个实施 agent 各有一次 git 写命令（单文件 `git stash push/pop`、`git checkout -- 文件`）用于还原能红验证，效果等同 sed 还原、无副作用；已在任务书层面重申禁止。
